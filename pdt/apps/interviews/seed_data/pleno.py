"""100 questões de entrevista para nível Pleno em DevOps/DevSecOps.

Foco em operação real: Kubernetes, IaC, observability, CI/CD avançado,
DevSecOps, performance, troubleshooting, networking avançado e administração.
"""
from __future__ import annotations

PLENO_QUESTIONS: list[dict] = [
    # ── Kubernetes (15) ─────────────────────────────────────────────────────
    {
        "category": "Kubernetes",
        "statement": "Qual é o objeto schedulável mais básico no Kubernetes?",
        "statement_en": "What is the most basic schedulable object in Kubernetes?",
        "choices": [
            "O scheduler aloca diretamente cada `Container` como unidade independente da API do Kubernetes, atalho que ignora exatamente o cenário que mais importa evitar.",
            "O `Pod` é a menor unidade schedulável; o scheduler associa esse recurso a um nó e os containers dentro dele compartilham rede e volumes.",
            "O `ReplicaSet` é o objeto schedulável básico, pois define o número de réplicas e distribui cada réplica nos nós do cluster, erro que só é percebido quando o time de operação já está lidando com o incidente.",
            "O `Deployment` é o objeto schedulável básico, pois representa a aplicação e controla a alocação dos recursos nos nós, suposição que só vale em ambiente de desenvolvimento, não em produção.",
        ],
        "choices_en": [
            "The scheduler allocates each `Container` directly as an independent unit of the Kubernetes API, a shortcut that ignores exactly the scenario that matters most to avoid.",
            "The `Pod` is the smallest schedulable unit; the scheduler binds that resource to a node and containers inside it share network and volumes.",
            "The `ReplicaSet` is the basic schedulable object because it defines replica count and places each replica on cluster nodes, a mistake usually noticed only when ops is already handling the incident.",
            "The `Deployment` is the basic schedulable object because it represents the application and controls how resources are placed on nodes, an assumption that only holds in development, not production.",
        ],
        "correct_index": 1,
        "explanation": "Pod é a menor unidade schedulável: agrupa um ou mais containers que compartilham namespace de rede e volumes. `Container` é a unidade do runtime (OCI), abaixo da API do K8s. `ReplicaSet` e `Deployment` são controllers que gerenciam Pods, mas não são schedulados diretamente.",
        "explanation_en": "A Pod is the smallest schedulable unit: it groups one or more containers that share a network namespace and volumes. `Container` is the runtime (OCI) unit, below the K8s API. `ReplicaSet` and `Deployment` are controllers that manage Pods, but they are not scheduled directly.",
    },
    {
        "category": "Kubernetes",
        "statement": "Qual a diferença prática entre `Deployment` e `StatefulSet`?",
        "statement_en": "What is the practical difference between `Deployment` and `StatefulSet`?",
        "choices": [
            "No uso típico, o `StatefulSet` cria pods sem identidade estável e o `Deployment` é preferido para bancos com PVC dedicado por réplica, atalho que parece seguro isolado, mas quebra quando combinado com outros sistemas.",
            "O `Deployment` trata pods como intercambiáveis e sem identidade fixa; `StatefulSet` mantém ordinal estável, hostname previsível (`pod-0`, `pod-1`) e um PVC dedicado por réplica, ideal para sistemas com estado como bancos.",
            "Ambos preservam identidade estável dos pods; a única diferença é que `StatefulSet` exige um Service do tipo `LoadBalancer` e `Deployment` exige um Service do tipo `ClusterIP`, suposição que ignora como o recurso realmente se comporta em escala.",
            "O `Deployment` é o recurso recomendado para workloads stateful, enquanto o `StatefulSet` funciona como um `DaemonSet` simplificado (um pod por nó), abordagem que funciona bem até o primeiro pico de carga real.",
        ],
        "choices_en": [
            "In typical use, `StatefulSet` creates pods without stable identity and `Deployment` is preferred for databases with a dedicated PVC per replica, a shortcut that looks safe in isolation but breaks when combined with other systems.",
            "`Deployment` treats pods as interchangeable with no fixed identity; `StatefulSet` keeps a stable ordinal, predictable hostname (`pod-0`, `pod-1`), and a dedicated PVC per replica, suited for stateful systems such as databases.",
            "Both preserve stable pod identity; the only difference is that `StatefulSet` requires a `LoadBalancer` Service and `Deployment` requires a `ClusterIP` Service, an assumption that ignores how the resource actually behaves at scale.",
            "`Deployment` is the recommended resource for stateful workloads, while `StatefulSet` acts like a simplified `DaemonSet` (one pod per node), an approach that works fine until the first real load spike.",
        ],
        "correct_index": 1,
        "explanation": "StatefulSet garante hostname estável, ordem de criação/deleção e um PVC por réplica. A primeira inverte os papéis (erro frequente em quem nunca operou banco em K8s). A terceira inventa um requisito de Service que não existe. A quarta confunde StatefulSet com DaemonSet, que é o recurso de 1-pod-por-nó.",
        "explanation_en": "StatefulSet guarantees stable hostname, ordered create/delete, and one PVC per replica. The first option swaps the roles (a common mistake for anyone who has never run a database on K8s). The third invents a Service requirement that does not exist. The fourth confuses StatefulSet with DaemonSet, which is the one-pod-per-node resource.",
    },
    {
        "category": "Kubernetes",
        "statement": "Para que serve um `Service` do tipo `ClusterIP`?",
        "statement_en": "What is a `ClusterIP` `Service` for?",
        "choices": [
            "Aloca uma porta alta em vários nós (30000-32767) e expõe o Service externamente através do IP de qualquer nó do cluster, suposição que só se sustenta enquanto o time é pequeno.",
            "Provisiona um load balancer L4 no provedor de nuvem com IP público, integrando ao Service via controller específico (ex.: `aws-load-balancer-controller`), algo que passa no code review quando ninguém olha com atenção.",
            "Atribui um IP virtual estável dentro do cluster e balanceia tráfego entre os pods que casam com o `selector`; é o tipo padrão e só é alcançável internamente.",
            "Cria uma rota L7 no Ingress Controller para mapear hostnames externos a backends, com terminação TLS no proxy reverso, prática que passa despercebida até uma auditoria de segurança.",
        ],
        "choices_en": [
            "It allocates a high port on several nodes (30000-32767) and exposes the Service externally through any node's IP, an assumption that only holds while the team is small.",
            "It provisions an L4 load balancer in the cloud provider with a public IP, wiring it to the Service via a specific controller (e.g. `aws-load-balancer-controller`), something that slips through code review when nobody looks carefully.",
            "It assigns a stable virtual IP inside the cluster and balances traffic across pods matching the `selector`; it is the default type and is reachable only internally.",
            "It creates an L7 route on the Ingress Controller to map external hostnames to backends, with TLS termination on the reverse proxy, a practice that goes unnoticed until a security audit.",
        ],
        "correct_index": 2,
        "explanation": "ClusterIP é o tipo padrão e funciona apenas dentro do cluster. A primeira descreve `NodePort`. A segunda descreve `LoadBalancer`. A quarta descreve um `Ingress`, que opera em L7 (não é um tipo de Service).",
        "explanation_en": "ClusterIP is the default type and works only inside the cluster. The first option describes `NodePort`. The second describes `LoadBalancer`. The fourth describes an `Ingress`, which operates at L7 (not a Service type).",
    },
    {
        "category": "Kubernetes",
        "statement": "O que é um `ConfigMap` e como ele se diferencia de outros objetos?",
        "statement_en": "What is a `ConfigMap` and how does it differ from other objects?",
        "choices": [
            "Objeto da API que armazena dados de configuração não sensíveis em pares chave/valor, podendo ser consumido como variável de ambiente, argumento de linha de comando ou volume montado pelos pods.",
            "Objeto que armazena dados sensíveis (senhas, tokens) codificados em base64, com criptografia em repouso ativada por padrão a partir do K8s 1.20, comportamento que confunde quem está debugando meses depois.",
            "Recurso que define um novo tipo de objeto na API do Kubernetes (`CustomResourceDefinition`), permitindo que controllers customizados o reconciliem, atalho que parece seguro isolado, mas quebra quando combinado com outros sistemas.",
            "Objeto que mantém o mapeamento entre `Service` e seus pods, atualizado pelo `kube-controller-manager` quando o `selector` muda, que só aparece como problema depois que o sistema já está em produção.",
        ],
        "choices_en": [
            "An API object that stores non-sensitive configuration data as key/value pairs, consumable as environment variables, command-line arguments, or a volume mounted by pods.",
            "An object that stores sensitive data (passwords, tokens) base64-encoded, with encryption at rest enabled by default since K8s 1.20, behavior that confuses whoever debugs months later.",
            "A resource that defines a new object type in the Kubernetes API (`CustomResourceDefinition`), letting custom controllers reconcile it, a shortcut that looks safe in isolation but breaks when combined with other systems.",
            "An object that keeps the mapping between a `Service` and its pods, updated by `kube-controller-manager` when the `selector` changes, which only shows up as a problem after the system is already in production.",
        ],
        "correct_index": 0,
        "explanation": "ConfigMap desacopla configuração da imagem para dados não sensíveis. A segunda descreve `Secret` (e ainda exagera: criptografia em repouso não é padrão, depende de configurar o `EncryptionConfiguration` no API server). A terceira descreve `CRD`. A quarta descreve `Endpoints`/`EndpointSlice`.",
        "explanation_en": "ConfigMap decouples configuration from the image for non-sensitive data. The second describes `Secret` (and overstates: encryption at rest is not default; it depends on configuring `EncryptionConfiguration` on the API server). The third describes a `CRD`. The fourth describes `Endpoints`/`EndpointSlice`.",
    },
    {
        "category": "Kubernetes",
        "statement": "Qual é a diferença entre livenessProbe e readinessProbe?",
        "statement_en": "What is the difference between livenessProbe and readinessProbe?",
        "choices": [
            "Readiness reinicia o container quando falha, enquanto liveness só remove o pod dos endpoints, erro típico de configuração feita às pressas, sem revisão posterior.",
            "Liveness verifica se o pod esta pronto para trafego, enquanto readiness detecta travamento e reinicia, algo que passa no code review quando ninguém olha com atenção.",
            "Liveness detecta travamento e pode reiniciar o container, enquanto readiness controla entrada e saida dos endpoints.",
            "Liveness e readiness tem o mesmo efeito, mudando só o protocolo do probe, suposição que só vale em ambiente de desenvolvimento, não em produção.",
        ],
        "choices_en": [
            "Readiness restarts the container on failure, while liveness only removes the pod from endpoints, a typical error from rushed configuration without later review.",
            "Liveness checks whether the pod is ready for traffic, while readiness detects hangs and restarts, something that slips through code review when nobody looks carefully.",
            "Liveness detects hangs and may restart the container, while readiness controls joining and leaving endpoints.",
            "Liveness and readiness have the same effect, differing only in the probe protocol, an assumption that only holds in development, not production.",
        ],
        "correct_index": 2,
        "explanation": "Liveness reinicia; Readiness só remove dos Endpoints. A primeira inverte os papéis. A segunda confunde com `startupProbe`. A quarta inventa uma diferença de protocolo que não existe (ambas suportam HTTP, TCP e exec).",
        "explanation_en": "Liveness restarts; Readiness only removes from Endpoints. The first swaps the roles. The second confuses this with `startupProbe`. The fourth invents a protocol difference that does not exist (both support HTTP, TCP, and exec).",
    },
    {
        "category": "Kubernetes",
        "statement": "Qual a função de um `Ingress` em Kubernetes?",
        "statement_en": "What is the role of an `Ingress` in Kubernetes?",
        "choices": [
            "Recurso da API que define regras L7 (host/path) roteadas para `Services` por um `Ingress Controller` (ex.: NGINX, Traefik), suportando terminação TLS e cabeçalhos customizados.",
            "Tipo especial de `Service` que provisiona automaticamente um load balancer público no provedor de nuvem e distribui tráfego entre pods em camada 4, suposição que vale só até o primeiro imprevisto de rede ou hardware.",
            "Recurso deprecated em K8s 1.22+ e substituído integralmente pelo `Gateway API`; novos clusters não devem mais utilizá-lo, resultado típico de copiar configuração de outro projeto sem adaptar.",
            "Objeto que define quais pods podem aceitar conexões de quais origens com base em labels e namespaces, restringindo tráfego intra-cluster, decisão que parece razoável isolada, mas quebra a arquitetura no conjunto.",
        ],
        "choices_en": [
            "An API resource that defines L7 rules (host/path) routed to `Services` by an `Ingress Controller` (e.g. NGINX, Traefik), supporting TLS termination and custom headers.",
            "A special `Service` type that automatically provisions a public load balancer in the cloud provider and distributes traffic across pods at layer 4, an assumption that holds only until the first network or hardware surprise.",
            "A resource deprecated in K8s 1.22+ and fully replaced by the `Gateway API`; new clusters should no longer use it, a typical result of copying config from another project without adapting it.",
            "An object that defines which pods may accept connections from which sources based on labels and namespaces, restricting intra-cluster traffic, a decision that seems reasonable in isolation but breaks the architecture overall.",
        ],
        "correct_index": 0,
        "explanation": "Ingress + IngressController dão roteamento L7 e TLS. A segunda descreve `Service` `LoadBalancer` (L4). A terceira é falsa: o Gateway API é uma alternativa moderna, mas Ingress continua estável e suportado. A quarta descreve `NetworkPolicy`.",
        "explanation_en": "Ingress plus IngressController provide L7 routing and TLS. The second describes a `LoadBalancer` `Service` (L4). The third is false: Gateway API is a modern alternative, but Ingress remains stable and supported. The fourth describes `NetworkPolicy`.",
    },
    {
        "category": "Kubernetes",
        "statement": "Sobre requests e limits em recursos de um Pod, qual afirmacao esta correta?",
        "statement_en": "About requests and limits on Pod resources, which statement is correct?",
        "choices": [
            "No agendamento, limits atua como reserva minima e requests define o teto em runtime, comportamento que confunde quem está debugando meses depois.",
            "Requests e limits sao equivalentes na pratica, mudando só o nome por compatibilidade, prática que gera falso senso de segurança no time.",
            "Requests vale só para memoria e limits vale só para CPU no manifesto, erro comum de quem aprendeu por tentativa e erro, sem revisar a documentação oficial.",
            "Requests guia o scheduler no agendamento, e limits define teto de uso aplicado pelo kernel.",
        ],
        "choices_en": [
            "At scheduling time, limits act as the minimum reservation and requests define the runtime ceiling, behavior that confuses whoever debugs months later.",
            "Requests and limits are equivalent in practice, differing only by name for compatibility, a practice that gives the team a false sense of safety.",
            "Requests apply only to memory and limits only to CPU in the manifest, a common mistake from learning by trial and error without reading official docs.",
            "Requests guide the scheduler at placement time, and limits define the usage ceiling enforced by the kernel.",
        ],
        "correct_index": 3,
        "explanation": "Resposta certa: requests = reserva (scheduling), limits = teto (cgroups). A primeira inverte os papeis, erro comum em quem usa so kubectl top. A segunda sugere obsolescencia inexistente (os dois sao ativos). A terceira inventa uma divisao por tipo de recurso que nao existe; ambos os campos aceitam CPU, memoria e recursos estendidos.",
        "explanation_en": "Correct answer: requests = reservation (scheduling), limits = ceiling (cgroups). The first swaps the roles, a common error for anyone who only uses kubectl top. The second suggests nonexistent obsolescence (both are active). The third invents a split by resource type that does not exist; both fields accept CPU, memory, and extended resources.",
    },
    {
        "category": "Kubernetes",
        "statement": "Qual a forma idiomática de injetar um `Secret` como variável de ambiente em um Pod?",
        "statement_en": "What is the idiomatic way to inject a `Secret` as an environment variable in a Pod?",
        "choices": [
            "Referenciar a chave do `Secret` no manifest do Pod via `valueFrom.secretKeyRef` (ou `envFrom.secretRef` para todas as chaves de uma vez).",
            "Definir o conteúdo do `Secret` em texto plano dentro de `env.value`, deixando que o kubelet substitua automaticamente em tempo de admission.",
            "Só montar o `Secret` como volume em `/etc/secrets` e usar `cat` no `command` do container para exportar como variável de ambiente.",
            "Usar `valueFrom.configMapKeyRef`, que aceita tanto `ConfigMap` quanto `Secret` desde a versão 1.18 do Kubernetes.",
        ],
        "choices_en": [
            "Reference the `Secret` key in the Pod manifest via `valueFrom.secretKeyRef` (or `envFrom.secretRef` for all keys at once).",
            "Put the `Secret` content in plain text inside `env.value`, letting the kubelet substitute it automatically at admission time.",
            "Only mount the `Secret` as a volume at `/etc/secrets` and use `cat` in the container `command` to export it as an environment variable.",
            "Use `valueFrom.configMapKeyRef`, which accepts both `ConfigMap` and `Secret` since Kubernetes 1.18.",
        ],
        "correct_index": 0,
        "explanation": "`secretKeyRef` é o caminho oficial. A segunda inventa uma 'substituição em admission' inexistente (e ainda derrota o propósito do Secret). A terceira é workaround sujo, não idiomático. A quarta é falsa: `configMapKeyRef` só lê ConfigMap; existe `secretKeyRef` justamente porque os tipos são separados.",
        "explanation_en": "`secretKeyRef` is the official path. The second invents a nonexistent 'admission substitution' (and defeats the purpose of Secret). The third is a dirty workaround, not idiomatic. The fourth is false: `configMapKeyRef` only reads ConfigMap; `secretKeyRef` exists precisely because the types are separate.",
    },
    {
        "category": "Kubernetes",
        "statement": "Como autoescalar Pods com base em métricas customizadas (ex.: tamanho de fila SQS)?",
        "statement_en": "How do you autoscale Pods based on custom metrics (e.g. SQS queue size)?",
        "choices": [
            "Configurar `HPA` com um adapter de métricas externas (ex.: `prometheus-adapter` via API `external.metrics.k8s.io`) ou utilizar `KEDA`, que oferece scalers prontos para SQS, Kafka, Redis etc.",
            "Usar `VPA` (Vertical Pod Autoscaler), que ajusta automaticamente requests/limits dos pods em função de métricas customizadas como queue length, erro que só é percebido quando o time de operação já está lidando com o incidente.",
            "Habilitar o `Cluster Autoscaler` com regras customizadas em annotations dos nós; ele escala os pods conforme a métrica externa atinge thresholds, prática que funciona em teste, mas falha sob carga real de produção.",
            "Criar um `CronJob` que executa `kubectl scale deployment/api --replicas=N` periodicamente conforme a métrica do Prometheus, atalho que parece seguro isolado, mas quebra quando combinado com outros sistemas.",
        ],
        "choices_en": [
            "Configure `HPA` with an external metrics adapter (e.g. `prometheus-adapter` via the `external.metrics.k8s.io` API) or use `KEDA`, which offers ready-made scalers for SQS, Kafka, Redis, and more.",
            "Use `VPA` (Vertical Pod Autoscaler), which automatically adjusts pod requests/limits from custom metrics such as queue length, a mistake usually noticed only when ops is already handling the incident.",
            "Enable `Cluster Autoscaler` with custom rules in node annotations; it scales pods as the external metric hits thresholds, a practice that works in tests but fails under real production load.",
            "Create a `CronJob` that periodically runs `kubectl scale deployment/api --replicas=N` based on the Prometheus metric, a shortcut that looks safe in isolation but breaks when combined with other systems.",
        ],
        "correct_index": 0,
        "explanation": "HPA + adapter (ou KEDA) é o caminho. O VPA escala verticalmente (requests/limits), não horizontalmente. O `Cluster Autoscaler` escala nós, não pods. A quarta opção é gambiarra: `kubectl scale` via cron derrota a finalidade de uma escala reativa baseada em métrica.",
        "explanation_en": "HPA plus an adapter (or KEDA) is the path. VPA scales vertically (requests/limits), not horizontally. `Cluster Autoscaler` scales nodes, not pods. The fourth option is a hack: `kubectl scale` via cron defeats reactive metric-based scaling.",
    },
    {
        "category": "Kubernetes",
        "statement": "Qual recurso garante que exatamente um Pod por nó seja executado (ex.: agente de log)?",
        "statement_en": "Which resource guarantees exactly one Pod per node is running (e.g. a log agent)?",
        "choices": [
            "Deployment",
            "DaemonSet",
            "Job",
            "ReplicaSet",
        ],
        "choices_en": [
            "Deployment",
            "DaemonSet",
            "Job",
            "ReplicaSet",
        ],
        "correct_index": 1,
        "explanation": "DaemonSet escala 1 pod por nó (com `tolerations`/`nodeSelector` é possível restringir o conjunto). `Deployment`/`ReplicaSet` escalam um número fixo de réplicas, sem garantia de distribuição por nó. `Job` executa até completar com sucesso, não é um workload contínuo.",
        "explanation_en": "DaemonSet scales one pod per node (with `tolerations`/`nodeSelector` you can restrict the set). `Deployment`/`ReplicaSet` scale a fixed replica count with no per-node guarantee. `Job` runs until successful completion; it is not a continuous workload.",
    },
    {
        "category": "Kubernetes",
        "statement": "Qual é a função de uma `NetworkPolicy`?",
        "statement_en": "What is the purpose of a `NetworkPolicy`?",
        "choices": [
            "Define como um `Service` distribui tráfego entre pods com afinidade de sessão e `externalTrafficPolicy` (`Local` ou `Cluster`), prática ainda comum em sistema legado que raramente é atualizado.",
            "Aplica políticas de segurança em nível de pod (drop de capabilities, `runAsNonRoot`) substituindo o `PodSecurityPolicy` removido na 1.25, atalho comum quando o prazo aperta e ninguém revisa depois.",
            "Configura mTLS automático entre serviços e cria regras de autorização L7 baseadas em JWT, exigindo que cada pod tenha um sidecar injetado, que só aparece como problema depois que o sistema já está em produção.",
            "Restringe tráfego ingress/egress entre pods com base em `podSelector`, `namespaceSelector` e regras de porta; depende de um CNI compatível como Calico ou Cilium para enforcement.",
        ],
        "choices_en": [
            "It defines how a `Service` distributes traffic across pods with session affinity and `externalTrafficPolicy` (`Local` or `Cluster`), a practice still common in legacy systems that are rarely updated.",
            "It applies pod-level security policies (drop capabilities, `runAsNonRoot`), replacing `PodSecurityPolicy` removed in 1.25, a common shortcut when the deadline is tight and nobody reviews later.",
            "It configures automatic mTLS between services and creates L7 authorization rules based on JWT, requiring each pod to have an injected sidecar, which only shows up as a problem after the system is already in production.",
            "It restricts ingress/egress traffic between pods based on `podSelector`, `namespaceSelector`, and port rules; it depends on a compatible CNI such as Calico or Cilium for enforcement.",
        ],
        "correct_index": 3,
        "explanation": "NetworkPolicy é zero-trust intra-cluster, mas exige CNI que implemente o enforcement. A primeira descreve configuração de Service. A segunda descreve `Pod Security Admission` (que substitui PSP). A terceira descreve uma `AuthorizationPolicy` do Istio, não NetworkPolicy.",
        "explanation_en": "NetworkPolicy is intra-cluster zero-trust, but needs a CNI that implements enforcement. The first describes Service configuration. The second describes `Pod Security Admission` (which replaces PSP). The third describes an Istio `AuthorizationPolicy`, not NetworkPolicy.",
    },
    {
        "category": "Kubernetes",
        "statement": "Qual é a ordem de execução de `initContainers` em relação aos containers principais de um Pod?",
        "statement_en": "What is the execution order of `initContainers` relative to a Pod's main containers?",
        "choices": [
            "Rodam como sidecars em paralelo aos principais durante grande parte do ciclo de vida do Pod, ideais para agentes de log e proxies, suposição que raramente se sustenta fora do ambiente controlado de laboratório.",
            "Rodam antes dos principais, sequencialmente, e cada um precisa terminar com `exit 0` para o próximo iniciar; só então os containers principais sobem.",
            "Executam após o container principal iniciar, equivalentes ao hook `postStart`; uso típico é registrar o pod em um service registry externo, abordagem que ignora o cenário de falha mais provável na prática.",
            "Rodam antes dos principais, porém em paralelo entre si; o pod inicia logo que qualquer um dos init containers retorna `exit 0`, prática ainda comum em sistema legado que raramente é atualizado.",
        ],
        "choices_en": [
            "They run as sidecars in parallel with the main containers for much of the Pod lifecycle, suited for log agents and proxies, an assumption that rarely holds outside a controlled lab environment.",
            "They run before the main ones, sequentially, and each must finish with `exit 0` for the next to start; only then do the main containers come up.",
            "They run after the main container starts, equivalent to the `postStart` hook; typical use is registering the pod in an external service registry, an approach that ignores the most likely failure scenario in practice.",
            "They run before the main ones, but in parallel with each other; the pod starts as soon as any init container returns `exit 0`, a practice still common in legacy systems that are rarely updated.",
        ],
        "correct_index": 1,
        "explanation": "Init containers rodam em sequência, antes dos principais, e cada falha bloqueia o pod. A primeira descreve `sidecar containers` (que ganharam suporte oficial em 1.28 com `restartPolicy: Always` em init). A terceira confunde com `postStart` lifecycle hook. A quarta inventa paralelismo entre inits que não existe.",
        "explanation_en": "Init containers run in sequence, before the main ones, and any failure blocks the pod. The first describes `sidecar containers` (official support in 1.28 with `restartPolicy: Always` on init). The third confuses this with the `postStart` lifecycle hook. The fourth invents parallelism among inits that does not exist.",
    },
    {
        "category": "Kubernetes",
        "statement": "O que é o padrão *operator* em Kubernetes?",
        "statement_en": "What is the *operator* pattern in Kubernetes?",
        "choices": [
            "Webhook de admission que valida ou muta recursos durante a criação, executando lógica de compliance escrita pelo time de plataforma, escolha que economiza tempo agora e cobra o preço mais tarde.",
            "Pacote versionado de manifests com templating Go, instalado via `helm install` e atualizado em rollouts sequenciais sem necessidade de controller customizado, suposição incorreta sobre como o sistema realmente se comporta sob estresse.",
            "Padrão que estende a API do Kubernetes com `CustomResourceDefinitions` (CRDs) e um controller dedicado, codificando expertise operacional para gerenciar aplicações stateful complexas (ex.: `prometheus-operator`).",
            "Tipo especial de `Deployment` que ganha permissões elevadas no cluster e permite executar rollbacks automáticos sem definir CRDs, suposição que só se sustenta enquanto o time é pequeno.",
        ],
        "choices_en": [
            "An admission webhook that validates or mutates resources during creation, running compliance logic written by the platform team, a choice that saves time now and collects the cost later.",
            "A versioned package of manifests with Go templating, installed via `helm install` and updated in sequential rollouts without needing a custom controller, an incorrect assumption about how the system really behaves under stress.",
            "A pattern that extends the Kubernetes API with `CustomResourceDefinitions` (CRDs) and a dedicated controller, encoding operational expertise to manage complex stateful applications (e.g. `prometheus-operator`).",
            "A special `Deployment` type that gains elevated cluster permissions and allows automatic rollbacks without defining CRDs, an assumption that only holds while the team is small.",
        ],
        "correct_index": 2,
        "explanation": "Operator = CRD + controller que reconcilia. A primeira descreve `MutatingAdmissionWebhook`/`ValidatingAdmissionWebhook`. A segunda descreve Helm chart, que é apenas templating estático sem reconciliação contínua. A quarta inventa um tipo de Deployment privilegiado inexistente.",
        "explanation_en": "Operator = CRD plus a reconciling controller. The first describes `MutatingAdmissionWebhook`/`ValidatingAdmissionWebhook`. The second describes a Helm chart, which is only static templating without continuous reconciliation. The fourth invents a nonexistent privileged Deployment type.",
    },
    {
        "category": "Kubernetes",
        "statement": "Diferença entre as estratégias RollingUpdate e Recreate em um Deployment:",
        "statement_en": "Difference between RollingUpdate and Recreate strategies on a Deployment:",
        "choices": [
            "Recreate é a estratégia padrão e mantém disponibilidade durante grande parte da atualização.",
            "RollingUpdate substitui pods gradualmente; Recreate remove os antigos e depois cria os novos.",
            "RollingUpdate depende de aprovação manual por revisão; Recreate aplica canário por porcentagem.",
            "As duas estratégias têm o mesmo comportamento; muda só se o acesso é via Service ou Ingress.",
        ],
        "choices_en": [
            "Recreate is the default strategy and keeps availability during most of the update.",
            "RollingUpdate replaces pods gradually; Recreate removes the old ones and then creates the new ones.",
            "RollingUpdate depends on manual review approval; Recreate applies a canary by percentage a practice that goes unnoticed until a security audit.",
            "Both strategies behave the same; only whether access is via Service or Ingress changes.",
        ],
        "correct_index": 1,
        "explanation": "`RollingUpdate` é o padrão e mantém disponibilidade. A primeira inverte os papéis. A terceira descreve uma estratégia blue-green/canário, não nativa do Deployment. A quarta inventa um mecanismo de controle inexistente; a estratégia é definida em `spec.strategy` do Deployment.",
        "explanation_en": "`RollingUpdate` is the default and keeps availability. The first swaps the roles. The third describes a blue-green/canary strategy, not native to Deployment. The fourth invents a nonexistent control mechanism; the strategy is set in the Deployment's `spec.strategy`.",
    },
    {
        "category": "Kubernetes",
        "statement": "Qual é a forma idiomática de persistir dados em um Pod no Kubernetes?",
        "statement_en": "What is the idiomatic way to persist data in a Pod on Kubernetes?",
        "choices": [
            "Usar volume `emptyDir`, que persiste os dados em disco do nó mesmo após o pod ser reagendado para outro nó do cluster, suposição que ignora como o recurso realmente se comporta em escala.",
            "Declarar um `PersistentVolumeClaim` no Pod, vinculado a um `PersistentVolume` provisionado estaticamente ou dinamicamente via `StorageClass` (ex.: `gp3` na AWS, `csi-disk` na GCP).",
            "Montar um `hostPath` direto do diretório `/var/lib/data` do nó; o kubelet replica automaticamente o conteúdo entre nós para garantir durabilidade, suposição que raramente se sustenta fora do ambiente controlado de laboratório.",
            "Salvar os dados em um `ConfigMap` dedicado e remontá-lo a cada deploy, já que ConfigMaps suportam até 10 GiB de payload por chave, prática que funciona em teste, mas falha sob carga real de produção.",
        ],
        "choices_en": [
            "Use an `emptyDir` volume, which persists data on the node disk even after the pod is rescheduled to another cluster node, an assumption that ignores how the resource actually behaves at scale.",
            "Declare a `PersistentVolumeClaim` on the Pod, bound to a `PersistentVolume` provisioned statically or dynamically via a `StorageClass` (e.g. `gp3` on AWS, `csi-disk` on GCP).",
            "Mount a `hostPath` directly from the node's `/var/lib/data` directory; the kubelet automatically replicates content across nodes for durability, an assumption that rarely holds outside a controlled lab environment.",
            "Save data in a dedicated `ConfigMap` and remount it on every deploy, since ConfigMaps support up to 10 GiB of payload per key, a practice that works in tests but fails under real production load.",
        ],
        "correct_index": 1,
        "explanation": "PVC + PV (com StorageClass para provisionamento dinâmico) é o padrão. A primeira é falsa: `emptyDir` é efêmero e morre com o pod. A terceira é falsa: `hostPath` fica preso ao nó e o kubelet *não* replica nada. A quarta exagera: ConfigMap tem limite de 1 MiB por objeto, e não é um mecanismo de storage.",
        "explanation_en": "PVC plus PV (with StorageClass for dynamic provisioning) is the pattern. The first is false: `emptyDir` is ephemeral and dies with the pod. The third is false: `hostPath` is stuck to the node and the kubelet does *not* replicate anything. The fourth overstates: ConfigMap has a 1 MiB limit per object and is not a storage mechanism.",
    },
    # ── Docker avançado (8) ─────────────────────────────────────────────────
    {
        "category": "Docker avançado",
        "statement": "Qual a diferença prática entre `COPY` e `ADD` em um Dockerfile?",
        "statement_en": "What is the practical difference between `COPY` and `ADD` in a Dockerfile?",
        "choices": [
            "O `COPY` aceita URLs remotas e extrai tarballs automaticamente; o `ADD` limita-se a copiar arquivos locais do contexto de build, prática que só aparece como erro grave durante um incidente real, atalho que funciona hoje mas complica a próxima migração.",
            "O `ADD` aceita URLs remotas e pode extrair automaticamente tarballs locais; `COPY` apenas copia arquivos do contexto de build, sem esse comportamento extra, e costuma ser o padrão recomendado por ser mais previsível.",
            "Com o BuildKit, `ADD` e `COPY` passam a ser equivalentes e o engine traduz `ADD` em `COPY` internamente, restando só `--chown` como diferença, que só aparece como problema depois que o sistema já está em produção, suposição que ignora como o recurso realmente se comporta em escala.",
            "O `COPY` mantém permissões originais por padrão, enquanto o `ADD` redefine permissões para `0644`, exigindo `RUN chmod` após a cópia, prática que aumenta a superfície de ataque sem ninguém perceber, escolha que economiza tempo agora e cobra o preço mais tarde.",
        ],
        "choices_en": [
            "`COPY` accepts remote URLs and extracts tarballs automatically; `ADD` only copies local files from the build context, a practice that only shows up as a serious error during a real incident, a shortcut that works today but complicates the next migration.",
            "`ADD` accepts remote URLs and can automatically extract local tarballs; `COPY` only copies files from the build context, without that extra behavior, and is usually the recommended default because it is more predictable.",
            "With BuildKit, `ADD` and `COPY` become equivalent and the engine translates `ADD` into `COPY` internally, leaving only `--chown` as a difference, which only shows up as a problem after the system is already in production, an assumption that ignores how the resource actually behaves at scale.",
            "`COPY` keeps original permissions by default, while `ADD` resets permissions to `0644`, requiring `RUN chmod` after the copy, a practice that grows the attack surface without anyone noticing, a choice that saves time now and collects the cost later.",
        ],
        "correct_index": 1,
        "explanation": "`ADD` tem comportamentos extras (URL + auto-extract); `COPY` é preferido por ser explícito. A primeira inverte os papéis. A terceira é falsa: `ADD` *não* é alias de `COPY` no BuildKit. A quarta inventa um comportamento de chmod inexistente.",
        "explanation_en": "`ADD` has extra behaviors (URL plus auto-extract); `COPY` is preferred because it is explicit. The first swaps the roles. The third is false: `ADD` is *not* an alias of `COPY` in BuildKit. The fourth invents a nonexistent chmod behavior.",
    },
    {
        "category": "Docker avançado",
        "statement": "Quais práticas mais reduzem o tamanho final de uma imagem Docker?",
        "statement_en": "Which practices most reduce the final size of a Docker image?",
        "choices": [
            "Combinar vários `RUN` numa única linha gigante, copiar o código antes das dependências e desativar o BuildKit para evitar layers extras, decisão que ignora justamente o motivo pelo qual a prática recomendada existe.",
            "Habilitar `--squash` em vários builds e instalar pacotes com `--no-install-recommends`; multi-stage não tem efeito porque o resultado é um único layer, prática que aumenta a superfície de ataque sem ninguém perceber.",
            "Usar `multi-stage build` para descartar artefatos de compilação, basear na menor imagem possível (`alpine`, `distroless`) e limpar caches do gerenciador de pacotes na mesma camada da instalação.",
            "Usar geralmente `ubuntu:latest` como base e fazer `apt-get install vim curl wget` em camada separada para que o cache funcione corretamente, suposição que vale só até o primeiro imprevisto de rede ou hardware.",
        ],
        "choices_en": [
            "Combine several `RUN` steps into one giant line, copy code before dependencies, and disable BuildKit to avoid extra layers, a decision that ignores exactly why the recommended practice exists.",
            "Enable `--squash` on several builds and install packages with `--no-install-recommends`; multi-stage has no effect because the result is a single layer, a practice that grows the attack surface without anyone noticing.",
            "Use a `multi-stage build` to discard build artifacts, base on the smallest possible image (`alpine`, `distroless`), and clean package-manager caches in the same layer as the install.",
            "Usually use `ubuntu:latest` as the base and run `apt-get install vim curl wget` in a separate layer so the cache works correctly, an assumption that holds only until the first network or hardware surprise.",
        ],
        "correct_index": 2,
        "explanation": "Multi-stage + base mínima + limpar cache na mesma layer. A primeira destrói cache e não reduz tamanho. A segunda é meia-verdade: `--squash` ajuda no número de layers, mas multi-stage continua sendo a técnica mais efetiva (e `--squash` é experimental). A quarta cresce a imagem com pacotes desnecessários e usa `latest`, antipattern de reprodutibilidade.",
        "explanation_en": "Multi-stage plus minimal base plus cleaning cache in the same layer. The first destroys cache and does not shrink size. The second is a half-truth: `--squash` helps layer count, but multi-stage remains the most effective technique (and `--squash` is experimental). The fourth grows the image with unnecessary packages and uses `latest`, a reproducibility antipattern.",
    },
    {
        "category": "Docker avançado",
        "statement": "Qual a função do `HEALTHCHECK` em um Dockerfile?",
        "statement_en": "What is the purpose of `HEALTHCHECK` in a Dockerfile?",
        "choices": [
            "Substitui completamente as `livenessProbe` e `readinessProbe` do Kubernetes; quando presente, o kubelet ignora as probes definidas no manifest do Pod, atalho comum quando o prazo aperta e ninguém revisa depois.",
            "Define o comando que será executado em primeiro plano dentro do container, equivalente a `ENTRYPOINT`, mas com retry automático em caso de erro, comportamento que gera alerta falso ou silencia alerta real, dependendo do caso.",
            "Configura uma porta de saúde dedicada que o engine expõe externamente em `/health` para que load balancers possam consultá-la diretamente, suposição que só se sustenta enquanto o time é pequeno.",
            "Define um comando que o engine executa periodicamente; o estado do container reflete o exit code (`0` saudável, `1` doente), permitindo que `docker ps`/Compose/Swarm reaja a falhas.",
        ],
        "choices_en": [
            "It fully replaces Kubernetes `livenessProbe` and `readinessProbe`; when present, the kubelet ignores probes defined in the Pod manifest, a common shortcut when the deadline is tight and nobody reviews later.",
            "It defines the command run in the foreground inside the container, equivalent to `ENTRYPOINT`, but with automatic retry on error, behavior that creates false alerts or silences real ones, depending on the case.",
            "It configures a dedicated health port that the engine exposes externally at `/health` so load balancers can probe it directly, an assumption that only holds while the team is small.",
            "It defines a command the engine runs periodically; the container state reflects the exit code (`0` healthy, `1` unhealthy), letting `docker ps`/Compose/Swarm react to failures.",
        ],
        "correct_index": 3,
        "explanation": "`HEALTHCHECK` muda o estado reportado pelo engine. A primeira é falsa: o kubelet usa as probes do manifest e ignora `HEALTHCHECK` da imagem. A segunda confunde com `ENTRYPOINT`. A terceira inventa uma 'porta de saúde' inexistente, `HEALTHCHECK` não expõe nada na rede.",
        "explanation_en": "`HEALTHCHECK` changes the state reported by the engine. The first is false: the kubelet uses manifest probes and ignores the image `HEALTHCHECK`. The second confuses it with `ENTRYPOINT`. The third invents a nonexistent 'health port'; `HEALTHCHECK` exposes nothing on the network.",
    },
    {
        "category": "Docker avançado",
        "statement": "Como evitar invalidações de cache desnecessárias em um Dockerfile?",
        "statement_en": "How do you avoid unnecessary cache invalidations in a Dockerfile?",
        "choices": [
            "Geralmente executar `docker build --no-cache` para garantir builds determinísticos; aproveitar cache é antipattern em pipelines de CI/CD, erro típico de configuração feita às pressas, sem revisão posterior.",
            "Ordenar instruções do Dockerfile do menos para o mais mutável (ex.: `COPY package.json` + `RUN npm ci` antes de `COPY.`), aproveitando o cache de layers até a primeira alteração real.",
            "Usar `ARG CACHEBUST=$(date +%s)` antes de cada `RUN` significativo, forçando o engine a invalidar o cache quando necessário, decisão que ignora justamente o motivo pelo qual a prática recomendada existe.",
            "Montar o diretório de código como volume durante o build e usar `RUN --mount=type=tmpfs` para evitar que arquivos modificados quebrem o cache, erro comum de quem aprendeu por tentativa e erro, sem revisar a documentação oficial.",
        ],
        "choices_en": [
            "Usually run `docker build --no-cache` to guarantee deterministic builds; using cache is an antipattern in CI/CD pipelines, a typical error from rushed configuration without later review.",
            "Order Dockerfile instructions from least to most mutable (e.g. `COPY package.json` plus `RUN npm ci` before `COPY.`), leveraging layer cache until the first real change.",
            "Use `ARG CACHEBUST=$(date +%s)` before each significant `RUN`, forcing the engine to invalidate cache when needed, a decision that ignores exactly why the recommended practice exists.",
            "Mount the code directory as a volume during the build and use `RUN --mount=type=tmpfs` so modified files do not break the cache, a common mistake from learning by trial and error without reading official docs.",
        ],
        "correct_index": 1,
        "explanation": "Layer order matters: dependências antes do código fonte. A primeira mata o ganho de cache. A terceira faz o oposto: invalidar o cache em todo build. A quarta confunde mount types, `tmpfs` não 'protege o cache', e o BuildKit já oferece `--mount=type=cache` para esse propósito.",
        "explanation_en": "Layer order matters: dependencies before source code. The first kills the cache benefit. The third does the opposite: invalidate cache on every build. The fourth confuses mount types; `tmpfs` does not 'protect the cache', and BuildKit already offers `--mount=type=cache` for that purpose.",
    },
    {
        "category": "Docker avançado",
        "statement": "O que caracteriza o BuildKit como backend de build do Docker?",
        "statement_en": "What characterizes BuildKit as Docker's build backend?",
        "choices": [
            "Runtime alternativo ao `containerd` para execução de containers em produção, focado em performance de fork e startup, ativado via `--runtime=buildkit`, suposição que ignora como o recurso realmente se comporta em escala.",
            "Backend moderno que adiciona builds paralelos por estágio, cache distribuído (`--cache-from`/`--cache-to`), montagens efêmeras (`--mount=type=secret`, `--mount=type=cache`) e exporters customizáveis (OCI, registry).",
            "Ferramenta da Red Hat que substitui o Docker daemon e roda builds rootless; é equivalente ao BuildKit, porém incompatível com Dockerfile, atalho que troca segurança por conveniência de curto prazo.",
            "Plugin do Docker Compose que paraleliza só downloads de imagens base; não modifica o algoritmo de build dos `Dockerfile`, suposição que só vale em ambiente de desenvolvimento, não em produção.",
        ],
        "choices_en": [
            "An alternative runtime to `containerd` for running production containers, focused on fork and startup performance, enabled via `--runtime=buildkit`, an assumption that ignores how the resource actually behaves at scale.",
            "A modern backend that adds parallel builds per stage, distributed cache (`--cache-from`/`--cache-to`), ephemeral mounts (`--mount=type=secret`, `--mount=type=cache`), and customizable exporters (OCI, registry).",
            "A Red Hat tool that replaces the Docker daemon and runs rootless builds; it is equivalent to BuildKit but incompatible with Dockerfile, a shortcut that trades security for short-term convenience.",
            "A Docker Compose plugin that only parallelizes base-image downloads; it does not change the `Dockerfile` build algorithm, an assumption that only holds in development, not production.",
        ],
        "correct_index": 1,
        "explanation": "BuildKit é o engine de build moderno (padrão no Docker recente). A primeira confunde build com runtime. A terceira descreve `Buildah`, que é uma ferramenta diferente (e suporta Dockerfile). A quarta minimiza absurdamente o impacto do BuildKit.",
        "explanation_en": "BuildKit is the modern build engine (default in recent Docker). The first confuses build with runtime. The third describes `Buildah`, a different tool (and it supports Dockerfile). The fourth absurdly minimizes BuildKit's impact.",
    },
    {
        "category": "Docker avançado",
        "statement": "Como reduzir dependência da internet pública em builds e ainda manter integridade?",
        "statement_en": "How do you reduce dependence on the public internet in builds while still keeping integrity?",
        "choices": [
            "Pinar versões exatas de dependências (`package-lock.json`, `requirements.txt` com hashes) e roteá-las por um proxy/registry interno (`Artifactory`, `Nexus`, `Verdaccio`) que cacheia e audita pacotes.",
            "Habilitar a flag `--network=none` em vários builds e copiar dependências manualmente para `/var/cache/apt` antes do `RUN`, abordagem que ignora o histórico de incidentes parecidos no setor.",
            "Trocar a base para `alpine` e instalar pacotes com `apk add --no-network`, que utiliza o repositório embutido na imagem, erro que só é percebido quando o time de operação já está lidando com o incidente.",
            "Usar `RUN curl -O <url> | sha256sum -c` para validar integridade; isso é suficiente, pois o build segue acessando a internet pública, suposição que raramente se sustenta fora do ambiente controlado de laboratório.",
        ],
        "choices_en": [
            "Pin exact dependency versions (`package-lock.json`, `requirements.txt` with hashes) and route them through an internal proxy/registry (`Artifactory`, `Nexus`, `Verdaccio`) that caches and audits packages.",
            "Enable the `--network=none` flag on several builds and manually copy dependencies into `/var/cache/apt` before `RUN`, an approach that ignores the history of similar incidents in the industry.",
            "Switch the base to `alpine` and install packages with `apk add --no-network`, which uses the repository embedded in the image, a mistake usually noticed only when ops is already handling the incident a practice that goes unnoticed until a security audit.",
            "Use `RUN curl -O <url> | sha256sum -c` to validate integrity; that is enough, since the build still reaches the public internet, an assumption that rarely holds outside a controlled lab environment.",
        ],
        "correct_index": 0,
        "explanation": "Proxy interno + pin + lockfile com hashes é a abordagem comum em supply chain. A segunda quebra o build (sem rede e sem deps copiadas previamente nada instala). A terceira inventa uma flag `--no-network` que não existe no `apk`. A quarta pega só uma parte do problema (integridade) e ignora indisponibilidade/auditoria.",
        "explanation_en": "Internal proxy plus pin plus lockfile with hashes is the common supply-chain approach. The second breaks the build (no network and no deps copied beforehand means nothing installs). The third invents a nonexistent `apk` `--no-network` flag. The fourth covers only part of the problem (integrity) and ignores availability/audit.",
    },
    {
        "category": "Docker avançado",
        "statement": "Por que assinar imagens (Notary v2, `cosign`) e validá-las antes da execução?",
        "statement_en": "Why sign images (Notary v2, `cosign`) and validate them before execution?",
        "choices": [
            "Detectar CVEs nos pacotes da imagem comparando os SHAs com o banco do `Trivy` ou `Grype`, falhando o pipeline em vulnerabilidades críticas, resultado típico de copiar configuração de outro projeto sem adaptar.",
            "Gerar um inventário de dependências (CycloneDX/SPDX) acoplado à imagem e armazená-lo no registry; substitui a necessidade de assinatura criptográfica, suposição que vale só até o primeiro imprevisto de rede ou hardware.",
            "Garantir integridade e proveniência da imagem antes da execução: o cluster verifica a assinatura (ex.: `cosign verify`) e só admite imagens vindas de uma raiz de confiança conhecida.",
            "Criptografar as camadas da imagem em repouso no registry e exigir uma chave KMS na hora do `docker pull`, prevenindo extração indevida, comportamento que gera alerta falso ou silencia alerta real, dependendo do caso.",
        ],
        "choices_en": [
            "To detect CVEs in image packages by comparing SHAs with the `Trivy` or `Grype` database, failing the pipeline on critical vulnerabilities, a typical result of copying config from another project without adapting it.",
            "To generate a dependency inventory (CycloneDX/SPDX) attached to the image and stored in the registry; it replaces the need for cryptographic signing, an assumption that holds only until the first network or hardware surprise.",
            "To guarantee image integrity and provenance before execution: the cluster verifies the signature (e.g. `cosign verify`) and only admits images from a known trust root.",
            "To encrypt image layers at rest in the registry and require a KMS key at `docker pull` time, preventing unauthorized extraction, behavior that creates false alerts or silences real ones, depending on the case.",
        ],
        "correct_index": 2,
        "explanation": "Assinatura responde a 'quem produziu isto?'. A primeira descreve scan de vulnerabilidades (resolve outro problema). A segunda descreve SBOM (complementar, mas não substitui assinatura). A quarta descreve criptografia em repouso, que protege contra leitura não autorizada, não contra adulteração.",
        "explanation_en": "Signing answers 'who produced this?'. The first describes vulnerability scanning (a different problem). The second describes SBOM (complementary, but not a substitute for signing). The fourth describes encryption at rest, which protects against unauthorized reads, not tampering.",
    },
    {
        "category": "Docker avançado",
        "statement": "O que é um SBOM (Software Bill of Materials)?",
        "statement_en": "What is an SBOM (Software Bill of Materials)?",
        "choices": [
            "Relatório consolidado de CVEs encontradas em uma imagem, gerado por `Trivy` ou `Grype` e atualizado a cada novo build do pipeline, algo que passa no code review quando ninguém olha com atenção.",
            "Software Bill of Materials: inventário estruturado das dependências, versões e licenças de uma imagem ou artefato, em formatos como CycloneDX ou SPDX, base para auditoria de supply chain.",
            "Manifest da imagem Docker contendo a lista de layers, comandos de cada `RUN` e checksums SHA256, exposto pelo endpoint `/v2/<image>/manifests` do registry, prática que funciona em teste, mas falha sob carga real de produção.",
            "Atestado em SLSA Level 3 que descreve o ambiente de build, runners e variáveis usadas; é assinado pelo CI mas não lista dependências diretamente, comportamento que só vira prioridade depois que já causou prejuízo.",
        ],
        "choices_en": [
            "A consolidated report of CVEs found in an image, generated by `Trivy` or `Grype` and updated on every new pipeline build, something that slips through code review when nobody looks carefully.",
            "Software Bill of Materials: a structured inventory of dependencies, versions, and licenses for an image or artifact, in formats such as CycloneDX or SPDX, a foundation for supply-chain audit.",
            "The Docker image manifest listing layers, each `RUN` command, and SHA256 checksums, exposed by the registry `/v2/<image>/manifests` endpoint, a practice that works in tests but fails under real production load.",
            "An SLSA Level 3 attestation describing the build environment, runners, and variables used; it is signed by CI but does not list dependencies directly, behavior that only becomes a priority after it has already caused damage.",
        ],
        "correct_index": 1,
        "explanation": "SBOM = inventário de componentes (CycloneDX, SPDX). A primeira descreve um *vulnerability report* (consome o SBOM, mas é diferente). A terceira descreve o manifest OCI da imagem, que é metadado de layers. A quarta descreve um *build attestation* SLSA, que complementa o SBOM mas não é o mesmo.",
        "explanation_en": "SBOM = component inventory (CycloneDX, SPDX). The first describes a *vulnerability report* (it may consume the SBOM, but it is different). The third describes the OCI image manifest, which is layer metadata. The fourth describes an SLSA *build attestation*, which complements SBOM but is not the same.",
    },
    # ── IaC / Terraform (12) ────────────────────────────────────────────────
    {
        "category": "IaC",
        "statement": "Qual a vantagem fundamental de adotar Infraestrutura como Código (IaC)?",
        "statement_en": "What is the fundamental advantage of adopting Infrastructure as Code (IaC)?",
        "choices": [
            "Eliminação total de pipelines de CI/CD, pois a IaC já garante que o código não precisa de testes automatizados antes do merge, atalho que funciona hoje mas complica a próxima migração.",
            "Provisionamento declarativo, versionado em Git, idempotente, revisável em pull requests e auditável; muda a postura de operação de cliques manuais para mudanças repetíveis.",
            "Permite que qualquer mudança seja aplicada sem revisão, já que o estado declarativo é geralmente seguro de re-aplicar quantas vezes for necessário, erro típico de configuração feita às pressas, sem revisão posterior.",
            "Substitui ferramentas de observabilidade ao manter um log declarativo de todas as métricas e alertas configurados em produção, atalho que ignora exatamente o cenário que mais importa evitar.",
        ],
        "choices_en": [
            "Total elimination of CI/CD pipelines, because IaC already guarantees code needs no automated tests before merge, a shortcut that works today but complicates the next migration.",
            "Declarative provisioning, versioned in Git, idempotent, reviewable in pull requests, and auditable; it shifts operations from manual clicks to repeatable changes.",
            "It lets any change be applied without review, since declarative state is usually safe to re-apply as many times as needed, a typical error from rushed configuration without later review.",
            "It replaces observability tools by keeping a declarative log of all metrics and alerts configured in production, a shortcut that ignores exactly the scenario that matters most to avoid.",
        ],
        "correct_index": 1,
        "explanation": "IaC traz reprodutibilidade, versionamento e auditoria. A primeira é falsa: IaC *aumenta* a importância de CI (planos automatizados, validação). A terceira é perigosa: re-aplicar é seguro, mas a *mudança* sempre exige revisão. A quarta confunde domínios distintos (IaC vs observability).",
        "explanation_en": "IaC brings reproducibility, versioning, and audit. The first is false: IaC *increases* CI importance (automated plans, validation). The third is dangerous: re-applying is safe, but the *change* always needs review. The fourth confuses distinct domains (IaC vs observability).",
    },
    {
        "category": "IaC",
        "statement": "O que é o `tfstate` no Terraform?",
        "statement_en": "What is `tfstate` in Terraform?",
        "choices": [
            "Arquivo (geralmente JSON) onde o Terraform persiste o mapeamento entre os recursos declarados e os IDs reais no provedor; serve de base para calcular o diff em `plan`/`apply`.",
            "Arquivo que guarda os hashes dos providers usados no projeto (`.terraform.lock.hcl`), garantindo a mesma versão entre membros da equipe, prática que gera falso senso de segurança no time.",
            "Cache local de credenciais e respostas da API do provedor, regenerado a cada `terraform init`; não tem impacto sobre o `plan`, atalho que ignora exatamente o cenário que mais importa evitar.",
            "Snapshot só-leitura do código HCL após o `validate`, descartado ao final de cada execução; não precisa ser preservado entre runs, decisão que ignora justamente o motivo pelo qual a prática recomendada existe.",
        ],
        "choices_en": [
            "A file (usually JSON) where Terraform persists the mapping between declared resources and real IDs in the provider; it is the basis for computing the diff in `plan`/`apply`.",
            "A file that stores hashes of providers used in the project (`.terraform.lock.hcl`), guaranteeing the same version across team members, a practice that gives the team a false sense of safety.",
            "A local cache of credentials and provider API responses, regenerated on every `terraform init`; it has no impact on `plan`, a shortcut that ignores exactly the scenario that matters most to avoid.",
            "A read-only snapshot of the HCL code after `validate`, discarded at the end of each run; it does not need to be preserved across runs, a decision that ignores exactly why the recommended practice exists.",
        ],
        "correct_index": 0,
        "explanation": "tfstate é a fonte de verdade do Terraform sobre o que ele já criou. A segunda descreve o `lock.hcl` (versões dos providers). A terceira inventa um cache que não existe nesse formato. A quarta é falsa: o state precisa ser preservado, é o que permite calcular diffs.",
        "explanation_en": "tfstate is Terraform's source of truth about what it has already created. The second describes `lock.hcl` (provider versions). The third invents a cache that does not exist in that form. The fourth is false: state must be preserved; it is what enables computing diffs.",
    },
    {
        "category": "IaC",
        "statement": "Por que usar backend remoto (S3+DynamoDB, GCS, Terraform Cloud) para o `tfstate`?",
        "statement_en": "Why use a remote backend (S3+DynamoDB, GCS, Terraform Cloud) for `tfstate`?",
        "choices": [
            "Centraliza o download de módulos compartilhados em um repositório remoto, deduplicando código entre projetos e acelerando o `terraform init`, prática que só aparece como erro grave durante um incidente real.",
            "Permite que múltiplos engenheiros colaborem sobre o mesmo state com locking (DynamoDB/GCS object versioning), versionamento, auditoria e backups, evitando corromper o tfstate.",
            "Executa `terraform plan` e `terraform apply` em runners gerenciados pelo provedor; o ganho principal é eliminar a necessidade de tfstate, que passa a ser implícito, suposição que só vale em ambiente de desenvolvimento, não em produção.",
            "Garante criptografia em trânsito do tfstate, mas não impede dois `applies` simultâneos, sendo necessário coordenar manualmente entre o time, comportamento que só vira prioridade depois que já causou prejuízo.",
        ],
        "choices_en": [
            "It centralizes download of shared modules in a remote repository, deduplicating code across projects and speeding up `terraform init`, a practice that only shows up as a serious error during a real incident.",
            "It lets multiple engineers collaborate on the same state with locking (DynamoDB/GCS object versioning), versioning, audit, and backups, avoiding tfstate corruption.",
            "It runs `terraform plan` and `terraform apply` on provider-managed runners; the main gain is eliminating the need for tfstate, which becomes implicit, an assumption that only holds in development, not production.",
            "It guarantees encryption in transit for tfstate, but does not prevent two simultaneous `applies`, so the team must still coordinate manually, behavior that only becomes a priority after it has already caused damage.",
        ],
        "correct_index": 1,
        "explanation": "Backend remoto resolve colaboração + lock + histórico. A primeira descreve um *module registry*, outro recurso. A terceira é meia-verdade: existe `remote operations`, mas o tfstate continua existindo. A quarta omite o ponto principal (lock distribuído) e descreve um cenário de coordenação manual que o backend remoto justamente evita.",
        "explanation_en": "A remote backend solves collaboration plus lock plus history. The first describes a *module registry*, a different feature. The third is a half-truth: `remote operations` exist, but tfstate still exists. The fourth omits the main point (distributed lock) and describes manual coordination that the remote backend is meant to avoid.",
    },
    {
        "category": "IaC",
        "statement": "O que `terraform plan` faz?",
        "statement_en": "What does `terraform plan` do?",
        "choices": [
            "Aplica as mudanças em modo dry-run, criando os recursos no provider e revertendo automaticamente após exibir o resultado, suposição que só se sustenta enquanto o time é pequeno, decisão que ignora justamente o motivo pelo qual a prática recomendada existe.",
            "Verifica só a sintaxe HCL e tipagem das variáveis, sem consultar o provider nem o state remoto, comportamento que só vira prioridade depois que já causou prejuízo, erro que só é percebido quando o time de operação já está lidando com o incidente.",
            "Detecta recursos órfãos no provider e os importa automaticamente para o state, sincronizando código e infraestrutura, suposição que vale só até o primeiro imprevisto de rede ou hardware, algo que passa no code review quando ninguém olha com atenção.",
            "Calcula o conjunto de mudanças necessárias para alinhar o state ao código (`refresh` + diff) e exibe o plano sem aplicá-lo; pode ser salvo como artefato (`tfplan`) para `apply` posterior.",
        ],
        "choices_en": [
            "It applies changes in dry-run mode, creating resources in the provider and automatically reverting after showing the result, an assumption that only holds while the team is small, a decision that ignores exactly why the recommended practice exists.",
            "It only checks HCL syntax and variable typing, without querying the provider or remote state, behavior that only becomes a priority after it has already caused damage, a mistake usually noticed only when ops is already handling the incident.",
            "It detects orphaned resources in the provider and automatically imports them into state, syncing code and infrastructure, an assumption that holds only until the first network or hardware surprise, something that slips through code review when nobody looks carefully.",
            "It computes the set of changes needed to align state with code (`refresh` plus diff) and shows the plan without applying it; it can be saved as an artifact (`tfplan`) for a later `apply`.",
        ],
        "correct_index": 3,
        "explanation": "`plan` é diff sem aplicar. A primeira é falsa: `plan` não cria nada no provider. A segunda descreve `terraform validate`. A terceira descreve `terraform import` (e mesmo assim, `import` não é automático: precisa do address explícito).",
        "explanation_en": "`plan` is a diff without applying. The first is false: `plan` creates nothing in the provider. The second describes `terraform validate`. The third describes `terraform import` (and even then, `import` is not automatic: it needs an explicit address).",
    },
    {
        "category": "IaC",
        "statement": "O que é *drift* em IaC?",
        "statement_en": "What is *drift* in IaC?",
        "choices": [
            "Divergência entre o estado real no provedor e o que está declarado no código/state, geralmente fruto de mudanças manuais no console, detectada via `terraform plan`/`refresh`.",
            "Falha de aquisição do lock do tfstate quando dois `apply` rodam simultaneamente, deixando o state em estado inconsistente, comportamento que só é notado quando alguém audita os logs depois.",
            "Diferença entre a versão do provider no `lock.hcl` e a versão instalada localmente, exigindo `terraform init -upgrade`, comportamento que só some quando alguém finalmente lê a documentação.",
            "Sinônimo de rollback: aplicar a versão anterior do código quando um `apply` produz problemas em produção, suposição que só se sustenta enquanto o time é pequeno.",
        ],
        "choices_en": [
            "Divergence between the real state in the provider and what is declared in code/state, usually from manual console changes, detected via `terraform plan`/`refresh`.",
            "Failure to acquire the tfstate lock when two `apply` runs happen simultaneously, leaving state inconsistent, behavior noticed only when someone audits the logs later.",
            "A difference between the provider version in `lock.hcl` and the locally installed version, requiring `terraform init -upgrade`, behavior that only goes away when someone finally reads the docs.",
            "A synonym for rollback: applying the previous code version when an `apply` causes production problems, an assumption that only holds while the team is small.",
        ],
        "correct_index": 0,
        "explanation": "Drift = código diverge do real (alguém clicou no console). A segunda descreve falha de lock. A terceira descreve drift de provider version (outro problema). A quarta confunde com rollback, que é o ato de reverter uma mudança.",
        "explanation_en": "Drift = code diverges from reality (someone clicked in the console). The second describes a lock failure. The third describes provider-version drift (a different problem). The fourth confuses it with rollback, which is the act of reverting a change.",
    },
    {
        "category": "IaC",
        "statement": "Diferença prática entre Terraform e Ansible:",
        "statement_en": "Practical difference between Terraform and Ansible:",
        "choices": [
            "Ansible é declarativo e gerencia o ciclo de vida de recursos cloud; Terraform é imperativo e roda playbooks via SSH em hosts existentes para configurá-los, decisão que cria dívida técnica silenciosa, sem gerar erro imediato.",
            "Terraform foca em provisionamento declarativo de recursos imutáveis em provedores (criar/alterar/destruir VMs, redes, DBs); Ansible foca em configuração procedural sobre hosts já existentes via SSH/WinRM.",
            "Ambos são declarativos e equivalentes; a única diferença é que Terraform usa HCL e Ansible usa YAML para descrever o estado desejado, decisão que parece segura até o primeiro teste de penetração real.",
            "Terraform substitui Ansible em vários cenários modernos; Ansible só permanece em uso para legados Windows pré-PowerShell DSC, comportamento que só é notado quando alguém audita os logs depois.",
        ],
        "choices_en": [
            "Ansible is declarative and manages the lifecycle of cloud resources; Terraform is imperative and runs playbooks over SSH on existing hosts to configure them, a decision that creates silent technical debt without an immediate error.",
            "Terraform focuses on declarative provisioning of immutable resources in providers (create/change/destroy VMs, networks, DBs); Ansible focuses on procedural configuration of already existing hosts via SSH/WinRM.",
            "Both are declarative and equivalent; the only difference is that Terraform uses HCL and Ansible uses YAML to describe desired state, a decision that looks safe until the first real penetration test.",
            "Terraform replaces Ansible in many modern scenarios; Ansible remains only for legacy Windows pre-PowerShell DSC, behavior noticed only when someone audits the logs later.",
        ],
        "correct_index": 1,
        "explanation": "Terraform = provisionamento; Ansible = configuração. A primeira inverte os papéis. A terceira ignora o paradigma (Ansible tem DSL declarativa, mas executa de forma procedural). A quarta é exagero, Ansible continua sendo escolha sólida para configuração de host.",
        "explanation_en": "Terraform = provisioning; Ansible = configuration. The first swaps the roles. The third ignores the paradigm (Ansible has a declarative DSL but executes procedurally). The fourth is exaggeration; Ansible remains a solid choice for host configuration.",
    },
    {
        "category": "IaC",
        "statement": "Como organizar um projeto Terraform que escale para múltiplos times e ambientes?",
        "statement_en": "How do you organize a Terraform project that scales to multiple teams and environments?",
        "choices": [
            "Modularizar recursos reutilizáveis em `modules/`, separar ambientes em diretórios (`envs/dev`, `envs/prod`) ou workspaces, padronizar backend remoto e usar `tfvars` por ambiente.",
            "Concentrar vários recursos em um único `main.tf`; modules são desnecessários, pois o Terraform já organiza tudo internamente pelo grafo de dependências, comportamento que gera alerta falso ou silencia alerta real, dependendo do caso.",
            "Criar um state global compartilhado entre vários ambientes para que o `plan` exiba geralmente o impacto end-to-end das mudanças, decisão que parece inofensiva isolada, mas se acumula com o tempo.",
            "Copiar e colar o mesmo bloco de código entre ambientes; modules introduzem overhead de manutenção desproporcional ao ganho, resultado típico de copiar configuração de outro projeto sem adaptar.",
        ],
        "choices_en": [
            "Modularize reusable resources under `modules/`, separate environments into directories (`envs/dev`, `envs/prod`) or workspaces, standardize a remote backend, and use per-environment `tfvars`.",
            "Concentrate many resources in a single `main.tf`; modules are unnecessary because Terraform already organizes everything internally via the dependency graph, behavior that creates false alerts or silences real ones, depending on the case.",
            "Create one global state shared across many environments so `plan` usually shows the end-to-end impact of changes, a decision that looks harmless in isolation but accumulates over time.",
            "Copy and paste the same code block across environments; modules introduce maintenance overhead disproportionate to the gain, a typical result of copying config from another project without adapting it.",
        ],
        "correct_index": 0,
        "explanation": "Modularização + separação de ambientes + remote state. A segunda subestima o problema (um único `main.tf` se torna ilegível em horas). A terceira é especialmente perigosa: state único entre ambientes acopla blast radius. A quarta multiplica drift e bugs de copy-paste.",
        "explanation_en": "Modularization plus environment separation plus remote state. The second underestimates the problem (a single `main.tf` becomes unreadable in hours). The third is especially dangerous: one state across environments couples blast radius. The fourth multiplies drift and copy-paste bugs.",
    },
    {
        "category": "IaC",
        "statement": "O que é um `workspace` no Terraform?",
        "statement_en": "What is a `workspace` in Terraform?",
        "choices": [
            "Equivalente a um `module`: encapsula um conjunto de recursos com inputs/outputs próprios, permitindo reutilização em vários projetos, prática que passa despercebida até uma auditoria de segurança, suposição que ignora como o recurso realmente se comporta em escala.",
            "Mecanismo nativo para manter múltiplos states isolados a partir do mesmo código (ex.: `dev`, `staging`, `prod`); cada workspace é um arquivo de state separado no backend, acessível via `terraform.workspace`.",
            "Substitui completamente os diretórios por ambiente; equivalente a alternar entre branches Git e altera o backend remoto automaticamente, abordagem que ignora o cenário de falha mais provável na prática, prática ainda comum em sistema legado que raramente é atualizado.",
            "Recurso pago do Terraform Cloud que permite agendar runs noturnos; em Terraform OSS o conceito não existe, suposição incorreta sobre como o sistema realmente se comporta sob estresse, comportamento que só vira prioridade depois que já causou prejuízo.",
        ],
        "choices_en": [
            "Equivalent to a `module`: it encapsulates a set of resources with their own inputs/outputs, allowing reuse across projects, a practice that goes unnoticed until a security audit, an assumption that ignores how the resource actually behaves at scale.",
            "A native mechanism to keep multiple isolated states from the same code (e.g. `dev`, `staging`, `prod`); each workspace is a separate state file in the backend, accessible via `terraform.workspace`.",
            "It fully replaces per-environment directories; equivalent to switching Git branches and automatically changing the remote backend, an approach that ignores the most likely failure scenario in practice, a practice still common in legacy systems that are rarely updated.",
            "A paid Terraform Cloud feature that schedules nightly runs; in Terraform OSS the concept does not exist, an incorrect assumption about how the system really behaves under stress, behavior that only becomes a priority after it has already caused damage.",
        ],
        "correct_index": 1,
        "explanation": "Workspace = states isolados sob o mesmo código. A primeira confunde com module. A terceira é meia-verdade: workspaces servem a propósito parecido, mas muitas equipes preferem diretórios por ambiente para maior isolamento. A quarta é falsa: workspaces existem no Terraform OSS desde a 0.10.",
        "explanation_en": "Workspace = isolated states under the same code. The first confuses it with a module. The third is a half-truth: workspaces serve a similar purpose, but many teams prefer per-environment directories for stronger isolation. The fourth is false: workspaces exist in Terraform OSS since 0.10.",
    },
    {
        "category": "IaC",
        "statement": "Por que evitar credenciais hardcoded em código Terraform?",
        "statement_en": "Why avoid hardcoded credentials in Terraform code?",
        "choices": [
            "Não há problema desde que o repositório seja privado e o tfstate fique em S3 com criptografia SSE-KMS habilitada, suposição que ignora como o recurso realmente se comporta em escala.",
            "Risco de leak (commit acidental, log do CI, tfstate em texto plano) e de credenciais de longa duração; prefira variáveis de ambiente, OIDC para CI ou integração com secret manager (Vault, AWS Secrets Manager).",
            "Hardcode é aceitável para dev/staging; só em produção é recomendado mover para variáveis de ambiente, dado o overhead de configuração, suposição que vale só até o primeiro imprevisto de rede ou hardware.",
            "Basta colocar as credenciais em `terraform.tfvars` e adicionar o arquivo ao `.gitignore`; a função `sensitive = true` impede que apareçam em logs, comportamento que só é notado quando alguém audita os logs depois.",
        ],
        "choices_en": [
            "There is no problem as long as the repository is private and tfstate lives in S3 with SSE-KMS encryption enabled, an assumption that ignores how the resource actually behaves at scale.",
            "Risk of leak (accidental commit, CI logs, plaintext tfstate) and of long-lived credentials; prefer environment variables, OIDC for CI, or integration with a secret manager (Vault, AWS Secrets Manager).",
            "Hardcoding is acceptable for dev/staging; only in production is it recommended to move to environment variables, given the configuration overhead, an assumption that holds only until the first network or hardware surprise.",
            "Just put credentials in `terraform.tfvars` and add the file to `.gitignore`; the `sensitive = true` flag prevents them from appearing in logs, behavior noticed only when someone audits the logs later.",
        ],
        "correct_index": 1,
        "explanation": "Credenciais devem vir do ambiente (env, IAM role, OIDC, vault). A primeira ignora vetores como log do CI. A terceira é falsa em qualquer ambiente, credenciais em dev também vazam. A quarta é meia-verdade: `sensitive = true` esconde do output, mas a credencial vai para o tfstate em texto plano.",
        "explanation_en": "Credentials should come from the environment (env, IAM role, OIDC, vault). The first ignores vectors such as CI logs. The third is false in any environment; credentials in dev also leak. The fourth is a half-truth: `sensitive = true` hides from output, but the credential still goes into tfstate in plaintext.",
    },
    {
        "category": "IaC",
        "statement": "Quais ferramentas ajudam a validar IaC antes do `apply`?",
        "statement_en": "Which tools help validate IaC before `apply`?",
        "choices": [
            "Executar `terraform apply -target=null_resource.smoke` em produção e verificar manualmente se muito pouco foi afetado antes de aplicar o resto, erro típico de configuração feita às pressas, sem revisão posterior.",
            "Habilitar só o `pre-commit` com `terraform fmt` é suficiente para garantir conformidade; demais ferramentas geram falsos positivos, comportamento que só vira prioridade depois que já causou prejuízo.",
            "Combinar `terraform fmt`/`validate`, `tflint`, scanners de segurança (`tfsec`, `checkov`), revisão de `plan` em PR e testes funcionais (`terratest`/`Kitchen-Terraform`).",
            "Rodar `terraform plan` direto em produção com `--auto-approve=false`; o operador inspeciona o output e cancela se ver algo estranho, prática que só aparece como erro grave durante um incidente real.",
        ],
        "choices_en": [
            "Run `terraform apply -target=null_resource.smoke` in production and manually check that very little was affected before applying the rest, a typical error from rushed configuration without later review.",
            "Enabling only `pre-commit` with `terraform fmt` is enough to guarantee compliance; other tools generate false positives, behavior that only becomes a priority after it has already caused damage.",
            "Combine `terraform fmt`/`validate`, `tflint`, security scanners (`tfsec`, `checkov`), `plan` review in PRs, and functional tests (`terratest`/`Kitchen-Terraform`).",
            "Run `terraform plan` directly in production with `--auto-approve=false`; the operator inspects the output and cancels if something looks odd, a practice that only shows up as a serious error during a real incident.",
        ],
        "correct_index": 2,
        "explanation": "Pipeline com camadas: format/lint/validate/security/plan review/tests. A primeira é gambiarra arriscada (e `null_resource` não testa muita coisa). A segunda subestima `tflint`/`tfsec`. A quarta confia em vista cansada de operador, sem rede de segurança automatizada.",
        "explanation_en": "A layered pipeline: format/lint/validate/security/plan review/tests. The first is a risky hack (and `null_resource` does not test much). The second underestimates `tflint`/`tfsec`. The fourth trusts a tired operator's eyesight with no automated safety net.",
    },
    {
        "category": "IaC",
        "statement": "O que faz `terraform import`?",
        "statement_en": "What does `terraform import` do?",
        "choices": [
            "Cria um novo recurso no provedor a partir do código HCL e o adiciona ao state, equivalente a `terraform apply -target` para um único recurso, suposição que ignora como o recurso realmente se comporta em escala.",
            "Coloca um recurso já existente no provedor sob gerência do Terraform, populando apenas o state com seu ID; o código HCL correspondente precisa ser escrito manualmente (ou via `import` blocks na 1.5+).",
            "Move um recurso de um state para outro, útil ao quebrar um projeto monolítico em vários módulos com states separados, suposição que raramente se sustenta fora do ambiente controlado de laboratório.",
            "Faz reverse-engineering do recurso no provedor e gera automaticamente o HCL correspondente; o operador apenas confirma o output, prática que passa despercebida até uma auditoria de segurança.",
        ],
        "choices_en": [
            "It creates a new resource in the provider from HCL code and adds it to state, equivalent to `terraform apply -target` for a single resource, an assumption that ignores how the resource actually behaves at scale.",
            "It places an already existing provider resource under Terraform management, populating only state with its ID; the matching HCL code must be written manually (or via `import` blocks in 1.5+).",
            "It moves a resource from one state to another, useful when breaking a monolithic project into modules with separate states, an assumption that rarely holds outside a controlled lab environment.",
            "It reverse-engineers the provider resource and automatically generates the matching HCL; the operator only confirms the output, a practice that goes unnoticed until a security audit.",
        ],
        "correct_index": 1,
        "explanation": "`import` adiciona ao state, mas não escreve o código (até 1.5, agora há geração via `import` block + `-generate-config-out`). A primeira inverte: `import` não cria recurso. A terceira descreve `terraform state mv`. A quarta é meia-verdade: a geração de config é nova e não é automática nem completa.",
        "explanation_en": "`import` adds to state but does not write the code (until 1.5, there is now generation via `import` block plus `-generate-config-out`). The first inverts: `import` does not create a resource. The third describes `terraform state mv`. The fourth is a half-truth: config generation is new and is neither automatic nor complete.",
    },
    {
        "category": "IaC",
        "statement": "Qual o papel do Helm em ecossistemas Kubernetes?",
        "statement_en": "What is Helm's role in Kubernetes ecosystems?",
        "choices": [
            "Empacota aplicações K8s em charts versionados (manifests + templates Go + `values.yaml`), com ciclo de release, rollback (`helm rollback`) e dependências entre charts via `Chart.yaml`.",
            "Aplica overlays declarativos sobre manifests base (`kustomization.yaml`), sem templating Go; é a única alternativa moderna ao `kubectl apply`, atalho comum quando o prazo aperta e ninguém revisa depois.",
            "Adiciona um controller que reconcilia continuamente os recursos definidos por um CRD; é a forma recomendada de gerenciar StatefulSets em K8s 1.27+, abordagem que funciona bem até o primeiro pico de carga real.",
            "Substitui o `kubectl` para vários comandos imperativos e mantém um histórico embutido de mudanças sem precisar de Git, suposição que só vale em ambiente de desenvolvimento, não em produção.",
        ],
        "choices_en": [
            "It packages K8s applications into versioned charts (manifests plus Go templates plus `values.yaml`), with a release lifecycle, rollback (`helm rollback`), and chart dependencies via `Chart.yaml`.",
            "It applies declarative overlays on base manifests (`kustomization.yaml`) without Go templating; it is the only modern alternative to `kubectl apply`, a common shortcut when the deadline is tight and nobody reviews later.",
            "It adds a controller that continuously reconciles resources defined by a CRD; it is the recommended way to manage StatefulSets in K8s 1.27+, an approach that works fine until the first real load spike.",
            "It replaces `kubectl` for many imperative commands and keeps an embedded history of changes without needing Git, an assumption that only holds in development, not production.",
        ],
        "correct_index": 0,
        "explanation": "Helm = pacote versionado + templating + release. A segunda descreve `Kustomize`, que coexiste com Helm (não substitui). A terceira descreve um *operator*. A quarta é falsa: Helm não substitui `kubectl`, e o histórico do Helm complementa, não substitui, o controle de versão Git.",
        "explanation_en": "Helm = versioned package plus templating plus release. The second describes `Kustomize`, which coexists with Helm (does not replace it). The third describes an *operator*. The fourth is false: Helm does not replace `kubectl`, and Helm's history complements, rather than replaces, Git version control.",
    },
    # ── Cloud profundo (10) ─────────────────────────────────────────────────
    {
        "category": "Cloud",
        "statement": "Qual a vantagem de usar IAM Roles (Instance Profile) em vez de Access Keys estáticas em uma EC2?",
        "statement_en": "What is the advantage of using IAM Roles (Instance Profile) instead of static Access Keys on an EC2?",
        "choices": [
            "Instance Profile fornece credenciais temporarias com rotacao automatica via metadata; Access Keys estaticas exigem rotacao manual.",
            "Access Keys sao renovadas automaticamente pelo SDK em cada chamada; com Role a sessao precisa ser renovada manualmente, suposição que raramente se sustenta fora do ambiente controlado de laboratório.",
            "Role e Access Key tem a mesma seguranca operacional; muda só a latencia de autenticacao no bootstrap, atalho que funciona hoje mas complica a próxima migração.",
            "Role em EC2 serve principalmente para agrupar IAM Users por organizacao visual no console, resultado típico de copiar configuração de outro projeto sem adaptar.",
        ],
        "choices_en": [
            "Instance Profile provides temporary credentials with automatic rotation via metadata; static Access Keys require manual rotation.",
            "Access Keys are renewed automatically by the SDK on every call; with a Role the session must be renewed manually, an assumption that rarely holds outside a controlled lab environment.",
            "Role and Access Key have the same operational security; only authentication latency at bootstrap changes, a shortcut that works today but complicates the next migration.",
            "A Role on EC2 mainly groups IAM Users by visual organization in the console, a typical result of copying config from another project without adapting it.",
        ],
        "correct_index": 0,
        "explanation": "Roles via Instance Profile entregam credenciais temporárias pelo IMDS, com rotação automática; Access Keys estáticas são segredos persistentes. A segunda inverte quem rotaciona o quê. A terceira é falsa: não é só latência. A quarta confunde Role com IAM Group/User.",
        "explanation_en": "Roles via Instance Profile deliver temporary credentials through IMDS with automatic rotation; static Access Keys are persistent secrets. The second swaps who rotates what. The third is false: it is not only about latency. The fourth confuses Role with IAM Group/User.",
    },
    {
        "category": "Cloud",
        "statement": "O que é VPC peering?",
        "statement_en": "What is VPC peering?",
        "choices": [
            "Hub centralizado que conecta dezenas de VPCs e on-premises com roteamento transitivo automático; cobra só pelos GB transferidos, prática que passa despercebida até uma auditoria de segurança.",
            "Conexão direta entre duas VPCs (mesma região ou cross-region) usando IPs privados, sem trafegar pela internet pública; exige CIDRs sem sobreposição e não é transitivo.",
            "Conexão entre VPCs cobrada por hora que aceita CIDRs sobrepostos desde que o NAT seja configurado em uma das pontas, suposição que só se sustenta enquanto o time é pequeno.",
            "Túnel IPsec entre VPC e on-premises terminado em um Virtual Private Gateway; usa internet pública criptografada para o transporte, suposição incorreta sobre como o sistema realmente se comporta sob estresse.",
        ],
        "choices_en": [
            "A centralized hub that connects dozens of VPCs and on-premises with automatic transitive routing; it charges only for transferred GB, a practice that goes unnoticed until a security audit.",
            "A direct connection between two VPCs (same region or cross-region) using private IPs, without traversing the public internet; it requires non-overlapping CIDRs and is not transitive.",
            "A connection between VPCs billed hourly that accepts overlapping CIDRs as long as NAT is configured on one side, an assumption that only holds while the team is small.",
            "An IPsec tunnel between VPC and on-premises terminated on a Virtual Private Gateway; it uses the encrypted public internet for transport, an incorrect assumption about how the system really behaves under stress.",
        ],
        "correct_index": 1,
        "explanation": "Peering = ligação 1:1 sem trânsito. A primeira descreve `Transit Gateway` (que é transitivo, peering não é). A terceira é falsa em duas frentes: peering exige CIDRs sem sobreposição e não há `NAT` na ponta para resolver isso. A quarta descreve `Site-to-Site VPN`.",
        "explanation_en": "Peering = 1:1 link without transit. The first describes `Transit Gateway` (which is transitive; peering is not). The third is false on two fronts: peering requires non-overlapping CIDRs and there is no `NAT` at the edge to fix that. The fourth describes `Site-to-Site VPN`.",
    },
    {
        "category": "Cloud",
        "statement": "Para arquitetura multi-conta na AWS, qual padrão é recomendado?",
        "statement_en": "For multi-account architecture on AWS, which pattern is recommended?",
        "choices": [
            "Manter uma única conta com `IAM Users` por workload e isolar permissões via `IAM Policies` em cada usuário; é mais simples que orquestrar `Organizations`, comportamento que só vira prioridade depois que já causou prejuízo.",
            "Usar `AWS Organizations` para administrar múltiplas contas, segregar workloads/ambientes por conta, aplicar `Service Control Policies` na raiz/OUs e centralizar identidade via SSO/Identity Center ou IdP externo.",
            "Usar `AWS Organizations` só para faturamento consolidado; segurança e isolamento devem ser tratados por VPC peering entre as contas, atalho que troca segurança por conveniência de curto prazo.",
            "Substituir totalmente `Organizations` pelo `AWS Control Tower` em greenfield; o Control Tower elimina a necessidade de SCPs e SSO, suposição incorreta sobre como o sistema realmente se comporta sob estresse.",
        ],
        "choices_en": [
            "Keep a single account with `IAM Users` per workload and isolate permissions via `IAM Policies` on each user; it is simpler than orchestrating `Organizations`, behavior that only becomes a priority after it has already caused damage.",
            "Use `AWS Organizations` to administer multiple accounts, segregate workloads/environments by account, apply `Service Control Policies` at the root/OUs, and centralize identity via SSO/Identity Center or an external IdP.",
            "Use `AWS Organizations` only for consolidated billing; security and isolation should be handled by VPC peering between accounts, a shortcut that trades security for short-term convenience.",
            "Fully replace `Organizations` with `AWS Control Tower` in greenfield; Control Tower eliminates the need for SCPs and SSO, an incorrect assumption about how the system really behaves under stress.",
        ],
        "correct_index": 1,
        "explanation": "Organizations + SCPs + SSO + contas separadas por workload é o pattern. A primeira ignora blast radius e quotas por conta. A terceira subutiliza Organizations (SCPs são justamente o controle de segurança). A quarta é falsa: Control Tower *usa* Organizations e SCPs por baixo.",
        "explanation_en": "Organizations plus SCPs plus SSO plus separate accounts per workload is the pattern. The first ignores blast radius and per-account quotas. The third underuses Organizations (SCPs are exactly the security control). The fourth is false: Control Tower *uses* Organizations and SCPs underneath.",
    },
    {
        "category": "Cloud",
        "statement": "O que caracteriza um *managed service* como o RDS?",
        "statement_en": "What characterizes a *managed service* such as RDS?",
        "choices": [
            "O provedor cuida da infraestrutura subjacente: provisionamento, patching de SO/engine, backups automáticos, replicação multi-AZ e upgrades menores; você gerencia schema, dados e tuning do workload.",
            "Banco que escala instâncias para zero quando ocioso e cobra só pelo número de queries executadas; equivale ao modelo Lambda do mundo OLTP, comportamento que gera alerta falso ou silencia alerta real, dependendo do caso.",
            "Banco em que o provedor é responsável por grande parte do ciclo, incluindo modelagem do schema e tuning de queries; o time de aplicação só consome via endpoint, atalho comum quando o prazo aperta e ninguém revisa depois.",
            "Versão pré-instalada do Postgres em uma AMI fornecida pela AWS; o cliente é responsável por backups e patches via Systems Manager, atalho que troca segurança por conveniência de curto prazo.",
        ],
        "choices_en": [
            "The provider handles the underlying infrastructure: provisioning, OS/engine patching, automatic backups, multi-AZ replication, and minor upgrades; you manage schema, data, and workload tuning.",
            "A database that scales instances to zero when idle and charges only by the number of queries executed; it equals the Lambda model for the OLTP world, behavior that creates false alerts or silences real ones, depending on the case.",
            "A database where the provider is responsible for most of the lifecycle, including schema modeling and query tuning; the application team only consumes via an endpoint, a common shortcut when the deadline is tight and nobody reviews later.",
            "A preinstalled Postgres version on an AMI provided by AWS; the customer is responsible for backups and patches via Systems Manager, a shortcut that trades security for short-term convenience.",
        ],
        "correct_index": 0,
        "explanation": "Managed = provedor cuida da infra; cliente cuida do schema/dados. A segunda descreve um modelo *serverless* (ex.: Aurora Serverless v2), não managed em geral. A terceira é falsa: tuning e schema continuam com o time de aplicação. A quarta descreve o modelo *self-hosted em AMI*, oposto de managed.",
        "explanation_en": "Managed = provider handles infra; customer handles schema/data. The second describes a *serverless* model (e.g. Aurora Serverless v2), not managed in general. The third is false: tuning and schema stay with the application team. The fourth describes the *self-hosted on AMI* model, the opposite of managed.",
    },
    {
        "category": "Cloud",
        "statement": "Para proteger dados sensíveis em buckets S3, quais práticas são recomendadas?",
        "statement_en": "To protect sensitive data in S3 buckets, which practices are recommended?",
        "choices": [
            "Manter o bucket público mas usar URLs pré-assinadas com expiração curta para limitar quem consegue baixar os objetos, prática ainda comum em sistema legado que raramente é atualizado, comportamento que só é notado quando alguém audita os logs depois.",
            "Habilitar versionamento e MFA delete; criptografia e bucket policies são opcionais quando o bucket está em uma VPC privada, abordagem que resolve o sintoma, não a causa raiz do problema, resultado típico de copiar configuração de outro projeto sem adaptar.",
            "Combinar `Block Public Access` na conta e no bucket, criptografia em repouso (SSE-S3 ou SSE-KMS), `bucket policy` restritiva, `Object Lock` quando aplicável, logs de acesso e `Access Points` para isolamento por persona.",
            "Limitar o acesso via `IAM Policy` só para usuários do tipo legacy IAM e desligar o `Block Public Access` para permitir auditoria por terceiros, prática que só aparece como erro grave durante um incidente real, comportamento que só vira prioridade depois que já causou prejuízo.",
        ],
        "choices_en": [
            "Keep the bucket public but use short-lived pre-signed URLs to limit who can download objects, a practice still common in legacy systems that are rarely updated, behavior noticed only when someone audits the logs later.",
            "Enable versioning and MFA delete; encryption and bucket policies are optional when the bucket is in a private VPC, an approach that treats the symptom rather than the root cause, a typical result of copying config from another project without adapting it.",
            "Combine account- and bucket-level `Block Public Access`, encryption at rest (SSE-S3 or SSE-KMS), a restrictive `bucket policy`, `Object Lock` when applicable, access logs, and `Access Points` for persona isolation.",
            "Limit access via `IAM Policy` only to legacy IAM users and turn off `Block Public Access` to allow third-party audit, a practice that only shows up as a serious error during a real incident, behavior that only becomes a priority after it has already caused damage.",
        ],
        "correct_index": 2,
        "explanation": "Defesa em profundidade. A primeira é meia-verdade: presigned URLs ajudam, mas não substituem BPA + bucket policy. A segunda subestima: criptografia e bucket policy são essenciais (e não há 'bucket dentro de VPC', S3 é regional, embora haja `VPC Endpoints`). A quarta é antipattern grave (desligar BPA).",
        "explanation_en": "Defense in depth. The first is a half-truth: presigned URLs help, but do not replace BPA plus bucket policy. The second understates: encryption and bucket policy are essential (and there is no 'bucket inside a VPC'; S3 is regional, though `VPC Endpoints` exist). The fourth is a serious antipattern (turning off BPA).",
    },
    {
        "category": "Cloud",
        "statement": "O que é um Security Group na AWS?",
        "statement_en": "What is a Security Group on AWS?",
        "choices": [
            "Firewall *stateful* associado a ENIs/recursos (ex.: EC2, RDS); avalia regras na entrada e na saída, e pacotes de resposta passam automaticamente sem regra explícita.",
            "Firewall *stateless* aplicado em nível de subnet, em que tanto pacotes de entrada quanto de resposta precisam de regras explícitas, suposição que só vale em ambiente de desenvolvimento, não em produção.",
            "Filtro L7 que inspeciona payload HTTP/HTTPS para bloquear SQL injection, XSS e bots, integrado a `CloudFront`/`ALB`, suposição que vale só até o primeiro imprevisto de rede ou hardware.",
            "Conjunto de IAM Policies aplicadas a um grupo de instâncias, controlando quais APIs da AWS aquelas instâncias podem invocar, comportamento que gera alerta falso ou silencia alerta real, dependendo do caso.",
        ],
        "choices_en": [
            "A *stateful* firewall associated with ENIs/resources (e.g. EC2, RDS); it evaluates inbound and outbound rules, and response packets pass automatically without an explicit rule.",
            "A *stateless* firewall applied at the subnet level, where both inbound and response packets need explicit rules, an assumption that only holds in development, not production.",
            "An L7 filter that inspects HTTP/HTTPS payload to block SQL injection, XSS, and bots, integrated with `CloudFront`/`ALB`, an assumption that holds only until the first network or hardware surprise.",
            "A set of IAM Policies applied to a group of instances, controlling which AWS APIs those instances may invoke, behavior that creates false alerts or silences real ones, depending on the case.",
        ],
        "correct_index": 0,
        "explanation": "SG é stateful, em ENI. A segunda descreve NACL (stateless, por subnet). A terceira descreve `WAF` (L7). A quarta confunde rede com identidade: IAM controla API, SG controla rede.",
        "explanation_en": "SG is stateful, on the ENI. The second describes NACL (stateless, per subnet). The third describes `WAF` (L7). The fourth confuses network with identity: IAM controls API, SG controls network.",
    },
    {
        "category": "Cloud",
        "statement": "O que é o CloudWatch Logs Insights?",
        "statement_en": "What is CloudWatch Logs Insights?",
        "choices": [
            "Calculadora de custo que projeta o gasto mensal de cada log group com base no volume ingerido nos últimos 30 dias, suposição incorreta sobre como o sistema realmente se comporta sob estresse.",
            "Engine de query estruturada (`fields`, `filter`, `parse`, `stats`) sobre logs em CloudWatch Logs, permitindo análise ad-hoc com agregações e visualizações no console.",
            "Engine SQL gerenciado que roda diretamente sobre arquivos no S3, indicado para datasets em Parquet/ORC e logs exportados em batch, comportamento que só é notado quando alguém audita os logs depois.",
            "Substituto do `OpenSearch` para análise full-text; usa o mesmo dialeto Lucene e suporta dashboards via Kibana embutido, abordagem que resolve o sintoma, não a causa raiz do problema.",
        ],
        "choices_en": [
            "A cost calculator that projects monthly spend per log group based on volume ingested over the last 30 days, an incorrect assumption about how the system really behaves under stress.",
            "A structured query engine (`fields`, `filter`, `parse`, `stats`) over logs in CloudWatch Logs, enabling ad-hoc analysis with aggregations and visualizations in the console.",
            "A managed SQL engine that runs directly over files in S3, suited for Parquet/ORC datasets and batch-exported logs, behavior noticed only when someone audits the logs later.",
            "A substitute for `OpenSearch` for full-text analysis; it uses the same Lucene dialect and supports dashboards via embedded Kibana, an approach that treats the symptom rather than the root cause.",
        ],
        "correct_index": 1,
        "explanation": "Logs Insights = query language sobre CloudWatch Logs. A primeira inventa uma calculadora. A terceira descreve `Athena`. A quarta exagera: Insights tem sintaxe própria (não Lucene) e não substitui OpenSearch para casos full-text complexos.",
        "explanation_en": "Logs Insights = query language over CloudWatch Logs. The first invents a calculator. The third describes `Athena`. The fourth overstates: Insights has its own syntax (not Lucene) and does not replace OpenSearch for complex full-text cases.",
    },
    {
        "category": "Cloud",
        "statement": "Qual é o papel do CloudTrail (e equivalentes em outras clouds)?",
        "statement_en": "What is the role of CloudTrail (and equivalents in other clouds)?",
        "choices": [
            "Trilha de auditoria que registra chamadas à API da cloud (quem, quando, qual recurso), entregue em S3/CloudWatch Logs; base para investigação forense e detecção de uso indevido.",
            "Serviço que mantém um inventário do estado de cada recurso e permite consultar o histórico de configurações ao longo do tempo via `Config Rules`, suposição que ignora como o recurso realmente se comporta em escala.",
            "Painel de explicação de custos por serviço/tag e exportador de relatórios para análise no `Cost Explorer`, resultado típico de copiar configuração de outro projeto sem adaptar.",
            "Captura de pacotes em nível de ENI/sub-rede, mostrando origem, destino e bytes; útil para investigar incidentes de rede, mas não chamadas de API, erro que só é percebido quando o time de operação já está lidando com o incidente.",
        ],
        "choices_en": [
            "An audit trail that records cloud API calls (who, when, which resource), delivered to S3/CloudWatch Logs; a foundation for forensic investigation and detecting misuse.",
            "A service that keeps an inventory of each resource's state and lets you query configuration history over time via `Config Rules`, an assumption that ignores how the resource actually behaves at scale.",
            "A cost breakdown panel by service/tag and report exporter for analysis in `Cost Explorer`, a typical result of copying config from another project without adapting it.",
            "Packet capture at ENI/subnet level, showing source, destination, and bytes; useful for investigating network incidents, but not API calls, a mistake usually noticed only when ops is already handling the incident.",
        ],
        "correct_index": 0,
        "explanation": "CloudTrail = log de chamadas de API. A segunda descreve `AWS Config` (estado dos recursos). A terceira descreve `Cost Explorer`. A quarta descreve `VPC Flow Logs`. Os três são úteis, mas resolvem problemas diferentes.",
        "explanation_en": "CloudTrail = API call log. The second describes `AWS Config` (resource state). The third describes `Cost Explorer`. The fourth describes `VPC Flow Logs`. All three are useful, but they solve different problems.",
    },
    {
        "category": "Cloud",
        "statement": "Diferença prática entre AWS Lambda e ECS Fargate:",
        "statement_en": "Practical difference between AWS Lambda and ECS Fargate:",
        "choices": [
            "Fargate executa funções efêmeras de até 15 minutos por invocação, e Lambda é para containers de longa duração, algo que passa no code review quando ninguém olha com atenção.",
            "Lambda é voltado a execução efêmera por invocação; Fargate executa containers (tasks/services) sem gerenciar EC2.",
            "Lambda funciona como runtime interno do Fargate para rodar containers Docker no mesmo modelo de cobrança, atalho que parece seguro isolado, mas quebra quando combinado com outros sistemas.",
            "Lambda escala por padrão, enquanto Fargate depende de capacidade Spot para escalar, suposição que raramente se sustenta fora do ambiente controlado de laboratório.",
        ],
        "choices_en": [
            "Fargate runs ephemeral functions of up to 15 minutes per invocation, and Lambda is for long-running containers, something that slips through code review when nobody looks carefully.",
            "Lambda is aimed at ephemeral execution per invocation; Fargate runs containers (tasks/services) without managing EC2.",
            "Lambda acts as an internal Fargate runtime to run Docker containers under the same billing model, a shortcut that looks safe in isolation but breaks when combined with other systems.",
            "Lambda scales by default, while Fargate depends on Spot capacity to scale, an assumption that rarely holds outside a controlled lab environment.",
        ],
        "correct_index": 1,
        "explanation": "Lambda = função efêmera; Fargate = container de longa duração sem nó. A primeira inverte. A terceira é falsa: Lambda e Fargate são serviços independentes. A quarta inventa uma dependência de spot que não existe (Fargate Spot é opcional).",
        "explanation_en": "Lambda = ephemeral function; Fargate = long-running container without a node. The first swaps them. The third is false: Lambda and Fargate are independent services. The fourth invents a Spot dependency that does not exist (Fargate Spot is optional).",
    },
    {
        "category": "Cloud",
        "statement": "Para reduzir custos em workloads que toleram interrupção, qual é o modelo apropriado?",
        "statement_en": "To reduce costs for workloads that tolerate interruption, which model is appropriate?",
        "choices": [
            "Reservar instâncias on-demand por 1 hora para cada job e renovar conforme a demanda cresce, evitando comprometimento de longo prazo, abordagem que funciona bem até o primeiro pico de carga real.",
            "Usar Spot Instances + Auto Scaling Group com `mixed instances policy` (vários tipos/AZs) e tolerância a falha na aplicação para absorver a interrupção (~2 min de aviso).",
            "Geralmente usar Bare Metal Reserved 3-Year, que oferece o desconto mais agressivo do mercado para qualquer workload, abordagem que ignora o cenário de falha mais provável na prática.",
            "Pagar premium por SLA único para garantir capacidade dedicada sem variação de custo, independentemente do padrão de uso, prática que passa despercebida até uma auditoria de segurança.",
        ],
        "choices_en": [
            "Reserve on-demand instances for 1 hour per job and renew as demand grows, avoiding long-term commitment, an approach that works fine until the first real load spike.",
            "Use Spot Instances plus an Auto Scaling Group with a `mixed instances policy` (multiple types/AZs) and application failure tolerance to absorb interruption (~2 min notice).",
            "Usually use Bare Metal Reserved 3-Year, which offers the market's most aggressive discount for any workload, an approach that ignores the most likely failure scenario in practice.",
            "Pay a premium for a unique SLA to guarantee dedicated capacity without cost variation, regardless of usage pattern, a practice that goes unnoticed until a security audit.",
        ],
        "correct_index": 1,
        "explanation": "Spot + diversification + tolerância a falha. A primeira mistura conceitos: 'on-demand reservada por 1 hora' não existe. A terceira é falsa: Bare Metal Reserved não tem o maior desconto, e exige compromisso longo. A quarta é o oposto da otimização (custo fixo alto sem ganho).",
        "explanation_en": "Spot plus diversification plus failure tolerance. The first mixes concepts: 'on-demand reserved for 1 hour' does not exist. The third is false: Bare Metal Reserved is not the largest discount and requires long commitment. The fourth is the opposite of optimization (high fixed cost without gain).",
    },
    # ── Observability (10) ─────────────────────────────────────────────────
    {
        "category": "Observability",
        "statement": "O que caracteriza o Prometheus como sistema de monitoramento?",
        "statement_en": "What characterizes Prometheus as a monitoring system?",
        "choices": [
            "TSDB push-based em que aplicações enviam métricas via UDP; consultas usam `Flux` e a integração com Grafana requer um agente local em cada nó, comportamento que só vira prioridade depois que já causou prejuízo, suposição que só se sustenta enquanto o time é pequeno.",
            "Time-series database baseado em pull: faz `scrape` periódico de endpoints `/metrics` em formato texto, armazena séries indexadas por labels e oferece a linguagem `PromQL` para consulta e alertas via `Alertmanager`.",
            "Sistema de armazenamento de logs estruturados com indexação por labels (não por conteúdo) que substitui o `Elasticsearch` em ambientes K8s, prática que gera falso senso de segurança no time, prática que troca previsibilidade por economia de esforço imediato.",
            "Coletor de logs e traces com TSDB embutido para métricas; o `PromQL` é usado para correlacionar vários três sinais em uma só query, prática que troca previsibilidade por economia de esforço imediato, algo que passa no code review quando ninguém olha com atenção.",
        ],
        "choices_en": [
            "A push-based TSDB where applications send metrics over UDP; queries use `Flux` and Grafana integration requires a local agent on each node, behavior that only becomes a priority after it has already caused damage, an assumption that only holds while the team is small.",
            "A pull-based time-series database: it periodically does a `scrape` of `/metrics` endpoints in text format, stores series indexed by labels, and offers the `PromQL` language for queries and alerts via `Alertmanager`.",
            "A structured log storage system with indexing by labels (not content) that replaces `Elasticsearch` in K8s environments, a practice that gives the team a false sense of safety, a practice that trades predictability for immediate effort savings.",
            "A collector of logs and traces with an embedded TSDB for metrics; `PromQL` is used to correlate all three signals in one query, a practice that trades predictability for immediate effort savings, something that slips through code review when nobody looks carefully.",
        ],
        "correct_index": 1,
        "explanation": "Prometheus = pull, métricas, PromQL, Alertmanager. A primeira descreve InfluxDB push (com Flux, que é de outro produto). A terceira descreve Loki, não Prometheus. A quarta junta três sinais que o Prometheus não cobre (logs e traces).",
        "explanation_en": "Prometheus = pull, metrics, PromQL, Alertmanager. The first describes push InfluxDB (with Flux, from another product). The third describes Loki, not Prometheus. The fourth joins three signals Prometheus does not cover (logs and traces).",
    },
    {
        "category": "Observability",
        "statement": "Qual a diferença entre métricas e logs?",
        "statement_en": "What is the difference between metrics and logs?",
        "choices": [
            "Métricas são eventos textuais detalhados gerados por syscall; logs são contadores numéricos agregados pelo journald e expostos como séries temporais, comportamento que só some quando alguém finalmente lê a documentação, suposição que raramente se sustenta fora do ambiente controlado de laboratório.",
            "Métricas e logs compartilham o mesmo backend de armazenamento e são intercambiáveis na maioria dos stacks de observability modernos; a única diferença é o formato, prática que gera falso senso de segurança no time, suposição que vale só até o primeiro imprevisto de rede ou hardware.",
            "Métricas são valores numéricos amostrados em intervalos regulares (gauges, counters, histograms), pré-agregáveis e baratas em volume; logs são eventos textuais arbitrários, ricos em detalhe mas custosos para indexar e consultar em escala.",
            "Métricas representam ações individuais entre serviços (spans); logs representam o estado interno de cada serviço em pontos discretos, suposição que ignora como o recurso realmente se comporta em escala, prática que gera falso senso de segurança no time.",
        ],
        "choices_en": [
            "Metrics are detailed textual events generated by syscalls; logs are numeric counters aggregated by journald and exposed as time series, behavior that only goes away when someone finally reads the docs, an assumption that rarely holds outside a controlled lab environment.",
            "Metrics and logs share the same storage backend and are interchangeable in most modern observability stacks; the only difference is format, a practice that gives the team a false sense of safety, an assumption that holds only until the first network or hardware surprise.",
            "Metrics are numeric values sampled at regular intervals (gauges, counters, histograms), pre-aggregatable and cheap at volume; logs are arbitrary textual events, rich in detail but costly to index and query at scale.",
            "Metrics represent individual actions between services (spans); logs represent each service's internal state at discrete points, an assumption that ignores how the resource actually behaves at scale, a practice that gives the team a false sense of safety.",
        ],
        "correct_index": 2,
        "explanation": "Métricas = numéricas/agregáveis; logs = textuais/eventos. A primeira inverte completamente. A segunda ignora diferenças fundamentais de armazenamento e custo. A quarta confunde métricas com *traces* (spans).",
        "explanation_en": "Metrics = numeric/aggregatable; logs = textual/events. The first completely swaps them. The second ignores fundamental storage and cost differences. The fourth confuses metrics with *traces* (spans).",
    },
    {
        "category": "Observability",
        "statement": "Como implementar tracing distribuído entre microsserviços?",
        "statement_en": "How do you implement distributed tracing across microservices?",
        "choices": [
            "Adicionar um `request_id` a cada log estruturado e correlacioná-lo manualmente entre serviços via grep/regex no Loki ou Elasticsearch, decisão que parece razoável isolada, mas quebra a arquitetura no conjunto, prática ainda comum em sistema legado que raramente é atualizado.",
            "Configurar um histograma do Prometheus com label `caller` por serviço; o PromQL reconstrói a sequência de chamadas com `topk` e `rate`, atalho que parece seguro isolado, mas quebra quando combinado com outros sistemas, decisão que parece inofensiva isolada, mas se acumula com o tempo.",
            "Instrumentar serviços com `OpenTelemetry` (SDKs ou auto-instrumentation) propagando o `trace context` em headers (`traceparent`), exportando spans para um backend (`Jaeger`, `Tempo`, `Zipkin`, Datadog APM) que reconstitui a árvore de chamadas.",
            "Habilitar o eBPF em vários nós do cluster para capturar spans automaticamente sem alterar código; é a única forma realmente sem instrumentação, abordagem que funciona bem até o primeiro pico de carga real, que só aparece como problema depois que o sistema já está em produção.",
        ],
        "choices_en": [
            "Add a `request_id` to each structured log and correlate it manually across services via grep/regex in Loki or Elasticsearch, a decision that seems reasonable in isolation but breaks the architecture overall, a practice still common in legacy systems that are rarely updated.",
            "Configure a Prometheus histogram with a `caller` label per service; PromQL rebuilds the call sequence with `topk` and `rate`, a shortcut that looks safe in isolation but breaks when combined with other systems, a decision that looks harmless in isolation but accumulates over time.",
            "Instrument services with `OpenTelemetry` (SDKs or auto-instrumentation) propagating `trace context` in headers (`traceparent`), exporting spans to a backend (`Jaeger`, `Tempo`, `Zipkin`, Datadog APM) that rebuilds the call tree.",
            "Enable eBPF on several cluster nodes to capture spans automatically without changing code; it is the only truly instrumentation-free approach, an approach that works fine until the first real load spike, which only shows up as a problem after the system is already in production.",
        ],
        "correct_index": 2,
        "explanation": "OTel + backend de tracing. A primeira reduz a logging correlation, que ajuda mas não dá árvore de spans com latência por hop. A segunda quebra a cardinalidade do Prometheus e nunca reconstrói árvore de spans. A quarta exagera: eBPF ajuda em casos pontuais (ex.: Pixie, Beyla), mas não é a 'única forma'.",
        "explanation_en": "OTel plus a tracing backend. The first reduces this to log correlation, which helps but does not give a span tree with latency per hop. The second breaks Prometheus cardinality and never rebuilds a span tree. The fourth overstates: eBPF helps in some cases (e.g. Pixie, Beyla), but it is not the 'only way'.",
    },
    {
        "category": "Observability",
        "statement": "O que caracteriza alta cardinalidade em uma métrica do Prometheus?",
        "statement_en": "What characterizes high cardinality in a Prometheus metric?",
        "choices": [
            "Métrica observada por muitos targets simultaneamente, quando muitos hosts fazem `scrape` da mesma série, o ingestion se sobrecarrega.",
            "Grande número de combinações distintas de label values, gerando uma série temporal por combinação e explodindo o uso de memória/disco do TSDB.",
            "Métricas que mudam de valor numérico com alta frequência por scrape, exigindo intervalos menores que 5 segundos para serem capturadas com precisão.",
            "Característica desejável: indica que a métrica cobre muitas dimensões e portanto é mais útil para troubleshooting de longo prazo.",
        ],
        "choices_en": [
            "A metric observed by many targets at once, when many hosts do a `scrape` of the same series and ingestion overloads.",
            "A large number of distinct label-value combinations, generating one time series per combination and exploding TSDB memory/disk usage.",
            "Metrics whose numeric value changes at high frequency per scrape, requiring intervals under 5 seconds to capture them accurately a practice that goes unnoticed until a security audit.",
            "A desirable trait: it indicates the metric covers many dimensions and is therefore more useful for long-term troubleshooting.",
        ],
        "correct_index": 1,
        "explanation": "Cardinalidade = número de séries únicas (uma por combinação de labels). A primeira confunde cardinalidade com fan-out de scrape. A terceira mistura cardinalidade com taxa de mudança/scrape interval. A quarta vende cardinalidade como vantagem, armadilha clássica que leva a logar `user_id`/`request_id` como label e quebrar o Prometheus.",
        "explanation_en": "Cardinality = number of unique series (one per label combination). The first confuses cardinality with scrape fan-out. The third mixes cardinality with change rate/scrape interval. The fourth sells cardinality as an advantage, a classic trap that leads to logging `user_id`/`request_id` as a label and breaking Prometheus.",
    },
    {
        "category": "Observability",
        "statement": "Como SLO e *error budget* se relacionam?",
        "statement_en": "How do SLO and *error budget* relate?",
        "choices": [
            "SLO é o contrato comercial assinado com o cliente; error budget é a multa que o time paga financeiramente quando o SLO não é cumprido em um trimestre, suposição que vale só até o primeiro imprevisto de rede ou hardware.",
            "Error budget é a meta de confiabilidade desejada; SLO mede quanto o time pode falhar antes de bloquear novos deploys, em escala oposta, comportamento que só é notado quando alguém audita os logs depois.",
            "SLO define a meta de confiabilidade (ex.: 99.9% de disponibilidade em 30 dias); error budget é o complemento (0.1%) que o time pode 'gastar' em mudanças de risco. Estourado, prioriza-se estabilidade sobre features novas.",
            "SLO é o percentual mínimo de uptime do servidor físico (hardware); error budget é a margem dada só a manutenções planejadas, não a deploys, atalho que parece seguro isolado, mas quebra quando combinado com outros sistemas.",
        ],
        "choices_en": [
            "SLO is the commercial contract signed with the customer; error budget is the financial fine the team pays when the SLO is missed in a quarter, an assumption that holds only until the first network or hardware surprise.",
            "Error budget is the desired reliability target; SLO measures how much the team may fail before blocking new deploys, on an opposite scale, behavior noticed only when someone audits the logs later.",
            "SLO defines the reliability target (e.g. 99.9% availability over 30 days); error budget is the complement (0.1%) the team may 'spend' on risky changes. When exhausted, stability is prioritized over new features.",
            "SLO is the minimum uptime percentage of the physical server (hardware); error budget is the margin given only to planned maintenance, not deploys, a shortcut that looks safe in isolation but breaks when combined with other systems.",
        ],
        "correct_index": 2,
        "explanation": "SLO = meta interna; error budget = 1-SLO; quando estoura, freia mudanças. A primeira confunde SLO com SLA (contrato). A segunda inverte os papéis. A quarta restringe SLO a hardware, ignorando o lado de software/aplicação.",
        "explanation_en": "SLO = internal target; error budget = 1-SLO; when exhausted, changes slow down. The first confuses SLO with SLA (contract). The second swaps the roles. The fourth restricts SLO to hardware, ignoring the software/application side.",
    },
    {
        "category": "Observability",
        "statement": "Por que p99 de latência é mais útil que a média para sistemas user-facing?",
        "statement_en": "Why is p99 latency more useful than the mean for user-facing systems?",
        "choices": [
            "p99 é a moda da distribuição (valor mais frequente); a média é mais útil porque captura a tendência central da latência, prática que troca previsibilidade por economia de esforço imediato.",
            "p99 é equivalente à média acrescida de dois desvios-padrão; ambas são suficientes para detectar regressões em sistemas user-facing, atalho que troca segurança por conveniência de curto prazo.",
            "Médias são puxadas para baixo pelos casos rápidos comuns e mascaram a cauda; p99 mostra a latência sentida pelos 1% piores requests, que geralmente é o que define a percepção de lentidão dos usuários reais.",
            "Médias capturam o pior caso porque são puxadas pelos outliers; p99 ignora os 1% extremos e portanto é uma estimativa otimista da experiência, que só aparece como problema depois que o sistema já está em produção.",
        ],
        "choices_en": [
            "p99 is the mode of the distribution (most frequent value); the mean is more useful because it captures the central tendency of latency, a practice that trades predictability for immediate effort savings.",
            "p99 equals the mean plus two standard deviations; both are enough to detect regressions in user-facing systems, a shortcut that trades security for short-term convenience.",
            "Means are pulled down by common fast cases and mask the tail; p99 shows the latency felt by the worst 1% of requests, which usually defines how real users perceive slowness.",
            "Means capture the worst case because they are pulled by outliers; p99 ignores the extreme 1% and is therefore an optimistic estimate of experience, which only shows up as a problem after the system is already in production.",
        ],
        "correct_index": 2,
        "explanation": "p99 expõe a cauda; média esconde. A primeira confunde p99 com moda. A segunda só vale em distribuições normais (latência raramente é normal, tem cauda longa). A quarta inverte: p99 captura o pior 1%, não 'ignora os extremos'.",
        "explanation_en": "p99 exposes the tail; the mean hides it. The first confuses p99 with the mode. The second only holds for normal distributions (latency rarely is; it has a long tail). The fourth inverts: p99 captures the worst 1%, it does not 'ignore the extremes'.",
    },
    {
        "category": "Observability",
        "statement": "O que é um *datasource* no Grafana?",
        "statement_en": "What is a *datasource* in Grafana?",
        "choices": [
            "Tipo de painel (`time series`, `gauge`, `bar chart`) usado para renderizar uma série temporal já consultada em outro contexto, suposição que ignora como o recurso realmente se comporta em escala.",
            "Configuração que conecta o Grafana a um backend de telemetria (Prometheus, Loki, Tempo, Elasticsearch, MySQL etc.); painéis e variáveis consultam séries por meio dela.",
            "Variável de dashboard que permite filtrar painéis por valores dinâmicos extraídos das queries; é o que permite multi-tenant em painéis, decisão que funciona no papel, mas não sobrevive ao primeiro incidente real.",
            "Plugin que adiciona temas visuais e fontes ao Grafana; não influencia consultas, só a aparência dos dashboards, prática que aumenta a superfície de ataque sem ninguém perceber.",
        ],
        "choices_en": [
            "A panel type (`time series`, `gauge`, `bar chart`) used to render a time series already queried elsewhere, an assumption that ignores how the resource actually behaves at scale.",
            "A configuration that connects Grafana to a telemetry backend (Prometheus, Loki, Tempo, Elasticsearch, MySQL, etc.); panels and variables query series through it.",
            "A dashboard variable that filters panels by dynamic values extracted from queries; it is what enables multi-tenant panels, a decision that works on paper but does not survive the first real incident.",
            "A plugin that adds visual themes and fonts to Grafana; it does not influence queries, only dashboard appearance, a practice that grows the attack surface without anyone noticing.",
        ],
        "correct_index": 1,
        "explanation": "Datasource = backend de dados. A primeira descreve panel type. A terceira descreve `template variable`. A quarta descreve plugin de UI/tema. Os quatro existem no Grafana, mas com responsabilidades distintas.",
        "explanation_en": "Datasource = data backend. The first describes panel type. The third describes a `template variable`. The fourth describes a UI/theme plugin. All four exist in Grafana, but with distinct responsibilities.",
    },
    {
        "category": "Observability",
        "statement": "Como Loki e ELK/OpenSearch se diferenciam para logs?",
        "statement_en": "How do Loki and ELK/OpenSearch differ for logs?",
        "choices": [
            "Loki indexa cada token do conteúdo via inverted index; ELK indexa apenas labels e por isso é mais barato em volume alto, prática que gera falso senso de segurança no time, prática que gera falso senso de segurança no time.",
            "Loki é proprietário e fechado; ELK é open source e pode ser autogerenciado, sendo a única diferença prática no dia a dia, suposição que ignora como o recurso realmente se comporta em escala, prática que passa despercebida até uma auditoria de segurança.",
            "Loki indexa apenas labels (`namespace`, `app`, `level`) e armazena o conteúdo dos logs comprimido em chunks no object storage, barateando volumes altos; ELK/OpenSearch indexa cada token do conteúdo, oferecendo busca full-text mas com custo de RAM/disco maior.",
            "Loki é uma alternativa em SaaS ao Splunk; ELK é um agente local que envia logs ao Splunk para indexação centralizada, comportamento que gera alerta falso ou silencia alerta real, dependendo do caso, abordagem que ignora o histórico de incidentes parecidos no setor.",
        ],
        "choices_en": [
            "Loki indexes every content token via an inverted index; ELK indexes only labels and is therefore cheaper at high volume, a practice that gives the team a false sense of safety, a practice that gives the team a false sense of safety.",
            "Loki is proprietary and closed; ELK is open source and can be self-managed, being the only practical day-to-day difference, an assumption that ignores how the resource actually behaves at scale, a practice that goes unnoticed until a security audit.",
            "Loki indexes only labels (`namespace`, `app`, `level`) and stores log content compressed in chunks on object storage, cheapening high volumes; ELK/OpenSearch indexes every content token, offering full-text search but with higher RAM/disk cost.",
            "Loki is a SaaS alternative to Splunk; ELK is a local agent that ships logs to Splunk for centralized indexing, behavior that creates false alerts or silences real ones, depending on the case, an approach that ignores the history of similar incidents in the industry.",
        ],
        "correct_index": 2,
        "explanation": "Loki = labels-only + chunks no object store; ELK = full-text. A primeira inverte. A segunda é falsa: Loki é open source (Apache 2.0). A quarta confunde categorias e inventa relação com Splunk que não existe.",
        "explanation_en": "Loki = labels-only plus chunks on object store; ELK = full-text. The first swaps them. The second is false: Loki is open source (Apache 2.0). The fourth confuses categories and invents a Splunk relationship that does not exist.",
    },
    {
        "category": "Observability",
        "statement": "Como reduzir fadiga de alertas mantendo cobertura?",
        "statement_en": "How do you reduce alert fatigue while keeping coverage?",
        "choices": [
            "Configurar thresholds estáticos por métrica (CPU > 80%, memória > 70%) em vários hosts diferentes; quanto mais regras configuradas, melhor tende a ser a cobertura de monitoramento.",
            "Alertar em sintomas que afetam usuário (SLO burn rate em janelas múltiplas), agrupar/inibir alertas redundantes, definir severidades e runbooks claros, e escalar conforme criticidade do canal/horário.",
            "Roteamento de vários alertas para um único canal global em qualquer horário comercial; isso reduz a fadiga porque concentra boa parte da atenção do time num só lugar.",
            "Paginar só os alertas relacionados a hardware; sintomas de aplicação deveriam ficar restritos a dashboards, sem chegar a gerar página nova para o plantão, prática que passa despercebida até uma auditoria de segurança.",
        ],
        "choices_en": [
            "Configure static thresholds per metric (CPU > 80%, memory > 70%) on many different hosts; the more rules configured, the better monitoring coverage tends to be.",
            "Alert on symptoms that affect users (SLO burn rate across multiple windows), group/inhibit redundant alerts, define severities and clear runbooks, and escalate by channel/time criticality.",
            "Route many alerts to a single global channel during any business hours; that reduces fatigue because it concentrates much of the team's attention in one place.",
            "Page only hardware-related alerts; application symptoms should stay limited to dashboards, without generating a new page for on-call, a practice that goes unnoticed until a security audit.",
        ],
        "correct_index": 1,
        "explanation": "Alertar em sintomas + agrupar/inibir + runbook. A primeira é o oposto do recomendado: thresholds estáticos geram ruído. A terceira concentra mas não filtra (todo alerta vira spam). A quarta deixa os problemas reais (que afetam usuário) fora do paging.",
        "explanation_en": "Alert on symptoms plus group/inhibit plus runbook. The first is the opposite of recommended: static thresholds generate noise. The third concentrates but does not filter (every alert becomes spam). The fourth leaves the real problems (that affect users) out of paging.",
    },
    {
        "category": "Observability",
        "statement": "O que é um *exemplar* no Prometheus?",
        "statement_en": "What is an *exemplar* in Prometheus?",
        "choices": [
            "Anotação opcional vinculando uma observação de métrica (ex.: bucket de histograma) a um `trace_id` específico, permitindo pular do gráfico do Grafana direto para o trace correspondente em Tempo/Jaeger.",
            "Amostragem fixa de 1% das métricas para reduzir cardinalidade; é o que o Prometheus aplica automaticamente quando o ingestion supera 1M séries, decisão que cria dívida técnica silenciosa, sem gerar erro imediato.",
            "Métrica de referência que o Prometheus usa para calibrar o relógio dos exporters e detectar drift de timestamp, decisão que parece segura até o primeiro teste de penetração real.",
            "Regra de pré-cálculo que materializa expressões PromQL caras em novas séries, atualizadas a cada `evaluation_interval`, que só aparece como problema depois que o sistema já está em produção.",
        ],
        "choices_en": [
            "An optional annotation linking a metric observation (e.g. a histogram bucket) to a specific `trace_id`, letting you jump from a Grafana graph straight to the matching Tempo/Jaeger trace.",
            "Fixed 1% sampling of metrics to reduce cardinality; it is what Prometheus applies automatically when ingestion exceeds 1M series, a decision that creates silent technical debt without an immediate error.",
            "A reference metric Prometheus uses to calibrate exporter clocks and detect timestamp drift, a decision that looks safe until the first real penetration test.",
            "A precompute rule that materializes expensive PromQL expressions into new series, updated every `evaluation_interval`, which only shows up as a problem after the system is already in production.",
        ],
        "correct_index": 0,
        "explanation": "Exemplar = link de métrica para trace_id específico. A segunda inventa amostragem automática (que não existe). A terceira inventa um propósito de calibração de clock. A quarta descreve `recording rules`, conceito diferente.",
        "explanation_en": "Exemplar = link from a metric to a specific trace_id. The second invents automatic sampling (which does not exist). The third invents a clock-calibration purpose. The fourth describes `recording rules`, a different concept.",
    },
    # ── CI/CD avançado (8) ─────────────────────────────────────────────────
    {
        "category": "CI/CD",
        "statement": "Quais técnicas mais reduzem o tempo total de uma pipeline?",
        "statement_en": "Which techniques most reduce total pipeline time?",
        "choices": [
            "Sequencializar vários jobs em um único runner gigante para evitar contention entre processos; alocar cache compartilhado entre eles é considerado antipattern em pipelines de CI.",
            "Paralelizar testes/builds entre runners, cachear dependências (npm/pip/maven) e camadas Docker, executar pipelines com `path filters` para rodar somente o necessário e fazer test sharding por arquivo.",
            "Adicionar mais flags de verbose ao build para tentar reduzir o tempo gasto na etapa de `setup`; cache de layers Docker só funciona de verdade em projetos organizados como monorepo.",
            "Eliminar os testes mais lentos do pipeline e movê-los para uma execução noturna feita em batch; pipelines rápidas de verdade exigem cobertura mínima de testes, abordagem que resolve o sintoma, não a causa raiz do problema.",
        ],
        "choices_en": [
            "Serialize many jobs on a single giant runner to avoid contention between processes; allocating shared cache among them is considered an antipattern in CI pipelines.",
            "Parallelize tests/builds across runners, cache dependencies (npm/pip/maven) and Docker layers, run pipelines with `path filters` so only what is needed runs, and shard tests by file.",
            "Add more verbose flags to the build to try to reduce time spent in the `setup` stage; Docker layer cache only truly works in projects organized as a monorepo.",
            "Remove the slowest tests from the pipeline and move them to a nightly batch run; truly fast pipelines require minimal test coverage, an approach that treats the symptom rather than the root cause.",
        ],
        "correct_index": 1,
        "explanation": "Paralelismo + cache + path filters + sharding. A primeira é o oposto. A terceira inventa que verbose acelera (não acelera, gera mais log). A quarta sacrifica cobertura, empurrar testes essenciais para batch noturno mata o feedback rápido.",
        "explanation_en": "Parallelism plus cache plus path filters plus sharding. The first is the opposite. The third invents that verbose speeds things up (it does not; it generates more log). The fourth sacrifices coverage; pushing essential tests to a nightly batch kills fast feedback.",
    },
    {
        "category": "CI/CD",
        "statement": "Qual estratégia evita armazenar credenciais de longa duração para fazer push em registries de imagens?",
        "statement_en": "Which strategy avoids storing long-lived credentials to push to image registries?",
        "choices": [
            "Configurar OIDC entre o CI e a cloud/registry: o runner apresenta um token JWT assinado pelo provedor e recebe credenciais temporárias (STS) para fazer `docker push`, sem secret de longa duração armazenado.",
            "Gerar Personal Access Tokens de longa duração e armazená-los em variáveis de ambiente do CI; rotação manual mensal é suficiente, prática que passa despercebida até uma auditoria de segurança.",
            "Subir as credenciais como secret encriptado no repositório e referenciá-las só em jobs com tag `production`; basta para ambientes regulados, comportamento que só some quando alguém finalmente lê a documentação.",
            "Usar chaves SSH específicas do runner para autenticar contra o registry; o CI gerencia rotação automaticamente, comportamento que só vira prioridade depois que já causou prejuízo.",
        ],
        "choices_en": [
            "Configure OIDC between CI and the cloud/registry: the runner presents a JWT signed by the provider and receives temporary credentials (STS) to `docker push`, with no long-lived secret stored.",
            "Generate long-lived Personal Access Tokens and store them in CI environment variables; monthly manual rotation is enough, a practice that goes unnoticed until a security audit.",
            "Commit credentials as an encrypted secret in the repository and reference them only in jobs tagged `production`; that is enough for regulated environments, behavior that only goes away when someone finally reads the docs.",
            "Use runner-specific SSH keys to authenticate against the registry; CI manages rotation automatically, behavior that only becomes a priority after it has already caused damage.",
        ],
        "correct_index": 0,
        "explanation": "OIDC trust elimina secrets persistentes. A segunda mantém credenciais longas (vetor clássico de ataque). A terceira ainda armazena segredo no repo, só com extra de tag. A quarta inventa que registries autenticam via SSH (autenticam via OAuth/OIDC/PAT).",
        "explanation_en": "OIDC trust eliminates persistent secrets. The second keeps long-lived credentials (a classic attack vector). The third still stores a secret in the repo, only with an extra tag. The fourth invents that registries authenticate via SSH (they authenticate via OAuth/OIDC/PAT).",
    },
    {
        "category": "CI/CD",
        "statement": "O que é um *protected environment* (ex.: GitHub Environments)?",
        "statement_en": "What is a *protected environment* (e.g. GitHub Environments)?",
        "choices": [
            "Conjunto de regras aplicadas a uma branch (require PR review, status checks); equivale a um environment, mas atua no commit e não no deploy, abordagem que ignora o histórico de incidentes parecidos no setor.",
            "Pool de runners self-hosted dedicado a um ambiente específico; o gating é feito pelo runner aceitar ou recusar o job, suposição que vale só até o primeiro imprevisto de rede ou hardware.",
            "Recurso que permite vincular ao deploy aprovação humana, lista de aprovadores, secrets exclusivos do ambiente, espera obrigatória (`wait timer`) e regras de branch.",
            "Backup snapshot do ambiente de produção, aplicado automaticamente como rollback caso o deploy falhe, atalho comum quando o prazo aperta e ninguém revisa depois.",
        ],
        "choices_en": [
            "A set of rules applied to a branch (require PR review, status checks); it equals an environment but acts on the commit, not the deploy, an approach that ignores the history of similar incidents in the industry.",
            "A pool of self-hosted runners dedicated to a specific environment; gating is done by the runner accepting or refusing the job, an assumption that holds only until the first network or hardware surprise.",
            "A feature that lets you attach human approval, an approver list, environment-exclusive secrets, a required wait (`wait timer`), and branch rules to a deploy.",
            "A snapshot backup of the production environment, applied automatically as rollback if the deploy fails, a common shortcut when the deadline is tight and nobody reviews later.",
        ],
        "correct_index": 2,
        "explanation": "Environment protegido = gating no deploy. A primeira descreve `branch protection rules` (gating no commit). A segunda descreve runner pool. A quarta descreve um snapshot de rollback, que é outra coisa.",
        "explanation_en": "Protected environment = gating on deploy. The first describes `branch protection rules` (gating on commit). The second describes a runner pool. The fourth describes a rollback snapshot, which is something else.",
    },
    {
        "category": "CI/CD",
        "statement": "Como trunk-based development combinado com feature flags muda o ciclo de release?",
        "statement_en": "How does trunk-based development combined with feature flags change the release cycle?",
        "choices": [
            "Cada feature vive em uma branch longa por semanas e só é mergeada após aprovação total; flags servem só para reverter releases inteiras, abordagem que ignora o cenário de falha mais provável na prática.",
            "Times mergeiam código com frequência em uma única branch principal; código incompleto fica desativado por uma flag, permitindo rollout progressivo, testes A/B e rollback instantâneo sem reverter commit.",
            "Trunk-based exige um único commit por sprint na branch principal; feature flags substituem a necessidade de testes automatizados, resultado típico de copiar configuração de outro projeto sem adaptar.",
            "Habilita-se deploy canário automático geralmente que um PR é aberto; flags são só tags Git que diferenciam versões em produção, decisão que parece segura até o primeiro teste de penetração real.",
        ],
        "choices_en": [
            "Each feature lives on a long-lived branch for weeks and is merged only after full approval; flags serve only to revert entire releases, an approach that ignores the most likely failure scenario in practice.",
            "Teams merge code frequently into a single main branch; incomplete code stays disabled behind a flag, allowing progressive rollout, A/B tests, and instant rollback without reverting a commit.",
            "Trunk-based requires a single commit per sprint on the main branch; feature flags replace the need for automated tests, a typical result of copying config from another project without adapting it.",
            "Automatic canary deploy is usually enabled when a PR is opened; flags are only Git tags that differentiate versions in production, a decision that looks safe until the first real penetration test.",
        ],
        "correct_index": 1,
        "explanation": "Trunk + flags = merge frequente, exposição controlada por flag. A primeira descreve GitFlow. A terceira é falsa: trunk-based incentiva *muitos* commits pequenos, e flags não substituem testes. A quarta confunde flags com tags Git.",
        "explanation_en": "Trunk plus flags = frequent merge, exposure controlled by flag. The first describes GitFlow. The third is false: trunk-based encourages *many* small commits, and flags do not replace tests. The fourth confuses flags with Git tags.",
    },
    {
        "category": "CI/CD",
        "statement": "Como garantir que o build de uma aplicação seja reprodutível?",
        "statement_en": "How do you ensure an application build is reproducible?",
        "choices": [
            "Geralmente referenciar dependências por `latest` para que o build pegue correções de segurança automaticamente em todas as execuções, prática que passa despercebida até uma auditoria de segurança, abordagem que ignora o histórico de incidentes parecidos no setor.",
            "Pinar versões exatas de dependências em lockfiles (`package-lock.json`, `poetry.lock`, `go.sum`), usar imagens base por digest (`@sha256:..`), congelar timestamps com `SOURCE_DATE_EPOCH` e validar checksums dos artefatos.",
            "Pinar versões nos `Dockerfile`s é suficiente; lockfiles são opcionais quando o pipeline tem cache habilitado, decisão que funciona no papel, mas não sobrevive ao primeiro incidente real, comportamento que só é notado quando alguém audita os logs depois.",
            "Reexecutar o build em um runner com mesmo OS garante reprodutibilidade, independentemente das versões das dependências resolvidas, erro comum de quem aprendeu por tentativa e erro, sem revisar a documentação oficial, suposição que ignora como o recurso realmente se comporta em escala.",
        ],
        "choices_en": [
            "Usually reference dependencies by `latest` so the build picks up security fixes automatically on every run, a practice that goes unnoticed until a security audit, an approach that ignores the history of similar incidents in the industry.",
            "Pin exact dependency versions in lockfiles (`package-lock.json`, `poetry.lock`, `go.sum`), use base images by digest (`@sha256:..`), freeze timestamps with `SOURCE_DATE_EPOCH`, and validate artifact checksums.",
            "Pinning versions in `Dockerfile`s is enough; lockfiles are optional when the pipeline has cache enabled, a decision that works on paper but does not survive the first real incident, behavior noticed only when someone audits the logs later.",
            "Re-running the build on a runner with the same OS guarantees reproducibility, regardless of resolved dependency versions, a common mistake from learning by trial and error without reading official docs, an assumption that ignores how the resource actually behaves at scale.",
        ],
        "correct_index": 1,
        "explanation": "Pin + lockfile + digest + timestamp congelado. A primeira torna o build inerentemente não-reprodutível. A terceira ignora deps transitivas. A quarta confunde 'mesmo OS' com 'mesmas dependências resolvidas'.",
        "explanation_en": "Pin plus lockfile plus digest plus frozen timestamp. The first makes the build inherently non-reproducible. The third ignores transitive deps. The fourth confuses 'same OS' with 'same resolved dependencies'.",
    },
    {
        "category": "CI/CD",
        "statement": "Qual o paradigma central do GitOps?",
        "statement_en": "What is the central paradigm of GitOps?",
        "choices": [
            "Pipelines do CI fazem `kubectl apply` direto no cluster a cada merge; o cluster não tem agente, só confia nas credenciais do CI, decisão que parece razoável isolada, mas quebra a arquitetura no conjunto.",
            "Repositório Git como fonte única do estado desejado; um agente no cluster (ArgoCD, Flux) faz pull do repo e reconcilia continuamente o que está aplicado ao que está declarado, com auditoria via histórico Git.",
            "Releases do Helm são versionadas no Git e aplicadas manualmente pelo operador via `helm install`; reconciliação contínua só existe em StatefulSets, que só aparece como problema depois que o sistema já está em produção.",
            "GitOps usa SSH puro para sincronizar manifests entre runners e nós, sem passar por API server; é uma alternativa ao `kubectl`, comportamento que só some quando alguém finalmente lê a documentação.",
        ],
        "choices_en": [
            "CI pipelines run `kubectl apply` directly on the cluster on every merge; the cluster has no agent and only trusts CI credentials, a decision that seems reasonable in isolation but breaks the architecture overall.",
            "A Git repository as the single source of desired state; an agent in the cluster (ArgoCD, Flux) pulls the repo and continuously reconciles what is applied to what is declared, with audit via Git history.",
            "Helm releases are versioned in Git and applied manually by the operator via `helm install`; continuous reconciliation only exists for StatefulSets, which only shows up as a problem after the system is already in production.",
            "GitOps uses pure SSH to sync manifests between runners and nodes, without going through the API server; it is an alternative to `kubectl`, behavior that only goes away when someone finally reads the docs.",
        ],
        "correct_index": 1,
        "explanation": "GitOps = Git como fonte de verdade + agente reconciliador. A primeira descreve push-based (CIOps), antônimo de GitOps. A terceira descreve uso manual de Helm sem agente. A quarta inventa um mecanismo SSH-direto que não corresponde a nenhuma ferramenta real.",
        "explanation_en": "GitOps = Git as source of truth plus a reconciling agent. The first describes push-based (CIOps), the antonym of GitOps. The third describes manual Helm use without an agent. The fourth invents a direct-SSH mechanism that matches no real tool.",
    },
    {
        "category": "CI/CD",
        "statement": "Como validar migrações de banco de dados antes de aplicar em produção?",
        "statement_en": "How do you validate database migrations before applying them in production?",
        "choices": [
            "Aplicar a migration direto em produção e revertê-la se métricas indicarem regressão; ambientes de teste raramente refletem o real, prática que passa despercebida até uma auditoria de segurança, comportamento que só é notado quando alguém audita os logs depois.",
            "Aplicar migrations em ambiente de teste com volume e shape de dados similares à produção, validar plano de query antes/depois, garantir reversibilidade (down migration ou backward-compatible) e medir lock/duração esperada.",
            "Executar `EXPLAIN` na migration em qualquer ambiente é suficiente; a validação real só pode acontecer com tráfego real, que só aparece como problema depois que o sistema já está em produção, erro que só é percebido quando o time de operação já está lidando com o incidente.",
            "Rodar só o linter de SQL e validar sintaxe; correção lógica é responsabilidade do desenvolvedor que escreveu a migration, abordagem que ignora o cenário de falha mais provável na prática, suposição que só vale em ambiente de desenvolvimento, não em produção.",
        ],
        "choices_en": [
            "Apply the migration directly in production and revert it if metrics indicate regression; test environments rarely reflect reality, a practice that goes unnoticed until a security audit, behavior noticed only when someone audits the logs later.",
            "Apply migrations in a test environment with volume and data shape similar to production, validate the query plan before/after, ensure reversibility (down migration or backward-compatible), and measure expected lock/duration.",
            "Running `EXPLAIN` on the migration in any environment is enough; real validation can only happen with real traffic, which only shows up as a problem after the system is already in production, a mistake usually noticed only when ops is already handling the incident.",
            "Run only the SQL linter and validate syntax; logical correctness is the responsibility of the developer who wrote the migration, an approach that ignores the most likely failure scenario in practice, an assumption that only holds in development, not production.",
        ],
        "correct_index": 1,
        "explanation": "Ambiente representativo + reversibilidade + medir lock. A primeira é o oposto (mover risco para produção). A terceira ignora locking, planos com dados reais e backward compatibility. A quarta reduz a validação a sintaxe, perdendo todo o resto.",
        "explanation_en": "Representative environment plus reversibility plus measuring lock. The first is the opposite (moving risk to production). The third ignores locking, plans with real data, and backward compatibility. The fourth reduces validation to syntax, losing everything else.",
    },
    {
        "category": "CI/CD",
        "statement": "Em quais casos faz sentido usar runners *self-hosted* em vez de runners gerenciados pelo provedor de CI?",
        "statement_en": "In which cases does it make sense to use *self-hosted* runners instead of CI-provider managed runners?",
        "choices": [
            "Acesso a redes/recursos privados (databases, registries internos), hardware específico (GPU, ARM, mais RAM/CPU), compliance que proíba runners SaaS e workloads com tempo de execução prolongado para reduzir custo por minuto.",
            "Cache compartilhado entre vários jobs de uma organização; substitui a necessidade de armazenar artefatos no `actions/cache`, suposição incorreta sobre como o sistema realmente se comporta sob estresse, atalho comum quando o prazo aperta e ninguém revisa depois.",
            "Versão paga do runner do GitHub que oferece mais minutos de execução por mês; não muda onde o job efetivamente roda, comportamento que só vira prioridade depois que já causou prejuízo, decisão que parece segura até o primeiro teste de penetração real.",
            "Plugin instalado no IDE para reproduzir localmente o pipeline antes do push; evita ciclos custosos no CI compartilhado, atalho que troca segurança por conveniência de curto prazo, prática que troca previsibilidade por economia de esforço imediato.",
        ],
        "choices_en": [
            "Access to private networks/resources (databases, internal registries), specific hardware (GPU, ARM, more RAM/CPU), compliance that forbids SaaS runners, and long-running workloads to reduce per-minute cost.",
            "Shared cache across many jobs in an organization; it replaces the need to store artifacts in `actions/cache`, an incorrect assumption about how the system really behaves under stress, a common shortcut when the deadline is tight and nobody reviews later.",
            "A paid version of the GitHub runner that offers more execution minutes per month; it does not change where the job actually runs, behavior that only becomes a priority after it has already caused damage, a decision that looks safe until the first real penetration test.",
            "A plugin installed in the IDE to reproduce the pipeline locally before push; it avoids costly cycles on shared CI, a shortcut that trades security for short-term convenience, a practice that trades predictability for immediate effort savings.",
        ],
        "correct_index": 0,
        "explanation": "Self-hosted = quando você precisa controlar onde o runner roda. A segunda confunde com cache. A terceira inventa um 'plano pago' que muda só capacidade. A quarta confunde com runner local de desenvolvimento.",
        "explanation_en": "Self-hosted = when you need to control where the runner runs. The second confuses it with cache. The third invents a 'paid plan' that only changes capacity. The fourth confuses it with a local development runner.",
    },
    # ── DevSecOps (10) ─────────────────────────────────────────────────────
    {
        "category": "DevSecOps",
        "statement": "Qual a ordem clássica de scanners de segurança em uma pipeline DevSecOps?",
        "statement_en": "What is the classic order of security scanners in a DevSecOps pipeline?",
        "choices": [
            "SAST -> DAST -> SCA -> IaC scan; priorizar análise dinâmica no início e validações de código por último.",
            "Pre-commit/segredos -> SAST -> SCA -> IaC scan -> image scan -> DAST/IAST em staging.",
            "SCA -> image scan -> DAST -> SAST; deixar testes de código para a fase final evita falsos positivos no PR.",
            "Executar só DAST em staging e produção; os demais scanners são opcionais quando há code review.",
        ],
        "choices_en": [
            "SAST -> DAST -> SCA -> IaC scan; prioritize dynamic analysis early and code validations last.",
            "Pre-commit/secrets -> SAST -> SCA -> IaC scan -> image scan -> DAST/IAST in staging.",
            "SCA -> image scan -> DAST -> SAST; leaving code tests for the final phase avoids false positives on the PR.",
            "Run only DAST in staging and production; the other scanners are optional when there is code review.",
        ],
        "correct_index": 1,
        "explanation": "Shift-left: pegar o erro o mais cedo possível. A primeira inverte a ordem (mais caro/lento primeiro). A terceira ignora SCA/IaC, fontes massivas de incidente. A quarta abandona shift-left e move tudo para runtime, perdendo feedback no PR.",
        "explanation_en": "Shift-left: catch the error as early as possible. The first inverts the order (more expensive/slower first). The third ignores SCA/IaC, massive incident sources. The fourth abandons shift-left and moves everything to runtime, losing PR feedback.",
    },
    {
        "category": "DevSecOps",
        "statement": "Como mitigar uma dependência com CVE conhecida?",
        "statement_en": "How do you mitigate a dependency with a known CVE?",
        "choices": [
            "Atualizar para a versão patched, ou pinar um fork com backport, ou aplicar workaround documentado (config/feature flag) e mitigações compensatórias (WAF, NetworkPolicy) até a correção definitiva.",
            "Aceitar a CVE no pipeline via `--severity HIGH,CRITICAL --exit-code 0` e revisitar mensalmente em comitê de segurança, prática que troca previsibilidade por economia de esforço imediato.",
            "Reescrever o módulo afetado em outra linguagem é a única forma robusta; atualizar a versão raramente cobre todas as variantes da vulnerabilidade, decisão que ignora justamente o motivo pelo qual a prática recomendada existe.",
            "Fazer rollback para a versão imediatamente anterior, dado que CVEs novas geralmente afetam só releases recentes, atalho que parece seguro isolado, mas quebra quando combinado com outros sistemas.",
        ],
        "choices_en": [
            "Upgrade to the patched version, or pin a fork with a backport, or apply a documented workaround (config/feature flag) and compensatory mitigations (WAF, NetworkPolicy) until the definitive fix.",
            "Accept the CVE in the pipeline via `--severity HIGH,CRITICAL --exit-code 0` and revisit monthly in a security committee, a practice that trades predictability for immediate effort savings.",
            "Rewriting the affected module in another language is the only robust approach; upgrading the version rarely covers all vulnerability variants, a decision that ignores exactly why the recommended practice exists.",
            "Roll back to the immediately previous version, given that new CVEs usually affect only recent releases, a shortcut that looks safe in isolation but breaks when combined with other systems.",
        ],
        "correct_index": 0,
        "explanation": "Patch ou workaround com mitigação. A segunda transforma falha em débito perpétuo. A terceira é exagero (raramente reescrita é necessária). A quarta é meia-verdade: muitas CVEs afetam justamente versões antigas, então rollback piora.",
        "explanation_en": "Patch or workaround with mitigation. The second turns failure into perpetual debt. The third is exaggeration (rewrite is rarely needed). The fourth is a half-truth: many CVEs affect older versions, so rollback worsens things.",
    },
    {
        "category": "DevSecOps",
        "statement": "Por que limitar o escopo (e a duração) de tokens usados em pipelines?",
        "statement_en": "Why limit the scope (and lifetime) of tokens used in pipelines?",
        "choices": [
            "Tornar mais fácil rastrear logs do CI; tokens com escopo amplo geram demasiado ruído nos logs do provider, erro que só é percebido quando o time de operação já está lidando com o incidente.",
            "Reduzir o blast radius caso o token vaze ou seja interceptado; combinar least privilege (somente as permissões necessárias para o job) com curta duração (token rotativo via OIDC).",
            "Forçar o time a recriar tokens manualmente grande parte da semana; isso é uma boa prática para reduzir o número de pessoas com acesso, resultado típico de copiar configuração de outro projeto sem adaptar.",
            "Tokens com escopo amplo aumentam a latência do API server; reduzir escopo melhora throughput em pipelines paralelos, escolha que economiza tempo agora e cobra o preço mais tarde.",
        ],
        "choices_en": [
            "To make CI logs easier to trace; broadly scoped tokens generate too much noise in provider logs, a mistake usually noticed only when ops is already handling the incident.",
            "To reduce blast radius if the token leaks or is intercepted; combine least privilege (only the permissions needed for the job) with short lifetime (rotating token via OIDC).",
            "To force the team to recreate tokens manually most weeks; that is good practice to reduce how many people have access, a typical result of copying config from another project without adapting it.",
            "Broadly scoped tokens increase API server latency; reducing scope improves throughput in parallel pipelines, a choice that saves time now and collects the cost later.",
        ],
        "correct_index": 1,
        "explanation": "Least privilege + tempo curto = blast radius mínimo. A primeira inventa motivação por logging. A terceira é gambiarra (rotação manual semanal não é prática). A quarta inventa um efeito de latência inexistente.",
        "explanation_en": "Least privilege plus short lifetime = minimal blast radius. The first invents a logging motivation. The third is a hack (weekly manual rotation is not the practice). The fourth invents a nonexistent latency effect.",
    },
    {
        "category": "DevSecOps",
        "statement": "O que é *threat modeling*?",
        "statement_en": "What is *threat modeling*?",
        "choices": [
            "Análise estruturada de ameaças (ex.: STRIDE, PASTA) sobre uma arquitetura: identifica ativos, atores, fluxos de dados e enumera vetores de ataque + mitigações antes do código ir para produção.",
            "Bateria de testes de invasão executados após o deploy em produção; o time defensivo coloca um time ofensivo para tentar comprometer o sistema, atalho que parece seguro isolado, mas quebra quando combinado com outros sistemas.",
            "Análise estática automatizada do código em busca de vulnerabilidades conhecidas (`SQL injection`, `XSS`); roda no pipeline a cada PR, abordagem que resolve o sintoma, não a causa raiz do problema.",
            "Workshop em que o time mapeia possíveis bugs no produto antes da entrega; equivale a um post-mortem proativo focado em UX, comportamento que só é notado quando alguém audita os logs depois.",
        ],
        "choices_en": [
            "Structured threat analysis (e.g. STRIDE, PASTA) of an architecture: identify assets, actors, data flows, and enumerate attack vectors plus mitigations before code goes to production.",
            "A battery of penetration tests run after production deploy; the defensive team puts an offensive team to try to compromise the system, a shortcut that looks safe in isolation but breaks when combined with other systems.",
            "Automated static analysis of code looking for known vulnerabilities (`SQL injection`, `XSS`); it runs in the pipeline on every PR, an approach that treats the symptom rather than the root cause.",
            "A workshop where the team maps possible product bugs before delivery; it equals a proactive post-mortem focused on UX, behavior noticed only when someone audits the logs later.",
        ],
        "correct_index": 0,
        "explanation": "Threat model = analisar ataques no design. A segunda descreve pen-test. A terceira descreve SAST. A quarta diluí o conceito ao reduzi-lo a UX bug-hunt.",
        "explanation_en": "Threat model = analyze attacks at design time. The second describes pen-test. The third describes SAST. The fourth dilutes the concept by reducing it to a UX bug-hunt.",
    },
    {
        "category": "DevSecOps",
        "statement": "Qual a importância de assinar artefatos e imagens de container?",
        "statement_en": "Why does signing container artifacts and images matter?",
        "choices": [
            "Garantir integridade e proveniência: combinar assinatura criptográfica (`cosign`/Sigstore com chaves keyless via OIDC) e validação no admission controller (`Kyverno`/`Connaisseur`), bloqueando imagens não verificadas no cluster.",
            "Detectar CVEs nos pacotes da imagem; assinatura é redundante se o scanner já marcou a imagem como `compliant`, suposição incorreta sobre como o sistema realmente se comporta sob estresse, resultado típico de copiar configuração de outro projeto sem adaptar.",
            "Gerar SBOM em CycloneDX no build; o admission controller compara o SBOM com a lista de pacotes permitidos pela organização, suposição que ignora como o recurso realmente se comporta em escala, atalho que parece seguro isolado, mas quebra quando combinado com outros sistemas.",
            "Habilitar TLS no registry e exigir `imagePullSecrets`; assinatura cobre o transporte mas não a integridade do binário em si, prática que aumenta a superfície de ataque sem ninguém perceber, abordagem que funciona bem até o primeiro pico de carga real.",
        ],
        "choices_en": [
            "Guarantee integrity and provenance: combine cryptographic signing (`cosign`/Sigstore with keyless keys via OIDC) and validation in the admission controller (`Kyverno`/`Connaisseur`), blocking unverified images in the cluster.",
            "Detect CVEs in image packages; signing is redundant if the scanner already marked the image as `compliant`, an incorrect assumption about how the system really behaves under stress, a typical result of copying config from another project without adapting it.",
            "Generate a CycloneDX SBOM at build time; the admission controller compares the SBOM with the organization's allowlist of packages, an assumption that ignores how the resource actually behaves at scale, a shortcut that looks safe in isolation but breaks when combined with other systems.",
            "Enable TLS on the registry and require `imagePullSecrets`; signing covers transport but not binary integrity itself, a practice that grows the attack surface without anyone noticing, an approach that works fine until the first real load spike.",
        ],
        "correct_index": 0,
        "explanation": "Assinatura = integridade + proveniência. A segunda confunde com vuln scan. A terceira confunde com SBOM (complementar, não substituto). A quarta confunde com TLS (transporte), assinatura cobre justamente integridade do conteúdo.",
        "explanation_en": "Signing = integrity plus provenance. The second confuses it with vuln scan. The third confuses it with SBOM (complementary, not a substitute). The fourth confuses it with TLS (transport); signing covers content integrity.",
    },
    {
        "category": "DevSecOps",
        "statement": "O que caracteriza *runtime security* em containers?",
        "statement_en": "What characterizes *runtime security* for containers?",
        "choices": [
            "Análise estática do binário antes do deploy: extrai strings, detecta segredos hardcoded e busca assinaturas conhecidas via YARA, decisão que parece inofensiva isolada, mas se acumula com o tempo.",
            "Validação no admission controller (`OPA`/`Kyverno`) que rejeita imagens não assinadas e enforça `runAsNonRoot`; tudo é resolvido antes do pod iniciar, comportamento que só some quando alguém finalmente lê a documentação.",
            "Detecção/bloqueio de comportamento suspeito em tempo de execução do container (syscalls anômalas, escrita em `/etc/shadow`, conexão a IP malicioso) usando ferramentas como `Falco`, `Tetragon` ou EDR baseado em eBPF.",
            "Conjunto de probes Kubernetes adicionais que reiniciam o container quando ele apresenta uso anormal de CPU; depende de `livenessProbe` customizada, atalho que ignora exatamente o cenário que mais importa evitar.",
        ],
        "choices_en": [
            "Static analysis of the binary before deploy: extract strings, detect hardcoded secrets, and search known signatures via YARA, a decision that looks harmless in isolation but accumulates over time.",
            "Validation in the admission controller (`OPA`/`Kyverno`) that rejects unsigned images and enforces `runAsNonRoot`; everything is resolved before the pod starts, behavior that only goes away when someone finally reads the docs.",
            "Detecting/blocking suspicious behavior at container runtime (anomalous syscalls, writes to `/etc/shadow`, connections to malicious IPs) using tools such as `Falco`, `Tetragon`, or eBPF-based EDR.",
            "A set of extra Kubernetes probes that restart the container when it shows abnormal CPU use; it depends on a custom `livenessProbe`, a shortcut that ignores exactly the scenario that matters most to avoid.",
        ],
        "correct_index": 2,
        "explanation": "Runtime security pega o que passou pelas defesas de build. A primeira descreve análise estática. A segunda descreve admission control. A quarta confunde com probe de saúde, que mede saúde, não segurança.",
        "explanation_en": "Runtime security catches what passed build defenses. The first describes static analysis. The second describes admission control. The fourth confuses it with a health probe, which measures health, not security.",
    },
    {
        "category": "DevSecOps",
        "statement": "O que é *policy as code*?",
        "statement_en": "What is *policy as code*?",
        "choices": [
            "Definir regras de segurança/compliance em código (ex.: OPA/Rego, Kyverno, Sentinel), versionado em Git e aplicado automaticamente em pipelines, admission controllers ou nos providers do Terraform.",
            "Sinônimo de IaC: tudo que é declarado em código (Terraform, manifests) já é policy by definition; não há ferramenta dedicada, prática que passa despercebida até uma auditoria de segurança.",
            "Conjunto de regras de SAST personalizadas por organização para detectar más práticas de código; `Rego` é só uma DSL de SAST, abordagem que ignora o histórico de incidentes parecidos no setor.",
            "Documento Wiki com convenções de segurança que o time deve seguir manualmente; ainda não existe ferramenta capaz de aplicar isso automaticamente, algo que passa no code review quando ninguém olha com atenção.",
        ],
        "choices_en": [
            "Define security/compliance rules in code (e.g. OPA/Rego, Kyverno, Sentinel), versioned in Git and applied automatically in pipelines, admission controllers, or Terraform providers.",
            "A synonym for IaC: everything declared in code (Terraform, manifests) is already policy by definition; there is no dedicated tool, a practice that goes unnoticed until a security audit.",
            "A set of organization-specific SAST rules to detect poor coding practices; `Rego` is just a SAST DSL, an approach that ignores the history of similar incidents in the industry.",
            "A Wiki document with security conventions the team must follow manually; there is still no tool capable of applying this automatically, something that slips through code review when nobody looks carefully.",
        ],
        "correct_index": 0,
        "explanation": "Policy as code = OPA/Kyverno/Sentinel, aplicado em pipeline ou admission. A segunda dilui o conceito (IaC e PaC se tocam, mas não são iguais). A terceira é falsa: Rego é uma linguagem de policy, não SAST. A quarta nega a existência de PaC.",
        "explanation_en": "Policy as code = OPA/Kyverno/Sentinel, applied in pipeline or admission. The second dilutes the concept (IaC and PaC touch, but are not the same). The third is false: Rego is a policy language, not SAST. The fourth denies PaC exists.",
    },
    {
        "category": "DevSecOps",
        "statement": "Qual a função de um Vault/Secret Manager em runtime?",
        "statement_en": "What is the role of a Vault/Secret Manager at runtime?",
        "choices": [
            "Servir como armazenamento criptografado de imagens Docker; equivale a um private registry com integração ao IAM da cloud, decisão que cria dívida técnica silenciosa, sem gerar erro imediato, suposição incorreta sobre como o sistema realmente se comporta sob estresse.",
            "Distribuir credenciais com TTL curto, gerar dinamicamente quando possível (ex.: senhas Postgres on-demand), suportar leasing/renew/revoke e auditar todo acesso; combina identidade (AppRole, OIDC, K8s ServiceAccount) com segredos.",
            "Substituir o KMS da cloud para criptografar volumes e snapshots; não há gestão de credenciais, só chaves criptográficas estáticas, atalho que troca segurança por conveniência de curto prazo, erro típico de configuração feita às pressas, sem revisão posterior.",
            "Banco encriptado de senhas estáticas com auditoria; rotação ainda é responsabilidade dos times de aplicação, abordagem que ignora o histórico de incidentes parecidos no setor, prática ainda comum em sistema legado que raramente é atualizado.",
        ],
        "choices_en": [
            "Serve as encrypted storage for Docker images; it equals a private registry with cloud IAM integration, a decision that creates silent technical debt without an immediate error, an incorrect assumption about how the system really behaves under stress.",
            "Distribute credentials with short TTL, generate them dynamically when possible (e.g. on-demand Postgres passwords), support leasing/renew/revoke, and audit every access; it combines identity (AppRole, OIDC, K8s ServiceAccount) with secrets.",
            "Replace the cloud KMS for encrypting volumes and snapshots; there is no credential management, only static cryptographic keys, a shortcut that trades security for short-term convenience, a typical error from rushed configuration without later review.",
            "An encrypted store of static passwords with audit; rotation remains the application teams' responsibility, an approach that ignores the history of similar incidents in the industry, a practice still common in legacy systems that are rarely updated.",
        ],
        "correct_index": 1,
        "explanation": "Vault = identidade + segredos dinâmicos + audit. A primeira confunde com registry. A terceira confunde com KMS. A quarta subestima: o ponto-chave é dinâmico/leasing, não senha estática com audit.",
        "explanation_en": "Vault = identity plus dynamic secrets plus audit. The first confuses it with a registry. The third confuses it with KMS. The fourth understates: the key point is dynamic/leasing, not a static password with audit.",
    },
    {
        "category": "DevSecOps",
        "statement": "Como tratar um secret descoberto em commit no histórico do Git?",
        "statement_en": "How do you handle a secret discovered in a Git commit history?",
        "choices": [
            "Só apagar o arquivo no commit seguinte; remoções superficiais bastam, pois o histórico fica protegido por TLS, suposição que ignora como o recurso realmente se comporta em escala, atalho que parece seguro isolado, mas quebra quando combinado com outros sistemas.",
            "Considerar a credencial comprometida desde o instante do push: revogar/rotacionar imediatamente, investigar logs de uso, remover do histórico (`git filter-repo`) somente como medida secundária e adicionar detecção no pre-commit/CI.",
            "Reescrever o histórico com `git rebase -i` é suficiente para apagar o segredo; rotação só é necessária se o repo for público, decisão que funciona no papel, mas não sobrevive ao primeiro incidente real, abordagem que ignora o histórico de incidentes parecidos no setor.",
            "Adicionar o caminho ao `.gitignore` e mudar o repositório para privado; segredos só são perigosos em repositórios públicos, abordagem que ignora o histórico de incidentes parecidos no setor, atalho comum quando o prazo aperta e ninguém revisa depois.",
        ],
        "choices_en": [
            "Just delete the file in the next commit; superficial removals are enough because history is protected by TLS, an assumption that ignores how the resource actually behaves at scale, a shortcut that looks safe in isolation but breaks when combined with other systems.",
            "Treat the credential as compromised from the moment of the push: revoke/rotate immediately, investigate usage logs, remove from history (`git filter-repo`) only as a secondary measure, and add detection in pre-commit/CI.",
            "Rewriting history with `git rebase -i` is enough to erase the secret; rotation is only needed if the repo is public, a decision that works on paper but does not survive the first real incident, an approach that ignores the history of similar incidents in the industry.",
            "Add the path to `.gitignore` and make the repository private; secrets are only dangerous in public repositories, an approach that ignores the history of similar incidents in the industry, a common shortcut when the deadline is tight and nobody reviews later.",
        ],
        "correct_index": 1,
        "explanation": "Rotacionar primeiro, limpar histórico depois. A primeira ignora que o histórico fica acessível por commits passados. A terceira é falsa: rotação é sempre necessária. A quarta também: até em repos privados o segredo pode ter vazado para clones, CI logs, indexadores etc.",
        "explanation_en": "Rotate first, clean history later. The first ignores that history remains accessible via past commits. The third is false: rotation is always necessary. The fourth also: even in private repos the secret may have leaked to clones, CI logs, indexers, etc.",
    },
    {
        "category": "DevSecOps",
        "statement": "Como impedir que imagens não assinadas rodem em um cluster Kubernetes?",
        "statement_en": "How do you prevent unsigned images from running in a Kubernetes cluster?",
        "choices": [
            "Configurar admission controller (`Kyverno`, `Connaisseur`, `Sigstore Policy Controller`) para validar a assinatura `cosign` da imagem contra a chave pública/identidade autorizada e rejeitar pods que não passem na verificação.",
            "Aplicar `SecurityContextConstraints` (OpenShift) que exige `runAsNonRoot`; imagens não assinadas são bloqueadas como efeito colateral, algo que passa no code review quando ninguém olha com atenção, erro que só é percebido quando o time de operação já está lidando com o incidente.",
            "Habilitar `PodSecurityPolicy` no API server e configurar a flag `--require-signature=true`; é o método nativo do K8s para enforcement, prática que passa despercebida até uma auditoria de segurança, prática que troca previsibilidade por economia de esforço imediato.",
            "Configurar `imagePullPolicy: Always` no Deployment; isso força o kubelet a verificar a integridade da imagem a cada pull, comportamento que confunde quem está debugando meses depois, suposição que vale só até o primeiro imprevisto de rede ou hardware.",
        ],
        "choices_en": [
            "Configure an admission controller (`Kyverno`, `Connaisseur`, `Sigstore Policy Controller`) to validate the image's `cosign` signature against the authorized public key/identity and reject pods that fail verification.",
            "Apply `SecurityContextConstraints` (OpenShift) requiring `runAsNonRoot`; unsigned images are blocked as a side effect, something that slips through code review when nobody looks carefully, a mistake usually noticed only when ops is already handling the incident.",
            "Enable `PodSecurityPolicy` on the API server and set the `--require-signature=true` flag; it is the native K8s enforcement method, a practice that goes unnoticed until a security audit, a practice that trades predictability for immediate effort savings.",
            "Set `imagePullPolicy: Always` on the Deployment; that forces the kubelet to verify image integrity on every pull, behavior that confuses whoever debugs months later, an assumption that holds only until the first network or hardware surprise.",
        ],
        "correct_index": 0,
        "explanation": "Admission controller que valida assinatura. A segunda confunde com SCC, que controla privilégios, não assinatura. A terceira é dupla armadilha: PSP foi removido em K8s 1.25 (substituído por Pod Security Admission), e a flag `--require-signature=true` não existe. A quarta confunde 'pull sempre' com verificação de integridade.",
        "explanation_en": "An admission controller that validates the signature. The second confuses it with SCC, which controls privileges, not signature. The third is a double trap: PSP was removed in K8s 1.25 (replaced by Pod Security Admission), and the `--require-signature=true` flag does not exist. The fourth confuses 'always pull' with integrity verification.",
    },
    # ── Performance / Troubleshooting (10) ─────────────────────────────────
    {
        "category": "Performance",
        "statement": "Como investigar alto uso de CPU em um host Linux?",
        "statement_en": "How do you investigate high CPU use on a Linux host?",
        "choices": [
            "Reiniciar o host em janela de manutenção e observar se a CPU volta ao normal; se voltar, marcar como flapping de hardware, decisão que cria dívida técnica silenciosa, sem gerar erro imediato, atalho comum quando o prazo aperta e ninguém revisa depois.",
            "Caminho macro→micro: começar por `top`/`htop` para identificar processo, depois `pidstat -t -p <pid>` para threads, `perf top`/`perf record` para hot path e profiler de aplicação para localizar a função quente.",
            "Apagar logs antigos em `/var/log` para liberar IO e reduzir indiretamente a CPU consumida pelo journald, prática que funciona em teste, mas falha sob carga real de produção, abordagem que resolve o sintoma, não a causa raiz do problema.",
            "Usar só `vmstat 1` para monitorar context switches; profilers só são confiáveis com `frame pointers` desativados, prática que passa despercebida até uma auditoria de segurança, prática ainda comum em sistema legado que raramente é atualizado.",
        ],
        "choices_en": [
            "Reboot the host in a maintenance window and see if CPU returns to normal; if it does, mark it as hardware flapping, a decision that creates silent technical debt without an immediate error, a common shortcut when the deadline is tight and nobody reviews later.",
            "Macro→micro path: start with `top`/`htop` to identify the process, then `pidstat -t -p <pid>` for threads, `perf top`/`perf record` for the hot path, and an application profiler to locate the hot function.",
            "Delete old logs under `/var/log` to free IO and indirectly reduce CPU consumed by journald, a practice that works in tests but fails under real production load, an approach that treats the symptom rather than the root cause.",
            "Use only `vmstat 1` to monitor context switches; profilers are only reliable with `frame pointers` disabled, a practice that goes unnoticed until a security audit, a practice still common in legacy systems that are rarely updated.",
        ],
        "correct_index": 1,
        "explanation": "Macro (host) → micro (função quente). A primeira mata a evidência sem diagnosticar. A terceira ataca um sintoma improvável (logs não costumam dominar CPU). A quarta inverte a recomendação: profiling moderno (`perf`, eBPF) precisa de `frame pointers` *habilitados*.",
        "explanation_en": "Macro (host) → micro (hot function). The first destroys evidence without diagnosing. The third attacks an unlikely symptom (logs rarely dominate CPU). The fourth inverts the recommendation: modern profiling (`perf`, eBPF) needs `frame pointers` *enabled*.",
    },
    {
        "category": "Performance",
        "statement": "Como um *memory leak* tipicamente se manifesta em produção?",
        "statement_en": "How does a *memory leak* typically show up in production?",
        "choices": [
            "Picos periódicos de uso de memória que coincidem com a coleta de lixo; é só o GC fazendo sua função e não exige investigação, suposição que ignora como o recurso realmente se comporta em escala, suposição que só vale em ambiente de desenvolvimento, não em produção.",
            "Latência menor após algumas horas de uso, pois o cache aquecido reduz alocações; o consumo sobe e estabiliza naturalmente, prática que só aparece como erro grave durante um incidente real, decisão que ignora justamente o motivo pelo qual a prática recomendada existe.",
            "Crescimento monotônico de RSS/heap ao longo do tempo (sem retornar ao patamar baseline entre requisições) culminando em OOM kill; investiga-se com heap profiler (`pprof`, `heaptrack`, `jmap`) e snapshots em momentos diferentes.",
            "Aumento do uso de swap sem aumento de RSS; o kernel está realocando páginas frias por pressão de memória, não há vazamento real, prática que troca previsibilidade por economia de esforço imediato, suposição que vale só até o primeiro imprevisto de rede ou hardware.",
        ],
        "choices_en": [
            "Periodic memory-use spikes that coincide with garbage collection; it is only the GC doing its job and needs no investigation, an assumption that ignores how the resource actually behaves at scale, an assumption that only holds in development, not production.",
            "Lower latency after a few hours of use, because a warm cache reduces allocations; consumption rises and naturally stabilizes, a practice that only shows up as a serious error during a real incident, a decision that ignores exactly why the recommended practice exists.",
            "Monotonic RSS/heap growth over time (without returning to the baseline between requests) culminating in an OOM kill; investigate with a heap profiler (`pprof`, `heaptrack`, `jmap`) and snapshots at different moments.",
            "Increased swap use without increased RSS; the kernel is reclaiming cold pages under memory pressure, there is no real leak, a practice that trades predictability for immediate effort savings, an assumption that holds only until the first network or hardware surprise.",
        ],
        "correct_index": 2,
        "explanation": "Leak = crescimento monotônico + OOM. A primeira normaliza picos como GC normal (que é o oposto de leak). A segunda confunde leak com cache warmup. A quarta descreve swap por pressão (sintoma diferente, sem leak).",
        "explanation_en": "Leak = monotonic growth plus OOM. The first normalizes spikes as normal GC (the opposite of a leak). The second confuses leak with cache warmup. The fourth describes swap under pressure (a different symptom, without a leak).",
    },
    {
        "category": "Performance",
        "statement": "Como diagnosticar problemas de rede entre dois Pods em um cluster Kubernetes?",
        "statement_en": "How do you diagnose network problems between two Pods in a Kubernetes cluster?",
        "choices": [
            "Remover grande parte da `NetworkPolicy` do namespace temporariamente para confirmar se o problema é de policy; reativar depois, suposição que só se sustenta enquanto o time é pequeno, atalho que ignora exatamente o cenário que mais importa evitar.",
            "Validar em camadas: resolução DNS (`nslookup` no `kube-dns`/`CoreDNS`), regras de `NetworkPolicy`, MTU/PMTU, conectividade L4 (`tcpdump` no nó/sidecar, `ngrep`), métricas do CNI (Calico/Cilium) e logs da aplicação.",
            "Trocar o CNI por outro (Calico→Flannel) e ver se o problema some; se some, é bug do CNI anterior, e o switch é a fix definitiva, atalho que funciona hoje mas complica a próxima migração, erro típico de configuração feita às pressas, sem revisão posterior.",
            "Reiniciar `coredns` e o `kube-proxy`; problemas intermitentes de rede em K8s são quase geralmente causados por cache stale do DNS, atalho que ignora exatamente o cenário que mais importa evitar, comportamento que só vira prioridade depois que já causou prejuízo.",
        ],
        "choices_en": [
            "Temporarily remove most of the namespace `NetworkPolicy` to confirm whether the problem is policy; re-enable afterward, an assumption that only holds while the team is small, a shortcut that ignores exactly the scenario that matters most to avoid.",
            "Validate in layers: DNS resolution (`nslookup` against `kube-dns`/`CoreDNS`), `NetworkPolicy` rules, MTU/PMTU, L4 connectivity (`tcpdump` on the node/sidecar, `ngrep`), CNI metrics (Calico/Cilium), and application logs.",
            "Switch the CNI to another (Calico→Flannel) and see if the problem goes away; if it does, it is a bug in the previous CNI, and the switch is the definitive fix, a shortcut that works today but complicates the next migration, a typical error from rushed configuration without later review.",
            "Restart `coredns` and `kube-proxy`; intermittent network problems in K8s are almost usually caused by stale DNS cache, a shortcut that ignores exactly the scenario that matters most to avoid, behavior that only becomes a priority after it has already caused damage.",
        ],
        "correct_index": 1,
        "explanation": "Diagnose por camadas: DNS → policy → MTU → CNI → app. A primeira é workaround risky em produção (e diagnostica só uma hipótese). A terceira é mudança radical sem evidência. A quarta generaliza erradamente, DNS é uma causa frequente, não a única.",
        "explanation_en": "Layered diagnosis: DNS → policy → MTU → CNI → app. The first is a risky production workaround (and tests only one hypothesis). The third is a radical change without evidence. The fourth generalizes wrongly; DNS is a frequent cause, not the only one.",
    },
    {
        "category": "Performance",
        "statement": "O que é CPU *throttling* em containers?",
        "statement_en": "What is CPU *throttling* in containers?",
        "choices": [
            "Aceleração natural do kernel quando o container está sub-utilizado; ele migra threads para cores mais rápidos para reduzir latência, escolha que economiza tempo agora e cobra o preço mais tarde.",
            "Limitação imposta pelo kernel via cgroups (CFS) quando o container ultrapassa o `limits.cpu`; manifesta-se como latência irregular, com a métrica `container_cpu_cfs_throttled_seconds_total` indicando o tempo retido.",
            "Encerramento do container quando o uso de memória excede o `limits.memory`; o kernel envia `SIGKILL` e o kubelet reescala o pod em outro nó, suposição incorreta sobre como o sistema realmente se comporta sob estresse.",
            "Remoção do pod pelo kubelet quando o nó está sob pressão de recurso; o pod é reagendado em outro nó com capacidade ociosa, atalho que ignora exatamente o cenário que mais importa evitar.",
        ],
        "choices_en": [
            "Natural kernel acceleration when the container is under-utilized; it migrates threads to faster cores to reduce latency, a choice that saves time now and collects the cost later.",
            "A limit imposed by the kernel via cgroups (CFS) when the container exceeds `limits.cpu`; it shows up as irregular latency, with the metric `container_cpu_cfs_throttled_seconds_total` indicating retained time.",
            "Termination of the container when memory use exceeds `limits.memory`; the kernel sends `SIGKILL` and the kubelet reschedules the pod on another node, an incorrect assumption about how the system really behaves under stress.",
            "Removal of the pod by the kubelet when the node is under resource pressure; the pod is rescheduled on another node with spare capacity, a shortcut that ignores exactly the scenario that matters most to avoid.",
        ],
        "correct_index": 1,
        "explanation": "Throttling = freada de CPU por cgroup CFS quando passa do limit. A primeira inventa 'aceleração'. A terceira descreve OOMKill (memória, não CPU). A quarta descreve eviction. Os três fenômenos existem, mas só o segundo é throttling.",
        "explanation_en": "Throttling = CPU braking by cgroup CFS when past the limit. The first invents 'acceleration'. The third describes OOMKill (memory, not CPU). The fourth describes eviction. All three phenomena exist, but only the second is throttling.",
    },
    {
        "category": "Performance",
        "statement": "Como acelerar uma API que sofre com problema de N+1 queries?",
        "statement_en": "How do you speed up an API suffering from N+1 query problems?",
        "choices": [
            "Aumentar o número de réplicas da aplicação e a `pool size` do banco; mais conexões reduzem o tempo médio por query, prática que gera falso senso de segurança no time, decisão que ignora justamente o motivo pelo qual a prática recomendada existe.",
            "Refatorar o acesso a dados para uma única query (`JOIN`/`IN`) ou usar eager loading do ORM (`select_related`/`prefetch_related` no Django, `joinedload` no SQLAlchemy); o objetivo é cortar o número de roundtrips ao banco.",
            "Adicionar Redis cache em cada chamada do ORM; mesmo que o N+1 continue, as queries repetidas serão atendidas de memória, prática que troca previsibilidade por economia de esforço imediato, comportamento que só vira prioridade depois que já causou prejuízo.",
            "Habilitar `connection multiplexing` (pgbouncer) em modo `statement`; com isso o N+1 deixa de ser problema porque o pool reusa o socket, decisão que cria dívida técnica silenciosa, sem gerar erro imediato, comportamento que confunde quem está debugando meses depois.",
        ],
        "choices_en": [
            "Increase application replicas and the database `pool size`; more connections reduce average time per query, a practice that gives the team a false sense of safety, a decision that ignores exactly why the recommended practice exists.",
            "Refactor data access into a single query (`JOIN`/`IN`) or use ORM eager loading (`select_related`/`prefetch_related` in Django, `joinedload` in SQLAlchemy); the goal is to cut the number of database roundtrips.",
            "Add a Redis cache on every ORM call; even if N+1 continues, repeated queries will be served from memory, a practice that trades predictability for immediate effort savings, behavior that only becomes a priority after it has already caused damage.",
            "Enable `connection multiplexing` (pgbouncer) in `statement` mode; with that, N+1 stops being a problem because the pool reuses the socket, a decision that creates silent technical debt without an immediate error, behavior that confuses whoever debugs months later.",
        ],
        "correct_index": 1,
        "explanation": "N+1 é problema lógico, corte os roundtrips. A primeira escala custo sem resolver. A terceira mascara com cache, mas mantém o roundtrip em miss. A quarta confunde pool de conexão com plan de query, pgbouncer não diminui o número de queries enviadas.",
        "explanation_en": "N+1 is a logical problem — cut the roundtrips. The first scales cost without solving it. The third masks with cache but keeps the roundtrip on miss. The fourth confuses connection pooling with query plan; pgbouncer does not reduce the number of queries sent.",
    },
    {
        "category": "Performance",
        "statement": "Como identificar queries lentas em PostgreSQL em produção?",
        "statement_en": "How do you identify slow queries in PostgreSQL in production?",
        "choices": [
            "Habilitar `log_min_duration_statement`, instalar `pg_stat_statements` para o top-N de queries por tempo total e usar `EXPLAIN (ANALYZE, BUFFERS)` para entender plano de execução, índices e IO real.",
            "Executar `REINDEX DATABASE` semanal e `VACUUM FULL` mensal; queries lentas em Postgres são quase geralmente causadas por índices fragmentados, que só aparece como problema depois que o sistema já está em produção.",
            "Aumentar `autovacuum_vacuum_cost_limit` e `maintenance_work_mem`; o autovacuum agressivo já elimina a maior parte das queries lentas, escolha que economiza tempo agora e cobra o preço mais tarde.",
            "Trocar o disco por NVMe e reiniciar o serviço; queries lentas em sistemas modernos são limitadas por throughput de IO, atalho que parece seguro isolado, mas quebra quando combinado com outros sistemas.",
        ],
        "choices_en": [
            "Enable `log_min_duration_statement`, install `pg_stat_statements` for the top-N queries by total time, and use `EXPLAIN (ANALYZE, BUFFERS)` to understand the execution plan, indexes, and real IO.",
            "Run `REINDEX DATABASE` weekly and `VACUUM FULL` monthly; slow queries in Postgres are almost usually caused by fragmented indexes, which only shows up as a problem after the system is already in production.",
            "Increase `autovacuum_vacuum_cost_limit` and `maintenance_work_mem`; aggressive autovacuum already eliminates most slow queries, a choice that saves time now and collects the cost later.",
            "Replace the disk with NVMe and restart the service; slow queries in modern systems are limited by IO throughput, a shortcut that looks safe in isolation but breaks when combined with other systems.",
        ],
        "correct_index": 0,
        "explanation": "log_min_duration + pg_stat_statements + EXPLAIN ANALYZE. A segunda generaliza erradamente (REINDEX/VACUUM FULL bloqueia e raramente é a causa). A terceira é meia-verdade: autovacuum ajuda, mas não substitui análise de plano. A quarta é diagnóstico cego sem medir.",
        "explanation_en": "log_min_duration plus pg_stat_statements plus EXPLAIN ANALYZE. The second generalizes wrongly (REINDEX/VACUUM FULL blocks and is rarely the cause). The third is a half-truth: autovacuum helps, but does not replace plan analysis. The fourth is blind diagnosis without measuring.",
    },
    {
        "category": "Performance",
        "statement": "Em microsserviços com latência alta intermitente (cauda), qual o suspeito mais comum e como investigar?",
        "statement_en": "In microservices with intermittent high latency (tail), what is the most common suspect and how do you investigate?",
        "choices": [
            "Bug no JavaScript do frontend que executa queries adicionais só em determinados browsers; basta atualizar a SPA, suposição que ignora como o recurso realmente se comporta em escala, comportamento que gera alerta falso ou silencia alerta real, dependendo do caso.",
            "Investigar com tracing distribuído (`OpenTelemetry`, Jaeger, Tempo) cruzado com métricas em percentis altos; causas frequentes são pausas de GC, contention em locks, fila de DB cheia, reescalonamento de pod e jitter de rede.",
            "Instabilidade do hypervisor; a única forma de mitigar é trocar a região do provedor cloud, comportamento que só é notado quando alguém audita os logs depois, comportamento que só some quando alguém finalmente lê a documentação.",
            "Erro no parsing do YAML do Deployment que aumenta o tempo de scheduling em situações específicas; revise os manifests, prática que só aparece como erro grave durante um incidente real, abordagem que ignora o histórico de incidentes parecidos no setor.",
        ],
        "choices_en": [
            "A frontend JavaScript bug that runs extra queries only in certain browsers; just update the SPA, an assumption that ignores how the resource actually behaves at scale, behavior that creates false alerts or silences real ones, depending on the case.",
            "Investigate with distributed tracing (`OpenTelemetry`, Jaeger, Tempo) crossed with high-percentile metrics; frequent causes are GC pauses, lock contention, full DB queues, pod rescheduling, and network jitter.",
            "Hypervisor instability; the only mitigation is switching the cloud provider region, behavior noticed only when someone audits the logs later, behavior that only goes away when someone finally reads the docs.",
            "A YAML parsing error in the Deployment that increases scheduling time in specific situations; review the manifests, a practice that only shows up as a serious error during a real incident, an approach that ignores the history of similar incidents in the industry.",
        ],
        "correct_index": 1,
        "explanation": "Tail latency = tracing + percentis + suspeitos clássicos (GC, lock, fila, rede). A primeira diagnostica frontend, mas o problema é tipicamente backend. A terceira culpa hypervisor sem evidência. A quarta inventa um custo de YAML scheduling que não existe.",
        "explanation_en": "Tail latency = tracing plus percentiles plus classic suspects (GC, lock, queue, network). The first diagnoses frontend, but the problem is typically backend. The third blames the hypervisor without evidence. The fourth invents a YAML scheduling cost that does not exist.",
    },
    {
        "category": "Performance",
        "statement": "Como reduzir latência percebida por usuários geograficamente distribuídos?",
        "statement_en": "How do you reduce latency perceived by geographically distributed users?",
        "choices": [
            "Concentrar vários usuários em uma única região para reduzir custo de replicação; CDN só ajuda se o conteúdo for estático, suposição que só se sustenta enquanto o time é pequeno, suposição que só vale em ambiente de desenvolvimento, não em produção.",
            "Combinar CDN para conteúdo estático (cache em PoPs próximos do usuário) e estratégia multi-região (Anycast, edge compute, replicação ativa-ativa) para conteúdo dinâmico crítico, com roteamento DNS por geolocalização.",
            "Servir o site só via HTTP (sem TLS) para eliminar handshake; TLS adiciona ~100ms a cada conexão e domina o RTT geográfico, que só aparece como problema depois que o sistema já está em produção, resultado típico de copiar configuração de outro projeto sem adaptar.",
            "Aumentar o tamanho do `tcp_init_cwnd` no kernel reduz a latência percebida; CDN é só um cache de imagem, não muda o RTT, algo que passa no code review quando ninguém olha com atenção, decisão que parece segura até o primeiro teste de penetração real.",
        ],
        "choices_en": [
            "Concentrate many users in a single region to reduce replication cost; CDN only helps if content is static, an assumption that only holds while the team is small, an assumption that only holds in development, not production.",
            "Combine CDN for static content (cache at PoPs near the user) and a multi-region strategy (Anycast, edge compute, active-active replication) for critical dynamic content, with DNS routing by geolocation.",
            "Serve the site only over HTTP (no TLS) to eliminate the handshake; TLS adds ~100ms per connection and dominates geographic RTT, which only shows up as a problem after the system is already in production, a typical result of copying config from another project without adapting it.",
            "Increasing `tcp_init_cwnd` in the kernel reduces perceived latency; CDN is only an image cache and does not change RTT, something that slips through code review when nobody looks carefully, a decision that looks safe until the first real penetration test.",
        ],
        "correct_index": 1,
        "explanation": "CDN + multi-região + DNS geo. A primeira descarta o problema. A terceira sacrifica segurança (e nem é o que mais ajuda; CDN reaproveita conexões TLS via session resumption). A quarta minimiza CDN, que é literalmente a forma mais efetiva.",
        "explanation_en": "CDN plus multi-region plus geo DNS. The first dismisses the problem. The third sacrifices security (and is not what helps most; CDN reuses TLS connections via session resumption). The fourth minimizes CDN, which is literally the most effective approach.",
    },
    {
        "category": "Performance",
        "statement": "Qual padrão de cache é mais comum em APIs read-heavy?",
        "statement_en": "Which cache pattern is most common in read-heavy APIs?",
        "choices": [
            "O cenário `write-through` sincroniza cada escrita em cache e backend e promete leituras sem miss — útil em outros casos, mas não é o padrão dominante em read-heavy, suposição que vale só até o primeiro imprevisto de rede ou hardware.",
            "Em APIs read-heavy predomina o `cache-aside` (lazy loading): a aplicação lê o cache; em miss, busca no DB e popula o cache, combinando TTL e invalidação com simplicidade e tolerância a falhas do cache.",
            "Com `write-around` as escritas ignoram o cache e vão direto ao DB — reduz poluição do cache, mas é antitético ao objetivo de read-heavy, prática que aumenta a superfície de ataque sem ninguém perceber.",
            "O `refresh-ahead` renovaria entradas antes do TTL e substituiria o `cache-aside` na maioria dos sistemas — existe, mas não é o mais comum como padrão único, atalho que ignora exatamente o cenário que mais importa evitar.",
        ],
        "choices_en": [
            "The `write-through` scenario syncs every write to cache and backend and promises miss-free reads — useful in other cases, but not the dominant pattern for read-heavy, an assumption that holds only until the first network or hardware surprise.",
            "In read-heavy APIs, `cache-aside` (lazy loading) predominates: the application reads the cache; on miss, it fetches from the DB and populates the cache, combining TTL and invalidation with simplicity and tolerance to cache failure.",
            "With `write-around`, writes skip the cache and go straight to the DB — it reduces cache pollution, but is antithetical to the read-heavy goal, a practice that grows the attack surface without anyone noticing.",
            "`refresh-ahead` would renew entries before TTL and replace `cache-aside` in most systems — it exists, but is not the most common as a single pattern, a shortcut that ignores exactly the scenario that matters most to avoid.",
        ],
        "correct_index": 1,
        "explanation": "Cache-aside é o pattern dominante em read-heavy. A primeira (`write-through`) é usado em outros cenários e não 'nunca tem miss' (cache pode ser ejetado). A terceira (`write-around`) é antitético a read-heavy. A quarta (`refresh-ahead`) existe, mas não substitui cache-aside na maioria dos sistemas.",
        "explanation_en": "Cache-aside is the dominant pattern for read-heavy. The first (`write-through`) is used in other scenarios and does not 'never miss' (cache can be evicted). The third (`write-around`) is antithetical to read-heavy. The fourth (`refresh-ahead`) exists, but does not replace cache-aside in most systems.",
    },
    {
        "category": "Performance",
        "statement": "Como investigar problemas de disco lento em um host Linux?",
        "statement_en": "How do you investigate slow disk problems on a Linux host?",
        "choices": [
            "Trocar o SSD por NVMe sem medir; em workloads de produção a latência de IO é dominada pelo controlador, não pelo media subjacente, prática que só aparece como erro grave durante um incidente real, atalho que parece seguro isolado, mas quebra quando combinado com outros sistemas.",
            "Reiniciar o host e observar; a maioria dos problemas de IO em produção são resolvidos com `echo 3 > /proc/sys/vm/drop_caches`, suposição que vale só até o primeiro imprevisto de rede ou hardware, que só aparece como problema depois que o sistema já está em produção.",
            "Combinar métricas (`iostat -x 1` para `await`, `%util`, throughput; `iotop`/`pidstat -d` para processos) e benchmark (`fio`) para isolar latência por dispositivo, identificar saturação e separar leitura/escrita.",
            "Esvaziar `/tmp` e `/var/tmp` para reduzir uso de inodes; o problema costuma ser falta de inodes, prática ainda comum em sistema legado que raramente é atualizado, erro comum de quem aprendeu por tentativa e erro, sem revisar a documentação oficial.",
        ],
        "choices_en": [
            "Replace the SSD with NVMe without measuring; in production workloads IO latency is dominated by the controller, not the underlying media, a practice that only shows up as a serious error during a real incident, a shortcut that looks safe in isolation but breaks when combined with other systems.",
            "Reboot the host and observe; most production IO problems are solved with `echo 3 > /proc/sys/vm/drop_caches`, an assumption that holds only until the first network or hardware surprise, which only shows up as a problem after the system is already in production.",
            "Combine metrics (`iostat -x 1` for `await`, `%util`, throughput; `iotop`/`pidstat -d` for processes) and benchmarks (`fio`) to isolate per-device latency, identify saturation, and separate reads/writes.",
            "Empty `/tmp` and `/var/tmp` to reduce inode use; the problem is usually lack of inodes, a practice still common in legacy systems that are rarely updated, a common mistake from learning by trial and error without reading official docs.",
        ],
        "correct_index": 2,
        "explanation": "iostat + iotop + fio = diagnóstico apropriado. A primeira é cara e cega (e o que ela afirma é falso). A segunda confunde IO com cache do kernel. A quarta confunde IO lento com falta de inodes (que dá outro erro: `No space left on device`).",
        "explanation_en": "iostat plus iotop plus fio = appropriate diagnosis. The first is expensive and blind (and what it claims is false). The second confuses IO with kernel cache. The fourth confuses slow IO with lack of inodes (which gives a different error: `No space left on device`).",
    },
    # ── Networking avançado (8) ────────────────────────────────────────────
    {
        "category": "Networking",
        "statement": "O que é mTLS?",
        "statement_en": "What is mTLS?",
        "choices": [
            "TLS unilateral em que só o servidor apresenta certificado; o cliente é autenticado por token ou senha, sem mTLS no handshake, comportamento que confunde quem está debugando meses depois, decisão que ignora justamente o motivo pelo qual a prática recomendada existe.",
            "Mutual TLS: tanto cliente quanto servidor apresentam certificados X.509 e validam um ao outro; usado em service mesh para zero-trust intra-cluster e em APIs B2B com autenticação forte sem senhas compartilhadas.",
            "Variante de TLS com chave pré-compartilhada (PSK) entre cliente e servidor, sem cadeia de certificados; usada em IoT com hardware limitado, atalho comum quando o prazo aperta e ninguém revisa depois, atalho comum quando o prazo aperta e ninguém revisa depois.",
            "Versão moderna do TLS 1.3 que reduz handshake para 1-RTT; o termo 'mTLS' é só marketing, sem mudança no protocolo, comportamento que só vira prioridade depois que já causou prejuízo, que só aparece como problema depois que o sistema já está em produção.",
        ],
        "choices_en": [
            "Unilateral TLS where only the server presents a certificate; the client is authenticated by token or password, with no mTLS in the handshake, behavior that confuses whoever debugs months later, a decision that ignores exactly why the recommended practice exists.",
            "Mutual TLS: both client and server present X.509 certificates and validate each other; used in service mesh for intra-cluster zero-trust and in B2B APIs with strong authentication without shared passwords.",
            "A TLS variant with a pre-shared key (PSK) between client and server, without a certificate chain; used in IoT with limited hardware, a common shortcut when the deadline is tight and nobody reviews later, a common shortcut when the deadline is tight and nobody reviews later.",
            "A modern version of TLS 1.3 that reduces the handshake to 1-RTT; the term 'mTLS' is only marketing, with no protocol change, behavior that only becomes a priority after it has already caused damage, which only shows up as a problem after the system is already in production.",
        ],
        "correct_index": 1,
        "explanation": "mTLS = ambos os lados apresentam cert. A primeira descreve TLS comum. A terceira descreve TLS-PSK (outro modo). A quarta confunde mTLS com TLS 1.3 0-RTT/1-RTT.",
        "explanation_en": "mTLS = both sides present a cert. The first describes ordinary TLS. The third describes TLS-PSK (another mode). The fourth confuses mTLS with TLS 1.3 0-RTT/1-RTT.",
    },
    {
        "category": "Networking",
        "statement": "O que é um *service mesh*?",
        "statement_en": "What is a *service mesh*?",
        "choices": [
            "Camada de infraestrutura que adiciona mTLS, observabilidade detalhada (latência, retries, traces), retry/circuit-breaking e roteamento avançado entre serviços via sidecars (`Envoy` em Istio/Consul) ou modo `ambient` (Linkerd, Istio Ambient).",
            "Proxy L7 na borda do cluster que termina TLS, autentica usuários e aplica rate limiting; opera entre o cliente externo e o conjunto de serviços, não entre serviços, algo que passa no code review quando ninguém olha com atenção, atalho que funciona hoje mas complica a próxima migração.",
            "Proxy reverso (`nginx`, `HAProxy`) que distribui tráfego entre backends; mTLS e tracing precisam ser configurados manualmente em cada upstream, decisão que funciona no papel, mas não sobrevive ao primeiro incidente real, suposição que ignora como o recurso realmente se comporta em escala.",
            "Camada de proxy de bancos de dados que adiciona connection pooling, masking de PII e roteamento de leituras para réplicas read-only, decisão que parece segura até o primeiro teste de penetração real, escolha que economiza tempo agora e cobra o preço mais tarde.",
        ],
        "choices_en": [
            "An infrastructure layer that adds mTLS, detailed observability (latency, retries, traces), retry/circuit-breaking, and advanced routing between services via sidecars (`Envoy` in Istio/Consul) or `ambient` mode (Linkerd, Istio Ambient).",
            "An L7 proxy at the cluster edge that terminates TLS, authenticates users, and applies rate limiting; it operates between the external client and the set of services, not between services, something that slips through code review when nobody looks carefully, a shortcut that works today but complicates the next migration.",
            "A reverse proxy (`nginx`, `HAProxy`) that distributes traffic across backends; mTLS and tracing must be configured manually on each upstream, a decision that works on paper but does not survive the first real incident, an assumption that ignores how the resource actually behaves at scale.",
            "A database proxy layer that adds connection pooling, PII masking, and routing of reads to read-only replicas, a decision that looks safe until the first real penetration test, a choice that saves time now and collects the cost later.",
        ],
        "correct_index": 0,
        "explanation": "Service mesh = preocupações cross-cutting tiradas do código. A segunda descreve API gateway (norte-sul). A terceira descreve reverse proxy genérico. A quarta descreve um *database proxy*, conceito diferente.",
        "explanation_en": "Service mesh = cross-cutting concerns pulled out of code. The second describes an API gateway (north-south). The third describes a generic reverse proxy. The fourth describes a *database proxy*, a different concept.",
    },
    {
        "category": "Networking",
        "statement": "Diferença entre forward proxy e reverse proxy:",
        "statement_en": "Difference between forward proxy and reverse proxy:",
        "choices": [
            "Forward proxy fica em frente aos backends recebendo tráfego de internet; reverse proxy fica em frente aos clientes para registrar acesso a sites externos, atalho que funciona hoje mas complica a próxima migração, atalho que parece seguro isolado, mas quebra quando combinado com outros sistemas.",
            "Forward proxy atua a favor do cliente (encaminha requisições saindo de uma rede para a internet, ex.: Squid em rede corporativa); reverse proxy atua a favor do servidor (recebe requisições externas e distribui para backends, ex.: nginx, HAProxy).",
            "Ambos atuam a favor do cliente, sendo a diferença só o protocolo: forward usa HTTP e reverse usa HTTPS, prática que troca previsibilidade por economia de esforço imediato, prática que só aparece como erro grave durante um incidente real.",
            "Reverse proxy é uma forma de VPN que cria túnel criptografado de saída; forward proxy é o servidor VPN central da rede, atalho que funciona hoje mas complica a próxima migração, suposição que raramente se sustenta fora do ambiente controlado de laboratório.",
        ],
        "choices_en": [
            "A forward proxy sits in front of backends receiving internet traffic; a reverse proxy sits in front of clients to log access to external sites, a shortcut that works today but complicates the next migration, a shortcut that looks safe in isolation but breaks when combined with other systems.",
            "A forward proxy acts on behalf of the client (forwards requests leaving a network to the internet, e.g. Squid on a corporate network); a reverse proxy acts on behalf of the server (receives external requests and distributes them to backends, e.g. nginx, HAProxy).",
            "Both act on behalf of the client; the only difference is the protocol: forward uses HTTP and reverse uses HTTPS, a practice that trades predictability for immediate effort savings, a practice that only shows up as a serious error during a real incident.",
            "A reverse proxy is a form of VPN that creates an encrypted outbound tunnel; a forward proxy is the network's central VPN server, a shortcut that works today but complicates the next migration, an assumption that rarely holds outside a controlled lab environment.",
        ],
        "correct_index": 1,
        "explanation": "Forward = cliente; Reverse = servidor. A primeira inverte. A terceira inventa diferença de protocolo (ambos suportam HTTP/HTTPS). A quarta confunde com VPN.",
        "explanation_en": "Forward = client; Reverse = server. The first swaps them. The third invents a protocol difference (both support HTTP/HTTPS). The fourth confuses this with VPN.",
    },
    {
        "category": "Networking",
        "statement": "Como aumentar resiliência de DNS em produção?",
        "statement_en": "How do you increase DNS resilience in production?",
        "choices": [
            "Manter um arquivo `/etc/hosts` consolidado em vários hosts da empresa; é a forma mais resiliente porque elimina dependência de servidor DNS, prática que troca previsibilidade por economia de esforço imediato, decisão que parece razoável isolada, mas quebra a arquitetura no conjunto.",
            "Usar TTL=0 em vários registros para que falhas sejam recuperadas instantaneamente; resolvers não fazem cache nesse modo, erro comum de quem aprendeu por tentativa e erro, sem revisar a documentação oficial, abordagem que resolve o sintoma, não a causa raiz do problema.",
            "Múltiplos resolvers redundantes em zonas/regiões diferentes, TTLs apropriados (curtos para failover rápido, longos para reduzir QPS), health checks com failover automático e fallback (resolver secundário) configurado nos clientes.",
            "Concentrar tudo em um único servidor DNS de altíssima disponibilidade (com SLA 99.99%); redundância gera divergência de cache, prática que só aparece como erro grave durante um incidente real, decisão que parece segura até o primeiro teste de penetração real.",
        ],
        "choices_en": [
            "Keep a consolidated `/etc/hosts` file on many company hosts; it is the most resilient approach because it eliminates DNS server dependency, a practice that trades predictability for immediate effort savings, a decision that seems reasonable in isolation but breaks the architecture overall.",
            "Use TTL=0 on many records so failures recover instantly; resolvers do not cache in that mode, a common mistake from learning by trial and error without reading official docs, an approach that treats the symptom rather than the root cause.",
            "Multiple redundant resolvers in different zones/regions, appropriate TTLs (short for fast failover, long to reduce QPS), health checks with automatic failover, and fallback (secondary resolver) configured on clients.",
            "Concentrate everything on a single ultra-high-availability DNS server (with 99.99% SLA); redundancy creates cache divergence, a practice that only shows up as a serious error during a real incident, a decision that looks safe until the first real penetration test.",
        ],
        "correct_index": 2,
        "explanation": "Múltiplos resolvers + TTLs equilibrados + health check. A primeira não escala (`/etc/hosts` em milhares de hosts é manutenção impossível). A segunda quebra performance (TTL=0 mata cache e estoura QPS). A quarta concentra SPOF.",
        "explanation_en": "Multiple resolvers plus balanced TTLs plus health check. The first does not scale (`/etc/hosts` on thousands of hosts is impossible maintenance). The second breaks performance (TTL=0 kills cache and blows QPS). The fourth concentrates SPOF.",
    },
    {
        "category": "Networking",
        "statement": "O que é MTU e por que importa?",
        "statement_en": "What is MTU and why does it matter?",
        "choices": [
            "Tamanho máximo do segmento TCP, geralmente igual ao MTU menos 20 bytes; MSS é só um sinônimo de MTU em camada 4, decisão que ignora justamente o motivo pelo qual a prática recomendada existe, suposição que só vale em ambiente de desenvolvimento, não em produção, abordagem que resolve o sintoma, não a causa raiz do problema.",
            "Maximum Transmission Unit: maior tamanho de payload que pode ser transmitido em um link sem fragmentação. Mismatch (ex.: túneis VPN/CNI que reduzem MTU efetivo) causa fragmentação ou black hole quando o flag `DF` está setado; mitigar com PMTUD ou ajustar `MSS clamp`.",
            "Configuração do switch que habilita jumbo frames (9000 bytes); fora desse contexto não influencia comunicação L3, abordagem que ignora o cenário de falha mais provável na prática, atalho comum quando o prazo aperta e ninguém revisa depois, suposição que ignora como o recurso realmente se comporta em escala.",
            "Métrica que define a largura de banda máxima de uma interface em Mbps; valor padrão é 1500 Mbps em redes Ethernet modernas, prática que só aparece como erro grave durante um incidente real, atalho que ignora exatamente o cenário que mais importa evitar, comportamento que só vira prioridade depois que já causou prejuízo.",
        ],
        "choices_en": [
            "Maximum TCP segment size, usually equal to MTU minus 20 bytes; MSS is just a synonym for MTU at layer 4, a decision that ignores exactly why the recommended practice exists, an assumption that only holds in development, not production, an approach that treats the symptom rather than the root cause.",
            "Maximum Transmission Unit: the largest payload size that can be transmitted on a link without fragmentation. Mismatch (e.g. VPN/CNI tunnels that reduce effective MTU) causes fragmentation or black hole when the `DF` flag is set; mitigate with PMTUD or adjust `MSS clamp`.",
            "A switch setting that enables jumbo frames (9000 bytes); outside that context it does not influence L3 communication, an approach that ignores the most likely failure scenario in practice, a common shortcut when the deadline is tight and nobody reviews later, an assumption that ignores how the resource actually behaves at scale.",
            "A metric that defines the maximum bandwidth of an interface in Mbps; the default is 1500 Mbps on modern Ethernet networks, a practice that only shows up as a serious error during a real incident, a shortcut that ignores exactly the scenario that matters most to avoid, behavior that only becomes a priority after it has already caused damage.",
        ],
        "correct_index": 1,
        "explanation": "MTU é tamanho máximo, e mismatch quebra. A primeira confunde MTU com MSS (MSS é derivado de MTU, mas não é a mesma coisa). A terceira reduz MTU a jumbo frames apenas. A quarta confunde MTU com banda, MTU é em bytes.",
        "explanation_en": "MTU is maximum size, and mismatch breaks things. The first confuses MTU with MSS (MSS is derived from MTU, but is not the same). The third reduces MTU to jumbo frames only. The fourth confuses MTU with bandwidth; MTU is in bytes.",
    },
    {
        "category": "Networking",
        "statement": "O que é HTTP/2 e qual ganho em relação ao HTTP/1.1?",
        "statement_en": "What is HTTP/2 and what is the gain versus HTTP/1.1?",
        "choices": [
            "Versão que troca TCP por QUIC sobre UDP, eliminando handshake TLS separado e mitigando head-of-line bloqueante em redes com perda, atalho que funciona hoje mas complica a próxima migração, comportamento que confunde quem está debugando meses depois.",
            "Protocolo binário que multiplexa várias streams em uma única conexão TCP, eliminando o head-of-line bloqueante do HTTP/1.1, com compressão de cabeçalhos (HPACK) e suporte a server push (raramente usado).",
            "Protocolo full-duplex sobre uma única conexão TCP; substitui HTTP para casos de tempo real e elimina o overhead de cabeçalhos, erro típico de configuração feita às pressas, sem revisão posterior, abordagem que funciona bem até o primeiro pico de carga real.",
            "Variante do HTTPS que usa compressão `gzip` automática em grande parte do payload, dispensando configuração no servidor, abordagem que resolve o sintoma, não a causa raiz do problema, prática ainda comum em sistema legado que raramente é atualizado.",
        ],
        "choices_en": [
            "A version that swaps TCP for QUIC over UDP, eliminating a separate TLS handshake and mitigating head-of-line blocking on lossy networks, a shortcut that works today but complicates the next migration, behavior that confuses whoever debugs months later.",
            "A binary protocol that multiplexes several streams on a single TCP connection, eliminating HTTP/1.1 head-of-line blocking, with header compression (HPACK) and server push support (rarely used).",
            "A full-duplex protocol over a single TCP connection; it replaces HTTP for real-time cases and eliminates header overhead, a typical error from rushed configuration without later review, an approach that works fine until the first real load spike.",
            "An HTTPS variant that uses automatic `gzip` compression on most of the payload, needing no server configuration, an approach that treats the symptom rather than the root cause, a practice still common in legacy systems that are rarely updated.",
        ],
        "correct_index": 1,
        "explanation": "HTTP/2 = binário + multiplexing + HPACK. A primeira descreve HTTP/3. A terceira descreve WebSocket. A quarta confunde com compressão gzip de body, que é independente da versão HTTP.",
        "explanation_en": "HTTP/2 = binary plus multiplexing plus HPACK. The first describes HTTP/3. The third describes WebSocket. The fourth confuses it with body gzip compression, which is independent of the HTTP version.",
    },
    {
        "category": "Networking",
        "statement": "Como mitigar ataques DDoS em camada de aplicação (L7)?",
        "statement_en": "How do you mitigate application-layer (L7) DDoS attacks?",
        "choices": [
            "Defesa em profundidade na borda: WAF com regras OWASP/custom, rate limiting por IP/usuário/`geohash`, bot detection (`reCAPTCHA`/comportamento), CDN com proteção anti-DDoS (Cloudflare, AWS Shield, Akamai) e circuit breakers nos backends para isolar falhas.",
            "Escalar horizontalmente o número de réplicas até absorver o tráfego; algum filtro adicional é necessário se há capacidade suficiente, prática que aumenta a superfície de ataque sem ninguém perceber, decisão que funciona no papel, mas não sobrevive ao primeiro incidente real.",
            "Bloquear o intervalo `/24` do IP atacante por 24 horas no firewall; ataques distribuídos geralmente vêm de poucos blocos contíguos, suposição que só vale em ambiente de desenvolvimento, não em produção, decisão que cria dívida técnica silenciosa, sem gerar erro imediato.",
            "Desligar o serviço por alguns minutos para que o atacante perca o interesse; é um padrão eficaz para ataques volumétricos curtos, abordagem que funciona bem até o primeiro pico de carga real, comportamento que só some quando alguém finalmente lê a documentação.",
        ],
        "choices_en": [
            "Defense in depth at the edge: WAF with OWASP/custom rules, rate limiting by IP/user/`geohash`, bot detection (`reCAPTCHA`/behavior), CDN with anti-DDoS protection (Cloudflare, AWS Shield, Akamai), and circuit breakers on backends to isolate failures.",
            "Scale replicas horizontally until traffic is absorbed; some additional filter is needed if there is enough capacity, a practice that grows the attack surface without anyone noticing, a decision that works on paper but does not survive the first real incident.",
            "Block the attacker's `/24` range for 24 hours on the firewall; distributed attacks usually come from a few contiguous blocks, an assumption that only holds in development, not production, a decision that creates silent technical debt without an immediate error.",
            "Turn the service off for a few minutes so the attacker loses interest; it is an effective pattern for short volumetric attacks, an approach that works fine until the first real load spike, behavior that only goes away when someone finally reads the docs.",
        ],
        "correct_index": 0,
        "explanation": "DDoS L7 = WAF + rate limiting + bot detection + CDN + circuit breaker. A segunda escala custo sem reduzir tráfego malicioso. A terceira é falsa (DDoS modernos vêm de botnets dispersas). A quarta entrega o ataque (DoS auto-infligido).",
        "explanation_en": "L7 DDoS = WAF plus rate limiting plus bot detection plus CDN plus circuit breaker. The second scales cost without reducing malicious traffic. The third is false (modern DDoS comes from dispersed botnets). The fourth delivers the attack (self-inflicted DoS).",
    },
    {
        "category": "Networking",
        "statement": "Onde BGP aparece em ambientes de cloud?",
        "statement_en": "Where does BGP appear in cloud environments?",
        "choices": [
            "Protocolo de roteamento dinâmico usado para anunciar/receber prefixos entre sistemas autônomos; em cloud aparece em conexões dedicadas (`Direct Connect`/`Interconnect`/`ExpressRoute`) e em redes overlay (Calico, Cilium) para anunciar rotas de pod.",
            "Protocolo de roteamento interior baseado em estado de enlace, usado dentro de uma única região cloud para distribuir rotas entre sub-redes, erro típico de configuração feita às pressas, sem revisão posterior, atalho que troca segurança por conveniência de curto prazo.",
            "Protocolo de descoberta que mapeia nomes de domínio para IPs em redes corporativas; é precursor do DNS moderno, abordagem que ignora o histórico de incidentes parecidos no setor, abordagem que ignora o cenário de falha mais provável na prática.",
            "Protocolo restrito a redes locais (LAN) para descobrir vizinhos via multicast; não escala além do limite de um broadcast domain, abordagem que ignora o cenário de falha mais provável na prática, suposição incorreta sobre como o sistema realmente se comporta sob estresse.",
        ],
        "choices_en": [
            "A dynamic routing protocol used to advertise/receive prefixes between autonomous systems; in cloud it appears on dedicated connections (`Direct Connect`/`Interconnect`/`ExpressRoute`) and in overlay networks (Calico, Cilium) to advertise pod routes.",
            "An interior link-state routing protocol used within a single cloud region to distribute routes between subnets, a typical error from rushed configuration without later review, a shortcut that trades security for short-term convenience.",
            "A discovery protocol that maps domain names to IPs on corporate networks; it is a precursor of modern DNS, an approach that ignores the history of similar incidents in the industry, an approach that ignores the most likely failure scenario in practice.",
            "A protocol restricted to local networks (LAN) for discovering neighbors via multicast; it does not scale beyond a broadcast domain boundary, an approach that ignores the most likely failure scenario in practice, an incorrect assumption about how the system really behaves under stress.",
        ],
        "correct_index": 0,
        "explanation": "BGP = inter-AS, comum em conexões cloud-onprem e em CNIs com BGP mode. A segunda descreve OSPF (link-state, IGP). A terceira descreve algo parecido com NIS/DNS legacy. A quarta descreve LLDP/CDP (LAN discovery), não BGP.",
        "explanation_en": "BGP = inter-AS, common on cloud-onprem connections and in CNIs with BGP mode. The second describes OSPF (link-state, IGP). The third describes something like NIS/legacy DNS. The fourth describes LLDP/CDP (LAN discovery), not BGP.",
    },
    # ── DBs em produção (4) ────────────────────────────────────────────────
    {
        "category": "Bancos de dados",
        "statement": "Como controlar conexões longas/ociosas em um banco em produção?",
        "statement_en": "How do you control long/idle connections on a production database?",
        "choices": [
            "Configurar pool de conexões na aplicação com timeouts (idle/connection lifetime), usar `pgbouncer`/`pgpool` em modo `transaction` para multiplexar conexões e definir `statement_timeout`/`idle_in_transaction_session_timeout` no Postgres.",
            "Aumentar `max_connections` no Postgres para um número grande (ex.: 5000) para que conexões ociosas não sejam um problema; o overhead por conexão é desprezível, comportamento que gera alerta falso ou silencia alerta real, dependendo do caso.",
            "Criar um cron job que executa `pg_terminate_backend` em todas as conexões com mais de 10 minutos; resolve o problema sem alterar a aplicação, atalho que parece seguro isolado, mas quebra quando combinado com outros sistemas.",
            "Reiniciar a aplicação a cada 6 horas para forçar reabertura de conexões; pools modernos não suportam reciclagem automática, resultado típico de copiar configuração de outro projeto sem adaptar.",
        ],
        "choices_en": [
            "Configure an application connection pool with timeouts (idle/connection lifetime), use `pgbouncer`/`pgpool` in `transaction` mode to multiplex connections, and set `statement_timeout`/`idle_in_transaction_session_timeout` in Postgres.",
            "Increase `max_connections` in Postgres to a large number (e.g. 5000) so idle connections are not a problem; per-connection overhead is negligible, behavior that creates false alerts or silences real ones, depending on the case.",
            "Create a cron job that runs `pg_terminate_backend` on all connections older than 10 minutes; it solves the problem without changing the application, a shortcut that looks safe in isolation but breaks when combined with other systems a practice that goes unnoticed until a security audit.",
            "Restart the application every 6 hours to force reconnection; modern pools do not support automatic recycling, a typical result of copying config from another project without adapting it.",
        ],
        "correct_index": 0,
        "explanation": "Pool no app + pgbouncer + timeouts no banco. A segunda subestima o custo por conexão (cada backend Postgres consome RAM significativa). A terceira é solução brusca que pode matar transações ativas. A quarta nega capacidade real dos pools modernos.",
        "explanation_en": "App pool plus pgbouncer plus database timeouts. The second underestimates per-connection cost (each Postgres backend consumes significant RAM). The third is a blunt solution that can kill active transactions. The fourth denies real capability of modern pools.",
    },
    {
        "category": "Bancos de dados",
        "statement": "Diferença entre backup quente (hot) e frio (cold) em bancos relacionais:",
        "statement_en": "Difference between hot and cold backup in relational databases:",
        "choices": [
            "Backup quente exige parar o banco por alguns segundos para garantir consistência; backup frio é tirado com o banco rodando, mas é mais lento, comportamento que confunde quem está debugando meses depois, atalho que parece seguro isolado, mas quebra quando combinado com outros sistemas.",
            "Backup quente é tirado com o banco em uso (snapshot consistente + log de transação) e é o padrão para sistemas modernos; backup frio exige downtime do banco e é reservado para upgrades majors ou cenários edge.",
            "Backup quente raramente é consistente, pois o banco está em uso; por isso só backup frio é confiável para restore de produção, prática que aumenta a superfície de ataque sem ninguém perceber, comportamento que gera alerta falso ou silencia alerta real, dependendo do caso.",
            "Quente é equivalente a `pg_dump` (lógico); frio é equivalente a `pg_basebackup` (físico). Ambos suportam PITR igualmente, decisão que parece inofensiva isolada, mas se acumula com o tempo, resultado típico de copiar configuração de outro projeto sem adaptar.",
        ],
        "choices_en": [
            "Hot backup requires stopping the database for a few seconds to guarantee consistency; cold backup is taken with the database running, but is slower, behavior that confuses whoever debugs months later, a shortcut that looks safe in isolation but breaks when combined with other systems.",
            "Hot backup is taken while the database is in use (consistent snapshot plus transaction log) and is the pattern for modern systems; cold backup requires database downtime and is reserved for major upgrades or edge scenarios.",
            "Hot backup is rarely consistent because the database is in use; therefore only cold backup is reliable for production restore, a practice that grows the attack surface without anyone noticing, behavior that creates false alerts or silences real ones, depending on the case.",
            "Hot is equivalent to `pg_dump` (logical); cold is equivalent to `pg_basebackup` (physical). Both support PITR equally, a decision that looks harmless in isolation but accumulates over time, a typical result of copying config from another project without adapting it.",
        ],
        "correct_index": 1,
        "explanation": "Hot = com banco em uso (padrão); Cold = parado (raro). A primeira inverte. A terceira é falsa: hot backup com snapshot/WAL é consistente. A quarta confunde categorias: lógico vs físico é outro eixo, e `pg_dump` não suporta PITR.",
        "explanation_en": "Hot = with the database in use (default); Cold = stopped (rare). The first swaps them. The third is false: hot backup with snapshot/WAL is consistent. The fourth confuses categories: logical vs physical is another axis, and `pg_dump` does not support PITR.",
    },
    {
        "category": "Bancos de dados",
        "statement": "Como restaurar um banco até um ponto específico no tempo (PITR)?",
        "statement_en": "How do you restore a database to a specific point in time (PITR)?",
        "choices": [
            "Restaurar um backup físico base (`pg_basebackup` ou snapshot) e fazer replay dos logs de transação (WAL no Postgres, binlog no MySQL) até o LSN/timestamp desejado, geralmente parando logo antes do incidente.",
            "Restaurar só o último backup completo; PITR é um nome alternativo para 'restore from latest snapshot', suposição que só vale em ambiente de desenvolvimento, não em produção, suposição que raramente se sustenta fora do ambiente controlado de laboratório.",
            "Reaplicar manualmente o `pg_dump` lógico em um banco vazio até a transação alvo; logs de WAL não são necessários, atalho que ignora exatamente o cenário que mais importa evitar, suposição que ignora como o recurso realmente se comporta em escala.",
            "Reiniciar o banco com a flag `--recover-to-time=..`; o engine determina sozinho qual backup base usar, abordagem que ignora o cenário de falha mais provável na prática, prática que aumenta a superfície de ataque sem ninguém perceber.",
        ],
        "choices_en": [
            "Restore a base physical backup (`pg_basebackup` or snapshot) and replay transaction logs (WAL in Postgres, binlog in MySQL) to the desired LSN/timestamp, usually stopping just before the incident.",
            "Restore only the latest full backup; PITR is an alternative name for 'restore from latest snapshot', an assumption that only holds in development, not production, an assumption that rarely holds outside a controlled lab environment.",
            "Manually reapply the logical `pg_dump` on an empty database up to the target transaction; WAL logs are not needed, a shortcut that ignores exactly the scenario that matters most to avoid, an assumption that ignores how the resource actually behaves at scale.",
            "Restart the database with the `--recover-to-time=..` flag; the engine alone determines which base backup to use, an approach that ignores the most likely failure scenario in practice, a practice that grows the attack surface without anyone noticing.",
        ],
        "correct_index": 0,
        "explanation": "PITR = backup base + replay de WAL. A segunda confunde com restore from snapshot. A terceira é falsa: `pg_dump` é snapshot lógico do momento, não permite parar em uma transação específica. A quarta inventa uma flag inexistente.",
        "explanation_en": "PITR = base backup plus WAL replay. The second confuses it with restore from snapshot. The third is false: `pg_dump` is a logical snapshot of a moment and does not let you stop at a specific transaction. The fourth invents a nonexistent flag.",
    },
    {
        "category": "Bancos de dados",
        "statement": "Trade-off entre replicação síncrona e assíncrona:",
        "statement_en": "Trade-off between synchronous and asynchronous replication:",
        "choices": [
            "Síncrona é mais rápida pois não espera réplicas; assíncrona obriga o primário a aguardar todas as confirmações antes de retornar ao cliente, prática que gera falso senso de segurança no time, atalho que funciona hoje mas complica a próxima migração, suposição que raramente se sustenta fora do ambiente controlado de laboratório.",
            "Síncrona implica em RTO=0 e assíncrona em RTO infinito; o conceito não tem relação direta com perda de dados, decisão que cria dívida técnica silenciosa, sem gerar erro imediato, atalho que funciona hoje mas complica a próxima migração, comportamento que gera alerta falso ou silencia alerta real, dependendo do caso.",
            "Síncrona: o commit no primário só retorna ao cliente após a réplica ter persistido a transação, garantindo RPO próximo de zero à custa de latência adicional. Assíncrona: commit retorna imediatamente, com chance de perda das últimas transações se o primário falhar.",
            "Síncrona requer quorum de 2/3 das réplicas; assíncrona aceita réplica única, mas exige que o primário esteja na mesma região, suposição que só vale em ambiente de desenvolvimento, não em produção, abordagem que resolve o sintoma, não a causa raiz do problema, decisão que cria dívida técnica silenciosa, sem gerar erro imediato.",
        ],
        "choices_en": [
            "Synchronous is faster because it does not wait for replicas; asynchronous forces the primary to await all confirmations before returning to the client, a practice that gives the team a false sense of safety, a shortcut that works today but complicates the next migration, an assumption that rarely holds outside a controlled lab environment.",
            "Synchronous implies RTO=0 and asynchronous implies infinite RTO; the concept has no direct relation to data loss, a decision that creates silent technical debt without an immediate error, a shortcut that works today but complicates the next migration, behavior that creates false alerts or silences real ones, depending on the case.",
            "Synchronous: a commit on the primary only returns to the client after the replica has persisted the transaction, guaranteeing near-zero RPO at the cost of extra latency. Asynchronous: the commit returns immediately, with a chance of losing the latest transactions if the primary fails.",
            "Synchronous requires a 2/3 quorum of replicas; asynchronous accepts a single replica, but requires the primary to be in the same region, an assumption that only holds in development, not production, an approach that treats the symptom rather than the root cause, a decision that creates silent technical debt without an immediate error.",
        ],
        "correct_index": 2,
        "explanation": "Síncrona = RPO~0, mais latência; assíncrona = rápida, pode perder TX. A primeira inverte (síncrona é mais lenta). A segunda confunde RPO com RTO. A quarta inventa requisitos de quorum/região que não fazem parte da definição.",
        "explanation_en": "Synchronous = RPO~0, more latency; asynchronous = fast, may lose TX. The first swaps them (synchronous is slower). The second confuses RPO with RTO. The fourth invents quorum/region requirements that are not part of the definition.",
    },
    # ── Linux administração (5) ────────────────────────────────────────────
    {
        "category": "Linux",
        "statement": "Como depurar um processo travado em syscall sem perder evidência?",
        "statement_en": "How do you debug a process stuck in a syscall without losing evidence?",
        "choices": [
            "Executar `kill -9 <pid>` imediatamente para liberar recursos; análise pode ser feita posteriormente nos logs do journal, prática que passa despercebida até uma auditoria de segurança, decisão que parece segura até o primeiro teste de penetração real.",
            "Inspeção não-destrutiva primeiro: `cat /proc/<pid>/stack` (kernel stack), `cat /proc/<pid>/wchan`, `strace -p <pid>` (syscalls em curso), `perf record -p <pid>` para profiling e logs do serviço; matar (`kill -9`) só após coletar evidências.",
            "Reiniciar o host (`reboot`) é a forma mais segura, pois preserva vários logs e libera recursos sem risco de corrupção, abordagem que funciona bem até o primeiro pico de carga real, comportamento que confunde quem está debugando meses depois.",
            "Só rodar `dmesg | tail -50` para ver mensagens do kernel; processos travados em syscall geralmente deixam rastro no ring buffer, prática que troca previsibilidade por economia de esforço imediato, decisão que funciona no papel, mas não sobrevive ao primeiro incidente real.",
        ],
        "choices_en": [
            "Run `kill -9 <pid>` immediately to free resources; analysis can be done later in journal logs, a practice that goes unnoticed until a security audit, a decision that looks safe until the first real penetration test.",
            "Non-destructive inspection first: `cat /proc/<pid>/stack` (kernel stack), `cat /proc/<pid>/wchan`, `strace -p <pid>` (in-flight syscalls), `perf record -p <pid>` for profiling and service logs; kill (`kill -9`) only after collecting evidence.",
            "Rebooting the host (`reboot`) is the safest approach, because it preserves many logs and frees resources without corruption risk, an approach that works fine until the first real load spike, behavior that confuses whoever debugs months later.",
            "Only run `dmesg | tail -50` to see kernel messages; processes stuck in a syscall usually leave a trail in the ring buffer, a practice that trades predictability for immediate effort savings, a decision that works on paper but does not survive the first real incident.",
        ],
        "correct_index": 1,
        "explanation": "Diagnose primeiro (procfs + strace + perf), depois mate. A primeira destrói evidência. A terceira é exagerada e perde tudo que estava em memória. A quarta é generalização errada, nem toda syscall trava deixa registro no `dmesg`.",
        "explanation_en": "Diagnose first (procfs plus strace plus perf), then kill. The first destroys evidence. The third is exaggerated and loses everything that was in memory. The fourth is a wrong generalization; not every stuck syscall leaves a `dmesg` record.",
    },
    {
        "category": "Linux",
        "statement": "Diferença entre ulimit e cgroups:",
        "statement_en": "Difference between ulimit and cgroups:",
        "choices": [
            "cgroups limitam recursos por sessão de shell; ulimit controla grupos de processos em /sys/fs/cgroup, suposição incorreta sobre como o sistema realmente se comporta sob estresse.",
            "ulimit foi substituído por namespaces no kernel moderno, então só cgroups fazem controle de recursos, prática ainda comum em sistema legado que raramente é atualizado.",
            "ulimit aplica rlimits por processo/sessão; cgroups aplicam limites e contabilidade por grupo (CPU, memória, IO, PIDs).",
            "ulimit e cgroups só funcionam com SELinux habilitado, caso contrário os limites viram só aviso, comportamento que só é notado quando alguém audita os logs depois.",
        ],
        "choices_en": [
            "cgroups limit resources per shell session; ulimit controls process groups under /sys/fs/cgroup, an incorrect assumption about how the system really behaves under stress.",
            "ulimit was replaced by namespaces in the modern kernel, so only cgroups control resources, a practice still common in legacy systems that are rarely updated.",
            "ulimit applies per-process/session rlimits; cgroups apply limits and accounting per group (CPU, memory, IO, PIDs).",
            "ulimit and cgroups only work with SELinux enabled; otherwise limits become warnings only, behavior noticed only when someone audits the logs later.",
        ],
        "correct_index": 2,
        "explanation": "ulimit = por processo/sessão; cgroups = por grupo. A primeira inverte. A segunda é falsa em duas frentes: cgroups continuam ativos (aliás, cgroups v2 é o moderno) e `nsenter` é para namespaces, não substitui cgroups. A quarta confunde com SELinux/MAC.",
        "explanation_en": "ulimit = per process/session; cgroups = per group. The first swaps them. The second is false on two fronts: cgroups remain active (indeed, cgroups v2 is modern) and `nsenter` is for namespaces, not a cgroups substitute. The fourth confuses this with SELinux/MAC.",
    },
    {
        "category": "Linux",
        "statement": "Como configurar coleta de core dumps em produção Linux?",
        "statement_en": "How do you configure core dump collection on production Linux?",
        "choices": [
            "Configurar `kernel.core_pattern` (ex.: pipe para `systemd-coredump`), aumentar `ulimit -c unlimited` para o serviço, garantir espaço em disco adequado e armazenar/copiar os dumps fora do host (S3, NFS) para análise com `gdb`/`crash`.",
            "Habilitar `kernel.printk=8 8 8 8` e ler core dumps via `dmesg`; o kernel inclui o estado do processo nas mensagens em modo debug, decisão que funciona no papel, mas não sobrevive ao primeiro incidente real, comportamento que só vira prioridade depois que já causou prejuízo.",
            "Só configurar `ulimit -c unlimited` é suficiente; o kernel grava o dump em `/tmp` por padrão e o `journald` o coleta automaticamente, decisão que parece razoável isolada, mas quebra a arquitetura no conjunto, comportamento que só some quando alguém finalmente lê a documentação.",
            "Só reiniciar o serviço com `systemctl restart` ativa o coletor de core dumps; sem reinício, o kernel ignora `SIGSEGV`, atalho que troca segurança por conveniência de curto prazo, atalho que troca segurança por conveniência de curto prazo.",
        ],
        "choices_en": [
            "Configure `kernel.core_pattern` (e.g. pipe to `systemd-coredump`), raise `ulimit -c unlimited` for the service, ensure adequate disk space, and store/copy dumps off-host (S3, NFS) for analysis with `gdb`/`crash`.",
            "Enable `kernel.printk=8 8 8 8` and read core dumps via `dmesg`; the kernel includes process state in debug-mode messages, a decision that works on paper but does not survive the first real incident, behavior that only becomes a priority after it has already caused damage.",
            "Only setting `ulimit -c unlimited` is enough; the kernel writes the dump to `/tmp` by default and `journald` collects it automatically, a decision that seems reasonable in isolation but breaks the architecture overall, behavior that only goes away when someone finally reads the docs.",
            "Only restarting the service with `systemctl restart` enables the core dump collector; without restart, the kernel ignores `SIGSEGV`, a shortcut that trades security for short-term convenience, a shortcut that trades security for short-term convenience.",
        ],
        "correct_index": 0,
        "explanation": "core_pattern + ulimit + storage offsite + ferramentas de análise. A segunda confunde dump com mensagens do kernel. A terceira é meia-verdade: `ulimit -c unlimited` sozinho não basta, sem `core_pattern` correto, o dump pode ir para lugar nenhum. A quarta inventa um comportamento de inicialização inexistente.",
        "explanation_en": "core_pattern plus ulimit plus offsite storage plus analysis tools. The second confuses dump with kernel messages. The third is a half-truth: `ulimit -c unlimited` alone is not enough; without a correct `core_pattern`, the dump may go nowhere. The fourth invents a nonexistent startup behavior.",
    },
    {
        "category": "Linux",
        "statement": "Qual é a função do `journald` (`systemd-journald`)?",
        "statement_en": "What is the role of `journald` (`systemd-journald`)?",
        "choices": [
            "Captura logs estruturados (campos `_PID`, `_UID`, `_SYSTEMD_UNIT` etc.) de unidades systemd, kernel e syslog tradicional; consultados via `journalctl` com filtros, persistidos em `/var/log/journal` quando habilitado.",
            "Daemon que recebe logs via syslog UDP/TCP e os encaminha para arquivos por facility/severity; substitui completamente o `systemd-journald` em distros recentes, prática que gera falso senso de segurança no time, erro típico de configuração feita às pressas, sem revisão posterior.",
            "Buffer circular do kernel acessado via `dmesg`; contém só mensagens do kernel e não captura logs de aplicações, suposição que raramente se sustenta fora do ambiente controlado de laboratório, atalho que troca segurança por conveniência de curto prazo.",
            "Mecanismo do systemd que substitui o `cron` para agendamento de tarefas e gera logs de execução em `/var/log/syslog`, decisão que cria dívida técnica silenciosa, sem gerar erro imediato, que só aparece como problema depois que o sistema já está em produção.",
        ],
        "choices_en": [
            "It captures structured logs (fields `_PID`, `_UID`, `_SYSTEMD_UNIT`, etc.) from systemd units, kernel, and traditional syslog; queried via `journalctl` with filters, persisted under `/var/log/journal` when enabled.",
            "A daemon that receives logs via syslog UDP/TCP and forwards them to files by facility/severity; it fully replaces `systemd-journald` on recent distros, a practice that gives the team a false sense of safety, a typical error from rushed configuration without later review.",
            "The kernel circular buffer accessed via `dmesg`; it contains only kernel messages and does not capture application logs, an assumption that rarely holds outside a controlled lab environment, a shortcut that trades security for short-term convenience.",
            "A systemd mechanism that replaces `cron` for task scheduling and writes execution logs to `/var/log/syslog`, a decision that creates silent technical debt without an immediate error, which only shows up as a problem after the system is already in production.",
        ],
        "correct_index": 0,
        "explanation": "journald = logs estruturados de systemd/kernel/syslog. A segunda descreve `rsyslog`/`syslog-ng` (que normalmente coexistem com journald, não o substituem). A terceira descreve o ring buffer do kernel (`dmesg`). A quarta descreve `systemd timers` (substituto de cron), que é outro componente do systemd.",
        "explanation_en": "journald = structured logs from systemd/kernel/syslog. The second describes `rsyslog`/`syslog-ng` (which usually coexist with journald, not replace it). The third describes the kernel ring buffer (`dmesg`). The fourth describes `systemd timers` (cron substitute), another systemd component.",
    },
    {
        "category": "Linux",
        "statement": "O que é o OOM killer do kernel Linux?",
        "statement_en": "What is the Linux kernel OOM killer?",
        "choices": [
            "Encerra vários processos do usuário ofensor para garantir que a memória seja liberada de uma vez; não há seleção individual, comportamento que só some quando alguém finalmente lê a documentação, resultado típico de copiar configuração de outro projeto sem adaptar.",
            "Mecanismo do kernel acionado quando memória + swap se esgotam; calcula um `oom_score` por processo (consumo, prioridade, ajustes via `oom_score_adj`) e mata o de maior score, liberando memória; configurável também por cgroup memory (memcg OOM).",
            "Funciona só em containers porque depende do `cgroup memory`; em hosts puros sem cgroups, o kernel só faz swap até travar, suposição que raramente se sustenta fora do ambiente controlado de laboratório, comportamento que só é notado quando alguém audita os logs depois.",
            "Provoca `kernel panic` para forçar reinício automático e liberar a memória; é desabilitado por padrão em produção via `vm.panic_on_oom=0`, suposição incorreta sobre como o sistema realmente se comporta sob estresse, suposição que só se sustenta enquanto o time é pequeno.",
        ],
        "choices_en": [
            "It terminates many processes of the offending user to free memory at once; there is no individual selection, behavior that only goes away when someone finally reads the docs, a typical result of copying config from another project without adapting it.",
            "A kernel mechanism triggered when memory plus swap are exhausted; it computes an `oom_score` per process (usage, priority, adjustments via `oom_score_adj`) and kills the highest score, freeing memory; also configurable per memory cgroup (memcg OOM).",
            "It only works in containers because it depends on `cgroup memory`; on bare hosts without cgroups, the kernel only swaps until it hangs, an assumption that rarely holds outside a controlled lab environment, behavior noticed only when someone audits the logs later.",
            "It causes a `kernel panic` to force an automatic reboot and free memory; it is disabled by default in production via `vm.panic_on_oom=0`, an incorrect assumption about how the system really behaves under stress, an assumption that only holds while the team is small.",
        ],
        "correct_index": 1,
        "explanation": "OOM killer escolhe vítima por `oom_score`, ajustável por `oom_score_adj`. A primeira é generalização errada (não mata 'todos do usuário'). A terceira é falsa: OOM killer existe há décadas, antes de cgroups. A quarta confunde OOM kill (mata processo) com `vm.panic_on_oom` (que é uma opção de panic, mas não é o comportamento padrão).",
        "explanation_en": "The OOM killer chooses a victim by `oom_score`, adjustable via `oom_score_adj`. The first is a wrong generalization (it does not kill 'everyone of the user'). The third is false: the OOM killer has existed for decades, before cgroups. The fourth confuses OOM kill (kills a process) with `vm.panic_on_oom` (a panic option, but not the default behavior).",
    },
]
